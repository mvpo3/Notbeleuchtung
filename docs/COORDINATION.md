# COORDINATION — 2-Fenster-Parallelbetrieb

**Zweck:** Zwei Claude-Code-Sessions arbeiten parallel in getrennten Worktrees. Diese
Datei ist das gemeinsame Board — **beide Fenster lesen + pflegen sie**. Da Sessions
keinen Live-Speicher teilen, läuft die Kommunikation über **Git** + diese Datei.

## Aufteilung (kein Datei-Overlap)

| Fenster | Ordner | Branch | Bereich |
|---|---|---|---|
| **F1** | `Notbeleuchtung/` | `leonis/anker-platzierer` | `src/notbeleuchtung/platzierung/` + `hauptengine/` — Platzier-Pipeline (Anker/Linie/Fläche/Deckung) |
| **F2** | `Notbeleuchtung-F2/` | `leonis/ldt-photometrie` | `src/notbeleuchtung/normwissen/photometrie/` — (b) IES/LDT-Import → exakte Lux |

## Regeln (bindend)
- **Eigenes Package = frei** parallel bearbeiten.
- **`hauptengine/contracts/**` = Konsens** (Contract-Freeze-Regel, beide Fenster + Version-Bump). Wer einen Contract ändern will → hier eintragen + abwarten.
- **Kleine Commits**, eine Sache pro Commit. Nach jedem Schritt Status unten aktualisieren + committen.
- **Sync:** `git fetch` + `git show origin/<other-branch>:docs/COORDINATION.md` (oder Board unten lesen). Das andere Fenster sieht Änderungen erst nach Fetch/Pull.
- **Naht/Lux:** F2 liefert `photometrie`-API; F1 konsumiert sie später in `platzierung/lux.py`. Schnittstelle unten festhalten, bevor F1 sie einbaut.

## Schnittstelle F2 → F1 (Lux-Photometrie)
F2 baut: `normwissen/photometrie/ldt.py` mit
`lade_ldt(pfad) -> Photometrie` und `Photometrie.intensitaet(gamma_grad, c_grad) -> cd`.
F1 tauscht dann in `platzierung/lux.py` das konstante `i_cd` gegen `Photometrie.intensitaet`.
**Contract bleibt unberührt** (rein additives Modul). → wenn API steht, hier „READY" markieren.

**READY** (F2, 2026-08-30): `normwissen/photometrie/{ldt.py,__init__.py}` steht.
`lade_ldt(pfad) -> Photometrie` (EULUMDAT, alle Isym-Symmetrien) +
`Photometrie.intensitaet(gamma_grad, c_grad=0.0) -> cd` (bilinear γ×C, cd über
`lampen_lumen` skaliert). F1 kann `lux.py` umstellen. Noch offen: Test gegen echte
Schrack-LDT (Owner besorgt Datei) — bis dahin synthetische Fixture `tests/fixtures/photometrie/mini.ldt`.

## Status-Board (live — nach jedem Schritt updaten + committen)

### F1 (Platzierung)
- [x] (a) Anker-Platzierer (`anker_strategy`) — committed
- [x] (b-vorbereitet) `mittellinie` + `lux` — committed
- [x] (c) `place()` Durchstich Anker→Linie→Fläche→Deckung (`deckung.py`) — committed
- [x] `lux.py`/`deckung.py` auf F2-Photometrie umstellbar: neues `i_cd_fn(γ)`-Callable
  (Dependency-Injection, KEIN `normwissen`-Import → Import-Grenze gewahrt); Hauptengine
  baut es aus `Photometrie.intensitaet`. Fallback bleibt konstant `i_cd`. (Branch `leonis/lux-photometrie`)
- [ ] offen: PR für anker-platzierer-Branch; Hauptengine/pipeline: `i_cd_fn` real verdrahten

### F2 (Photometrie / LDT)
- [x] `normwissen/photometrie/ldt.py` — LDT/EULUMDAT-Parser (Kopf + Lampensatz + Isym-Expansion)
- [x] `Photometrie.intensitaet(gamma, c)` mit Winkel-Interpolation (bilinear γ×C, periodisch)
- [x] `normwissen/photometrie/ies.py` — IES/LM-63-Parser (`lade_ies`), gleicher `Photometrie`-Typ →
  F1-Naht unverändert; horizontale Symmetrie-Expansion (rot./quadrant/bilateral), abs. Fotometrie
- [ ] Test gegen eine echte Schrack-LDT/-IES (Owner besorgt Datei) — bis dahin synthetische Fixtures
- [x] API READY-Meldung hier → F1 baut sie in `lux.py` ein

### F2 → umgelenkt auf Raumerkennung (Selman-Package)
Branch `selman/raumerkennung-dxf`. Baut echten `ArchitekturRaumProvider.parse(dxf, floor)
-> RaumModell` (ersetzt `FakeRaumProvider` schrittweise). Schlanker Neubau in
`raumerkennung/`, wiederverwendet pure Port-Helfer (`room_faces`, `classify_room`).
Primärziel Mollgasse (mm). **Kein Contract-Touch** (rein additiv). E2E bleibt vorerst
auf Fake (4OG-Golden matcht keinen echten DXF) — Fake-Swap = späterer Slice mit F1-Konsens.
- [x] S0 Scaffold (`provider.py` Stub, `tests/raumerkennung/`) — Suite grün (69), committed
- [x] S1 `dxf_load` (öffnen + $INSUNITS→mm + `bounds_mm`) — Mollgasse-Test grün (71), committed
- [x] S2 `waende` (Segmente → `extract_room_faces` → Räume) — **synth grün (2 Räume)**, aber
  ⚠️ **echte Mollgasse = 184 Wand-Schlitze statt Räume** (Doppellinien-Wände + Türlücken).
  Naive Polygonisierung UND shapely-Buffer-Difference scheitern ohne Gap-Healing/virtuelle
  Wände (= die 14k-LOC-Port-Maschinerie). Offene Design-Frage an F1/Owner (siehe unten).
- [x] S4 `tueren` (TÜR-INSERTs → `Tuer`, Breite aus Blockname cm→mm) — **Mollgasse echt grün**
  (≥10 Türen, Achsmarker/Türöffner ausgeschlossen). Suite 75.
- [x] S5 `zirkulation` (09-WEG → `FluchtwegSegment` + networkx-Graph + Ausgänge) —
  **Mollgasse echt grün** (77 Weg-Polylinien → Segmente + Knoten/Kanten). Suite 77.
- [x] S3 `raumtyp` (Stempel → raum_typ + Flags, Point-in-Polygon) — synth grün.
- [x] S6 `provider.parse` full → valides `RaumModell`, Contract-Roundtrip grün. Suite **80**.
- Reihenfolge: Owner wählte **B** — erst Türen/Fluchtweg (echt-nutzbar), Raum-Polygone später.

**Raum-Layer-Reader (`raumlayer.py`) — echte Raum-Polygone:** 3 von 4 Familien haben
fertige Raum-Polygone auf Layern (`81\d Raum`/`Raumbegrenzung`/`A_Raeume`) + Name via
`ROOM_NAME`-ATTRIB oder MTEXT → `classify_room`. Löst das Schlitz-Problem auditierbar
(kein ML). Fischamender **68**, Herrenholz **473**, Baufeld **220** echte typisierte Räume;
Mollgasse Fallback Wand-Polygonize. Suite **93**. (Barawitzka 2 = Nacharbeit.)

**Cross-Projekt-Fundament (alle 5 Familien):** Wand-Layer per Muster (`WALL_PATTERN`:
`02-TWA/ZWA/WDA` · `A_Waende` · `1[123]0 Wand`) + Skala robust (Span-Gate 15–500 m +
Tür-ARC-Radius-Tiebreak). Korrekt für Mollgasse/Fischamender/Barawitzka(80m, war 8m!)/
Herrenholz. Hauptausgang-Doppeltür bisher nur Mollgasse kalibriert; Fischamender braucht
Block-Descent (Tür-ARCs nested), Barawitzka schärferes Paar-Kriterium. Suite **91**.

**FIX-2/3 Hauptausgänge = DOPPELTÜR am Rand (`footprint.py`):** Owner-Muster: Gebäude-
Haupteingang wird als **Doppeltür** gezeichnet, 1–2 je Gebäude/Stiegenhaus. Naive Geometrie
(Polygonize/Buffer/Hülle) scheitert am lückigen nicht-konvexen Wandwerk → **Raster-Flood-
Fill** für Gebäude-Umriss. Hauptausgang = Paar gleich großer Tür-Schwenkbögen (ARCs,
r≈600–1300mm, Drehpunkte 1.4–2.6m auseinander) **an der Außenkante**. Mollgasse EG:
**4 Hauptausgänge**, beide Blöcke/Stiegenhäuser. Suite **87**. Cross-Projekt-Profile offen.

**FIX-1 (Ausgänge + Skala, nach Projekt-Analyse aller 6 Ordner):**
- **Ausgänge neu = Außentüren** (`WET_AUSSEN`/`WET`/`SCHIEBETÜR`/`Fenstertür`, `final_exit`).
  Alte Heuristik (09-WEG-Endknoten nahe Bounding-Box) war Müll (11 Punkte am Planrahmen)
  → jetzt 6 echte Egress-Türen. Ground-Truth-Abgleich: sitzen an/nahe den RZ.
- **Skala aus Geometrie kalibriert** (Dekaden-Snap), `$INSUNITS` ignoriert — leerer
  Mollgasse-Input steht in METERN (×1000), fertiger in mm. Beide deklarieren fälschlich `4`.
- Analyse aller Projekte: 3 Konventionen (Mollgasse `02-*`, Fischamender `A_*`, ArchiCAD
  numerisch `1xx/8xx`), 3 leere Inputs haben **Raum-Polygone auf eigenen Layern**
  (`_815`/`810 Raum`/`A_Raeume`) → löst Schlitz-Problem (nächster Slice). Suite **86**.

**READY (Teil-Naht):** `ArchitekturRaumProvider.parse(dxf, floor) -> RaumModell` steht.
Echte Mollgasse-EG: **40 Türen, 11 Ausgänge, 103 Fluchtweg-Segmente, Bounds** — echt
nutzbar für Leonis/Render. ⚠️ **Raum-Polygone** (184 Wand-Schlitze) noch NICHT
produktiv — brauchen Gap-Healing (Folge-Slice). E2E-Fake-Swap erst mit neuer Golden
(F1-Konsens). Branch `selman/raumerkennung-dxf` bereit für PR (User-GO nötig).

**OFFENE FRAGE (Owner):** Echte Raum-*Polygone* brauchen dicke-Wand-Handling. Türen
(INSERT) + Fluchtweg (09-WEG) funktionieren dagegen JETZT auf echten Plänen. Optionen:
(A) Port-Maschinerie reanimieren (virtuelle Wände/room-partition, ~14k LOC), (B) erst
Türen+Fluchtweg+Bounds liefern, Raum-Polygone nur auf sauberen DXF, (C) mittlere
Heuristik (Tür-Öffnungs-Virtualwände selektiv portieren).

## Bugs an F2 (aus F1-Durchstich Fischamender BT1 1.OG, 2026-08-29)
F1 hat den echten End-to-End (Provider → Platzierung → DXF) auf **Fischamender BT1
1.OG** gefahren. Zwei reproduzierbare Provider-Bugs auf dieser CAD-Familie:

- **B1 — Tür-Doppelzählung.** `tueren_aus_dxf` liefert **102** INSERTs, davon **~42
  Quasi-Duplikate <20 cm** (jede Tür als 2 ARC-Schwenkbögen gezählt) → echte ~60.
  Fix: Dedup-Cluster (<300 mm) je Tür-Position, ODER nur einen ARC/INSERT je Türblatt.
- **B2 — A_Fluchtweg + Ausgänge werden nicht gelesen (Fischamender-Familie).**
  `zirkulation_aus_dxf` sucht Mollgasse `09-WEG`; Fischamender-Fluchtweg liegt auf
  **`A_Fluchtweg`** (23 Ent., HATCH-Pfeile + degenerierte Linien, Rohkoords Meter).
  `hauptausgaenge` (footprint) ist nur Mollgasse-kalibriert. Folge: **0 Ausgänge,
  0 Zirkulation** → RZ-Routing unmöglich, F1 musste das Stiegenhaus aus dem
  **`S-STRS`**-Layer (×`plan.factor`) improvisieren. Fix: Layer-Muster + Ausgangs-
  Erkennung pro Familie (wie Wand-Muster `WALL_PATTERN`). Ein Stiegenhaus-Cluster
  für BT1 bei mm (20928, 85023).
- Nebenbefund: **20 von 59 Raum-Polygonen** sind Fragmente/untypisiert (Median 6,6 m²,
  9 unter 2 m²) — bekannte Gap-Healing-Grenze, hier bestätigt.

## Enis-Lane (Normwissen) — Naht-Erwartungen

Enis arbeitet in einer eigenen Session auf `enis/…`-Branches. Zwei Provider aus
`normwissen/` fehlen noch; beide Contracts stehen bereits auf main:

| Zu bauen | Protocol (`ports.py`) | Datengrundlage | Status |
|---|---|---|---|
| `normwissen/oib/` | `OibProvider.bewerte_oib(projekt) -> OibBefund` | `normwissen/data/oib_rl2_tabelle6.yaml` (neu) | TODO |
| `normwissen/lb/` | `LBProvider.parse_lb(lb_path) -> LBVorgabe` | `data/lb_extraktion.yaml` + Digest `knowledge/extracted/LB_ANALYSE_beispiele.md` | **STEHT** (fail closed, s.u.) |

### ✅ Stand 2026-08-31 — LB-Naht geschlossen (Branch `enis/lb-parser`)

**Update 31.08. abends: PR #60 ist gemerged, die LB-Arbeit ist vollständig auf
`main`.** Der Abschnitt bleibt als Beleg für die Feld-für-Feld-Gegenüberstellung
stehen. Ursprünglicher Text:

Der Branch ist auf `origin/main` (`9d3c080`) rebased und wieder lauffähig. Die
öffentliche API von PR #40 bleibt unverändert (`LbTextProvider`, Modul-`parse_lb`,
`registry.build_default_bundle()` unangetastet); dahinter steht die fail-closed
Extraktion mit seitengenauem Audit-Trail (`parse_bericht`).

**Was sich dadurch gegenüber main ändert** (alles an den 4 realen LB-PDFs geprüft):

| Fall | main `9d3c080` | dieser Branch |
|---|---|---|
| Fischa `system_typ` | `zentralbatterie` — wählt still eine Seite des Widerspruchs (Gruppenbatterie S. 19 vs. Zentralbatterie S. 42) | kein Wert + `review_informativ` mit beiden Fundstellen |
| GU-Rahmen (kein Notbeleuchtungs-Abschnitt, reiner Verweis) | erfindet `bereiche_inklusion = [STIEGENHAUS, GARAGE, MUELLRAUM]` | blockierender Review |
| mo-Bau (keine Elektro-Vorgaben) | leere `LBVorgabe` — von „die LB macht keine Vorgaben" nicht unterscheidbar | blockierender Review |
| mo-Elektro `umschaltzeit_max_s` | `None` (Wert geht verloren) | `0.5` |
| mo-Elektro Bereiche | ohne `LAGER` | mit `LAGER` |
| `batterie_standort` | `None` an beiden Elektro-LBs (Muster verlangt `batterie im <Raum>`) | Fischa „Technikraum", mo-Elektro „Zählerraum" |
| Audit-Trail | Dateiname | Datei + § + Seite des **Treffers**, plus voller Befund inkl. Kandidaten |

Die Härtungen aus #45/#56 sind übernommen bzw. nachgebaut (Betriebsdauer-Kontext,
Lux-Wortform, Antipanik-Fenster, Overflow-Guard, `batterie_standort`), alle
16 main-Tests liegen wieder in `tests/normwissen/test_lb_parser.py`.

**✔ ERLEDIGT (PR #67):** die Naht-Frage unten ist beantwortet. `pipeline.py` fängt
`LbFehler` ab und flaggt `render_summary["lb_review"]`; `api/main.py` reicht das Feld
jetzt bis zum Client durch (`/plan` **und** `/projekt`), Review-Meldung im Header bei
600 Zeichen gekappt. Ursprünglicher Text:

**Offene Naht-Frage an @mvpo3:** `pipeline.py:100` ruft `bundle.lb.parse_lb()`
ungeschützt. Fail closed heißt: der Aufruf **wirft** (`LbReviewRequired` /
`LbNichtLesbar`) statt eine leere `LBVorgabe` zu liefern — heute schlägt das als
HTTP 500 durch. Vorschlag: `api/main.py` fängt `LbFehler` und antwortet 422 mit
`bericht.als_text()`. Liegt in der gemeinsamen Fläche, deshalb hier statt im PR.

**Vokabular-Naht (@polatselman/@mvpo3):** `unterstuetzte_raum_typen` in
`lb_extraktion.yaml` ist eine Kopie der Labels aus `raumerkennung/raumtyp.py` und
war gedriftet. Neu `tests/contract/test_lb_raumtyp_naht.py` als Drift-Gate in
beide Richtungen — wer dort ein Label ergänzt, sieht sofort, dass die LB-Seite
nachzuziehen ist.


### ⚠ Befund an @mvpo3: falsche Quellenzuordnung im LBVorgabe-Contract-Test

`tests/contract/test_lb_vorgabe_contract.py::test_fischa_gk4_exklusion_stiegenhaus`
baut eine `LBVorgabe` mit `betriebsdauer_min=480`, `umschaltzeit_max_s=0.5`,
`mindest_lux_fluchtweg=1.0`, `SonderLux("feuerloescher", 5.0)` und
`piktogramm_norm="EN ISO 7010"` — und schreibt als Quelle
`lb_quelle="20241209_E LV Fischa 46.pdf §2.10/2.11"`.

**Keiner dieser fünf Werte steht in Fischa.** Am Original nachgeprüft (2026-08-30):
`lux`/`lx` = **0 Treffer im gesamten Dokument**, ebenso „Umschaltzeit",
„Betriebsdauer", „Feuerlöscher" und „7010". Fischa nennt statt EN ISO 7010 die
**ÖNORM Z 1000**. Die fünf Werte stammen aus `mo-leistungsbeschreibung_Elektro`
(§5.1.23, S. 37–38). Derselbe Fehler stand in
`knowledge/extracted/LB_ANALYSE_beispiele.md` und ist dort korrigiert.

Was Fischa §2.10/§2.11 **wirklich** trägt: Exklusion STIEGENHAUS + GANG (GK4),
Inklusion GARAGE, Gruppenbatterie im Technikraum, Einzelleuchtenüberwachung,
automatische + WEB-Prüfung, Fabrikat DIN-Sicherheitstechnik Concept-LED,
Normbezug ÖVE 8101 / R 12-2 / EN 1838 / ÖNORM Z 1000.

Der Test liegt in deiner Lane — Enis hat ihn **nicht** angefasst. Vorschlag:
entweder die Skalare entfernen oder `lb_quelle` auf die mo-Elektro-LB umstellen.
Merksatz: **Fischa = Bereichslogik, mo-Elektro = Skalare.**

### ✔ ERLEDIGT (2026-08-31) — Befund an @mvpo3 vom 30.08.: PR #40 erzeugte am echten Fischa-PDF einen falschen LB-Wert

> **Beidseitig behoben.** Auf main durch `facabe0` (Kontext-Gating
> `betriebsdauer|auszulegen`, `notruf`-Ausschluss, Cap 1440), auf `enis/lb-parser`
> durch das Anker-Gating plus `plausibel_max`. Beide liefern für Fischa jetzt
> `betriebsdauer_min = None`. Der Regressionstest steht als
> `test_stoerungsfrist_erzeugt_keine_betriebsdauer`. Der Rest des Eintrags bleibt
> als Beleg stehen.

**PR #40 („normwissen — ② LB-Parser") wurde in die CODEOWNERS-Lane `@EnisAMG`
gemerged** und über `registry.build_default_bundle()` aktiv verdrahtet. Enis hatte
zu diesem Zeitpunkt eine fail-closed Implementierung derselben Naht fertig
(`enis/lb-parser`) — die Kollision wurde erst beim Rebase sichtbar.

**Belegter False Positive.** Der gemergte Parser, gegen das echte
`20241209_E LV Fischa 46.pdf` laufen gelassen:

```
betriebsdauer_min  = 1440            ← Fischa spezifiziert KEINE Betriebsdauer
system_typ         = zentralbatterie ← waehlt still eine Seite eines Widerspruchs
bereiche_inklusion = [GARAGE]        ← stiller No-op im Platzierer
```

Ursache: `_betriebsdauer_min()` sucht `(\d+)\s*(?:Std|Stunden|h)` im **gesamten**
Dokumenttext und trifft „Störungsbehebung binnen 24 h" (S. 12) → 24 × 60 = 1440.

**Warum das sicherheitsrelevant ist:** `LBVorgabe.betriebsdauer_min` ist eine
*explizite Auftraggeber-Vorgabe* und übersteuert nach der CLAUDE.md-Hierarchie
`LB-explizit → Norm` den EN-1838-Default von 60 min. Hier übersteuert ein Wert,
den das Dokument nie enthalten hat.

**Falsche Quellenzuordnung (am Original geprüft).** Fischa enthält **keine**
480 min, **keine** 0,5 s, **kein** 1 lx, **keine** 5 lx Feuerlöscher und **kein**
EN ISO 7010 — `lux`/`lx`, „Umschaltzeit", „Betriebsdauer", „Feuerlöscher" und
„7010" haben je **0 Treffer**; genannt ist ÖNORM Z 1000. Diese fünf Werte stammen
aus `mo-leistungsbeschreibung_Elektro_240718.pdf` §5.1.23 (S. 37–38).

Fischa liefert tatsächlich: Exklusion STIEGENHAUS + GANG (GK4, §2.10 S. 37),
Inklusion GARAGE (§2.11), Überwachung Einzelleuchte, Prüfung WEB, Fabrikat
DIN-Sicherheitstechnik Concept-LED (§2.21 S. 42), Normbezüge — und
**widersprüchliche Systemtyp-Angaben** (Gruppenbatterie S. 19 vs. Zentralbatterie
S. 42), die nicht still aufgelöst werden dürfen.

**Betroffen:** `tests/fixtures/lb/fischa_lb.txt` (synthetischer Text mit dem Titel
„Projekt Fischa 46", der die mo-Elektro-Skalare trägt) und die darauf gestützten
Tests `test_fischa_skalare_felder`, `test_fischa_sonderlux_und_normbezug`,
`test_fischa_inklusion_garage`. **Enis hat die Fixture NICHT verändert** — sie liegt
in der 3-Owner-CODEOWNERS-Lane. Auch `test_leere_lb_bleibt_norm_default` ist
semantisch heikel: eine leere `LBVorgabe` ist von „die LB macht keine Vorgaben"
nicht unterscheidbar und lässt die Engine still norm-getrieben weiterlaufen.

**Vorschlag (Owner-GO steht aus):** Enis' fail-closed Implementierung ersetzt die
Extraktionslogik, die öffentliche API (`LbTextProvider`, `parse_lb`) und die
Registry-Verdrahtung von #40 bleiben **unverändert** erhalten. Fixture und
Test-Korrektur macht Leonis in seiner Lane.

### ✔ ERLEDIGT (2026-08-31) — Befund an @polatselman: `GARAGE` war kein erzeugbarer `raum_typ`

> **Geschlossen durch PR #49** (GARAGE/TECHNIK/LAGER/MUELLRAUM) **und PR #57**
> (KELLER). Die LB-Stützliste ist nachgezogen, das Label heißt `TECHNIK` (nicht
> `TECHNIKRAUM`), `LAGER` und `MUELLRAUM` sind getrennt. Beide realen Elektro-LBs
> parsen dadurch durch. Der Rest des Eintrags bleibt als Beleg stehen.

`raumerkennung/raumtyp.py` (`_TYP_MAP`) erzeugt 13 Werte — `GARAGE` ist keiner
davon, `classify_room` liefert für einen Stempel „Garage" `UNKNOWN` und der Raum
behält `raum_typ == ""`. Eine `BereichsRegel(raum_typ="GARAGE")` ist damit im
Platzierer ein **stiller No-op**: `lb_override` findet kein Polygon, gibt die
Liste unverändert zurück, kein Fehler, kein Log.

Das trifft den kanonischen LB-Fall direkt — **beide** realen Elektro-LBs fordern
Sicherheitsbeleuchtung in der Garage. Der LB-Parser behandelt das deshalb als
**blockierenden Review** statt es still zu verschlucken. Dasselbe gilt für
`TECHNIKRAUM` und `LAGER` (mo-Elektro §5.1.23).

Zum Schließen wären `GERMAN_ROOM_TYPE_MAP` + `_TYP_MAP` (+ ein `RoomType`-Member)
zu erweitern — deine Lane, deshalb hier nur der Befund.

**Bitte an Leonis (blockiert den LB-Parser fachlich, nicht technisch):**
`BereichsRegel.raum_typ` muss exakt Selmans `RaumModell.raum_typ`-Vokabular
treffen (`STIEGENHAUS`/`GANG`/`GARAGE`, …). Wo ist die Liste kanonisch? Solange
das offen ist, parst Enis die LB-Bereiche auf genau diese drei Strings und
markiert alles andere als „nicht zuordenbar" statt zu raten.

**Naht verdrahtet:** `registry.build_default_bundle()` setzt `ProviderBundle.lb`
seit PR #40 — der Parser ist aktiv. Offen bleibt die OIB-Hälfte (`ProjektKontext`
→ `pipeline.run`, `ProviderBundle.oib`).

**Fail-closed-Verhalten, das die Verdrahtung kennen muss:** `parse_lb()` wirft
`LbNichtLesbar` bzw. `LbReviewRequired` statt eine leere `LBVorgabe`
zurückzugeben — eine leere wäre von „die LB macht keine Vorgaben" nicht
unterscheidbar und ließe die Engine still norm-getrieben weiterlaufen. Seit dem
angeglichenen Raumtyp-Vokabular parsen die beiden Elektro-LBs durch; GU-Rahmen und
Bau-/Ausstattungsbeschreibung brechen weiterhin ab — zu Recht, sie enthalten keine
Notbeleuchtungs-Vorgaben.
`LbTextProvider.parse_bericht()` liefert dazu den vollen Audit-Trail. Wer verdrahtet,
sollte diese Exceptions in eine sichtbare Rückmeldung übersetzen (API: 422 mit
Bericht), nicht verschlucken.

**Norm-Ausgabe-Bezeichnung:** `ÖNORM EN 1838:2013` bleibt vorerst stehen, obwohl
im Repo 2019-11-15 liegt (inhaltlich deckungsgleich). Grund: der String ist
Naht-Invariante und steckt auch in `tests/fakes.py` und
`tests/platzierung/test_flaechen_strategy.py:53`. Umstellung nur gemeinsam
(Fixture-Regen aus dem echten Provider) — Details `docs/NORMQUELLEN_AT.md` 2a.

## Log (append-only, neueste oben)

- 2026-09-18 **Selman: S4a gebaut (Branch `selman/fix-s4a-tuerbloecke` auf dem Gate-Branch, NICHT gemerged).** ArchiCAD-Weltkoordinaten-Türblöcke (INSERT außerhalb, Geometrie innerhalb) werden an ihrer Geometrie verortet: OG1 9 Blocktüren (7 Zargentür, 2 Schiebetür), M17-04-c → BESTANDEN, Durchgänge ohne Türblatt 26 → 19, Einraum-Wohnungen 2 → 0; Gate-Verstoß nur noch (2) M17-02-b (S5b). Soll aus Enis' Referenzen 02/06–09/14–17: T02/T05/T07/T10 entsprechen, T01/T03/T04/T06/T09 teilweise, T08 (Balkontür) fehlt (kein Türblock). **Nebenwirkung Rennweg OG3:** Wohnungen 2 → 1, GANG wird WOHNUNG_PRIVAT, Segmente GRAPH 5 → 0 → `tests/naht/test_soll_rennweg.py` 2 Tests neu rot (nicht angepasst; Familien-Soll-Tests laufen in Worktrees nur mit kopiertem `Projekte/_eingang`). Nur `raumerkennung/tueren.py`, `kaskade.py`, `provider.py` + Tests; kein contracts/**-Touch. Weiter erst nach Freigabe S4b.

- 2026-09-18 **Selman: Übernahme Enis-Paket M17 (Rennweg OG1) + Merge-Gate des Türstapels als Test.** Branch `selman/uebernahme-enis-m17` (auf `f15d03f`). `docs/UEBERNAHME_ENIS_M17.md`: Paket geprüft (SHA-256, 56/56 Dateien), Diagnosebasis = heutiger Kopf, Befund M17-02-b eigenständig nachgerechnet → Slice S5b (U13). `docs/GATE_TUERSTAPEL.md` + `tests/gate/`: 18 Erwartungen aus Beispiel 17, M1–M4 wortgetreu aus der Diagnose, OG1-Kennzahlen; Nullmessung auf `f15d03f`: **16 BESTANDEN, 1 NICHT_BESTANDEN (M17-02-b), 1 NICHT_MESSBAR (M17-04-c → Frage an @EnisAMG in OFFENE_FRAGEN.md)**; M17-02-c auf Quellpräzision mit Toleranz 1 mm / 1 mm² entschieden → BESTANDEN. Gate-Regel als strict-xfail (`pytest -m gate`, default deselektiert). Nur `tests/`, `docs/`, `pyproject.toml` (Marker), `.gitignore`; **Erkennung unverändert, kein Slice gebaut, kein contracts/**-Touch.** Suite 1448 grün. S4a erst nach Freigabe.

- 2026-09-16 **Selman: SYNC + Integrationsbranch `selman/rennweg-stand` (`59583ff`) + Rennweg_v2.** origin/main brachte nichts Neues (`2f610cc`). Die fünf Fix-Slices der ersten Tranche aus `docs/DIAGNOSE_RENNWEG_RAUMERKENNUNG.md` (S1 Loch-Topologie, S2 Innen-Zonen, S5a Durchgang-Guard, S3a Schacht-Evidenz, S9 Treppen-Hülle) plus `selman/darstellung-versioniert` konfliktfrei gemergt; **nur `raumerkennung/**`, `scripts/analyse/` und Tests, kein `contracts/**`-Touch.** pytest **1392 passed**, 74 skipped, 2 xfailed. Ausgabe `Projekte/_ergebnis_raumerkennung/Rennweg_v2/` (v1 `Rennweg/` unverändert, Stände in `VERSIONEN.md`). Außenbereich-Überlagerung mit Kanon-Räumen 0 in 6 von 7 Geschossen (v1 aus dem lokalen Cache mit derselben Kennzahl nachgerechnet: 20 über alle Geschosse); **DG2 = 3** (Dachstreifen über ZIMMER/BAD, offen bis F5; SCHACHT-Splitter 0,08 m²). **Nicht nach main:** Merge-Gates S1/S2/S9 (Barawitzka/Mollgasse/Muthgasse) und S3a-Nachmessung nach Türstapel offen.

- 2026-09-13 **Leonis: SYNC — origin/main (21 Commits) in `leonis/demo-l-gebaeude` gemergt (`0329ae4`, KONFLIKTFREI).** Rein: **Selmans `raumerkennung/breitenprofil.py`** (die aktuelle Raumerkennung — `Abschnitt.breite_max_mm` keyword-only, breiteste erfasste Stelle sichtbar) · Enis' Türbreite-Naht (`normwissen` tuerbreite_herkunft auf Contract 1.4.0, „keine Messung = None nicht 0.0", vierte Messherkunft, OIB RL2/RL4-Fluchtwegbreiten) · AStV-Prüfbericht-Paket v2 (`hauptengine/validierung.py` + `api/main.py` Header-Budget 503 + `pipeline.py`) via **PR #150 gemergt** · meine früheren Stände via PR #156. **Kein `contracts/**`-Touch → kein 3-Owner-Sonderfall.** Konfliktfrei, weil mein Branch via #156 schon konsistent mit den platzierung-Änderungen war. **1347 grün** (36 Tests mehr: test_breitenprofil, test_header_budget, test_astv_pruefbericht, test_quellenblock_e07_rl4). Arbeite ab jetzt auf der aktuellen Raumerkennung.

- 2026-09-13 **Leonis → @polatselman: MESSUNG — der NetworkX-Anker-Pfad ist auf realen Plänen strukturell tot (0/7 im Graph-Pfad).** Gemessen über 4 Fixtures + 3 reale Elektropläne, je `build_circulation_graph(raum)` + `kreuzungs_anker`: **kein einziger** landet im Graph-Pfad, alle im Segment/Gang-Fallback. Grund: `platzierung/graph.py:30-36` baut den Graphen aus `zirkulation.nodes` + `zirkulation.edges` — die sind auf allen gemessenen Plänen **leer**, nur `zirkulation.segmente` ist gefüllt (wohnbau-Fixtures 4–5 Segmente, Elektroplan EG/1OG **je nur 2**). Also 0 Graph-Knoten trotz vorhandener Segmente → `kreuzungs_anker` immer leer → nie ein RZ an einer Kreuzung (EN 1838 §4.1.2 f bleibt ungenutzt), und die Dijkstra-Distanz (`distanz_zu_ausgang`) läuft nie. Der einzige Plan mit IST-Graph (4og-Fixture, 4 Kn/2 Ka) hat deg<3 → auch Fallback. **Das ist die strukturelle Wurzel der S4-RZ-Dichte** (EG „im Segment-Pfad" ist kein Zufall dünner Erkennung, der Graph-Pfad KANN nicht greifen). `raumerkennung/zirkulation.py:85` baut intern eine `g.nodes`-Struktur — die kommt aber nicht als `ZirkulationsGraph.nodes/edges` am Contract an (jedenfalls nicht am 02-TWA-Dialekt). **Zwei Hebel:** (a) deine Lane — Zirkulation füllt `nodes`/`edges` (echte Kreuzungen mit degree≥3), nicht nur `segmente`; das ist direkter Input für `lf-3`/Zirkulation. (b) meine Lane — `graph.py` baut den Graphen aus den `segmente`-Polylinien (Knoten an Endpunkten, Kreuzung wo Polylinien sich treffen), dann greift der Anker-Pfad auch ohne `nodes`/`edges`. **Nicht gemessen:** Mollgasse/Barawitzka (DXF liegen in Zips, nicht lokal) — dort soll der Graph laut Doku glänzen; falls deine Erkennung dort `nodes`/`edges` füllt, wäre der Kontrast der Beleg, dass es dialekt-abhängig ist. Visuelle Übersicht als PDF an den Owner geliefert (`Projektbeispiele-demo-Platzierungslogik/networkx_uebersicht.pdf`, nicht im Repo). Kein Code geändert — reine Messung/Board-Notiz.

- 2026-09-13 **Leonis → @polatselman: Slice-3b-Entscheid vollständig, `lf-3` FREIGEGEBEN — `docs/proposals/SLICE3B_LABEL_ENTSCHEIDUNG.md` §7.** Deine Vorlage §6 (fünf Punkte) beantwortet, damit du bauen kannst; deckt sich mit meiner Notiz vom 2026-09-12, nur vollständig gemappt. Kurz: **(1) Option A**, nicht B — A behebt die Zirkularität, B ist eine eigene Score-Neutralitäts-Korrektur (löst das Label messbar nicht, netto −6) und darf nicht als Label-Fix geframed werden; ob du B als reine Score-Korrektur in denselben `lf-3`-Neuschrieb ziehst, ist dein Score-Call, solange der Vektor sauber als `lf-3` versioniert wird und die −6 transparent bleiben. **(2) Training nur `mensch_bestaetigt`** (Namensregel ist wegen der 119 `wand`↔`rest` kein Schiedsrichter; Regel-Ausgabe = Zirkularität); 209 Widersprüche als `strittig` behalten, nicht wegwerfen. **(3) Korpus selbstgenügsam:** `flaeche_median_m2` + `n_punkte` in den Vektor (Befund 1a; jetzt beim Neuschrieb billig). **(4) MAJOR 2 = zweites Flag** („eigene Segmente vorhanden"), nicht Renormierung — die 6 `UEBERNEHMEN`-Freigaben aus den 87 segmentlosen fallen dann raus. **(5) Raumzahl = eine Basis, polygontragend (≥3 Punkte = 97):** nur die kriegen Notlicht; Band darauf festnageln, „Ist 114" korrigieren, 114/113/109 als Zwischenstände markieren (die konkrete Testzahl bleibt deine Lane). Guard-Invariante: `label_quelle==mensch ⇒ label gesetzt` · `rolle≠label ⇒ nie regel-bestätigt`. Balkontüren-vor-`lf-3` ist umgesetzt → von meiner Seite freigegeben. Ping bei fertigem Korpus/`lf-3`, ich prüfe die Konsumenten-Naht (RaumModell→Platzierung).

- 2026-09-12 **Leonis → @polatselman: Korpus-Label-Frage — ZWEITES FLAG, keine Renormierung; + #155-Approval nachgeholt.** (1) **Label-Defekt (209/501 rolle↔label_quelle-Widersprüche): eindeutig zweites Feld.** Renormierung der Gewichte kaschiert den Defekt nur (die 209 Zeilen bleiben falsch beschriftet, bloß leiser) und ist exakt die Zirkularität aus meinem Einwand 1 — Trainingswahrheit und Detektor-Output müssen getrennte Spalten sein: `rolle` (was Regel/Detektor sagte) + `label` (Ground truth, eigenes Feld) + `label_quelle ∈ {mensch_bestaetigt, regel, uebernommen}`; **trainiert wird nur auf `mensch_bestaetigt`** (Regel-Zeilen höchstens als klar gekennzeichnetes Pretraining). Die 209 Widerspruchszeilen NICHT wegwerfen, sondern als `strittig` flaggen — das sind die wertvollsten Zeilen des Korpus (genau dort weiß die Regel etwas nicht; Owner-Nachlabeln in kleinen Batches). Dazu ein Guard-Test als Invariante: `label_quelle==mensch ⇒ label gesetzt` und `rolle≠label ⇒ zählt nie als regel-bestätigt`. Schema-Migration jetzt ist billig, bei 20 Büros teuer — dein Korpus-ab-Tag-1-Prinzip spricht selbst dafür. (2) **Balkontüren-als-Ausgänge vor 3b: volle Zustimmung** — das ist die Wurzel der dünnen Elektroplan-Fluchtwege und deckt meine Naht-Notiz (a) vom 2026-09-12 (Müllraum-Südtür von==nach) gleich mit. Bitte Ping, wenn dein Fix steht: ich re-teste dann Elektroplan v9+ und die E2E-Bänder (mein `bausteine.ist_echte_tuer`-Phantom-Filter und R2/aussen_tuer_rz hängen an derselben Tür-Semantik). (3) **#155:** Approval nachgeholt (Stand 3d91a2c = Merge-Stand; 1.5.0 rein additiv, meine Suite 1304 grün darauf). Die Überlappungs-Bereinigung 62→0 ist für meine Sichtkette direkt relevant (Gang-RZ lagen außerhalb zackiger Polygone → alle „geschützt") — ich re-teste raum_13 am Elektroplan mit deinem neuen Stand. (4) 1.6.0-Proposal in docs/proposals/ lese ich als Nächstes; meine Konsumenten-Punkte vom Board (None-Default, Kontext-Schwelle, berichtsfähiger Provider-Stop) gelten unverändert.

- 2026-09-12 **Leonis → @polatselman: Antwort auf deine 3b-Antworten — Konsens, plus meine Konsumenten-Position zu Contract 1.6.0.** Deine Zusagen (Reihenfolge 1→3→4, Schritt 2 nur Smoke, Modell erst ~20 Büros und nur wenn es die Regel deutlich schlägt · geometrische mm-Kalibrierung statt $INSUNITS + verhältnisbasierte Features + Golden-Feature-Vektoren · stiege raus zum Objekt-Matcher in `geometrie_typ.py`, Klassifikator nur `wand/oeffnung/raumkontur/rest` · zweistufig gegen die Zirkularität) decken alle meine Einwände — **einverstanden, kein Dissens offen.** Auch der Schwellen-Vorbehalt (0,85/0,60 bei 5 Plänen nicht kalibrierbar → Politik, Re-Check am Korpus) ist richtig; Bitte dazu: die Schwellen als **Parameter/Daten, nicht hart** einbauen, damit die Re-Kalibrierung kein Code-Umbau wird. **Zu Contract 1.6.0 (rolle+confidence je Layer, `dialekt_confidence`+`dialekt_quelle` am RaumModell, optionales `erkennungs_confidence` je Raum/Tür) — vier Punkte aus Konsumenten-Sicht:** (1) **Getrennt von 1.5.0: ja**, nicht in Enis' hängenden Schnitt reinschieben. (2) **Default-Semantik = None, nicht 1.0:** ein Alt-Plan/Legacy-Pfad ohne Detektor darf keine volle Konfidenz BEHAUPTEN — `None` heißt „nicht erhoben" und läuft bei mir wie heute (kein neues Gate), ein Wert heißt „Detektor lief". Das hält alle Fakes/Fixtures unberührt (additiv mit Default) und die Aussage ehrlich. (3) **Meine Schwelle kommt in den `PlatzierungsKontext`** (dafür ist er da, #123 — ein Feld, keine Parameter-Fädelei), und „ablehnen statt platzieren" baue ich als **definierten Status mit Grund** (Muster oib_gate/fail-closed: engine_status + Befund im Prüfbericht), nicht als Exception aus der Tiefe. Konsum-Slice sage ich zu, sobald 1.6.0 gemergt ist. (4) **Provider-Stop <0,60: bitte als definierter, berichtsfähiger Fehler** (Muster `LbReviewRequired`: Typ + Bericht, API kann 422/503 mit Grund antworten), nicht stilles None/leeres Modell — sonst sieht der Nutzer „0 Symbole" statt „Erkennung unsicher, Plan prüfen". **Einwand 5/GANG-Slice vor 3b: volle Zustimmung** — deckt sich mit meiner Nähte-Notiz von heute weiter unten (Elektroplan DE: Müllraum-Südtür, raum_13-Zacken, Treppenläufe, Vorraum-Flags); der Elektroplan blockiert HEUTE, 3b ist Zukunft. Wenn dein 1.6.0-Antrag steht: PR mit @EnisAMG + mir als CODEOWNERS-Review, ich antworte binnen eines Tages.

- 2026-09-12 **Leonis → @polatselman: vier Erkennungs-Nähte aus dem Elektroplan-DE-Nachzeichnen (Owner-Korrektur-Runden 1+2, Branch `leonis/demo-l-gebaeude`, gepusht).** Kontext: Owner hat zwei Korrektur-Runden in AutoCAD gefahren, daraus R1–R8 in `platzierung/` — vier Befunde liegen aber VOR meiner Lane in der Erkennung. **(a) Müllraum-Südtür kommt als Innen-Tür:** die 140er-Südtür des Müllraums (Elektroplan DE EG) landet als `tuer_21` mit `von_raum == nach_raum == raum_12` — damit ist sie nicht als AUSSEN-Tür erkennbar, und meine R2-Regel (communal-Raum+AUSSEN-Tür = Notausgang-RZ §4.1.2g) setzt das RZ an der erkannten Ost-Öffnung statt an der Owner-korrigierten Südtür. Erwartung: Außenwand-Öffnung → `nach_raum = AUSSEN` (euer `aussenoeffnung_*`-Pfad greift hier nicht). **(b) raum_13-Polygon-Zacken drücken den EG-Lux-Nachweis:** Emin 0,03 lx, gleiche Klasse wie Mollgasse raum_41/55 (Spikey-Polygone, bei dir schon als Naht angenommen 2026-09-09) — Elektroplan DE EG ist ein weiterer Repro-Fall. **(c) Treppenläufe kommen am 02-TWA-Dialekt nicht an:** der Contract kennt `StiegenhausModell.laeufe` + `Treppenlauf.richtung` seit v1.2.0 und `raumerkennung/stiegenhaus.py` baut Läufe — am Elektroplan DE kam für das STGH aber nichts Nutzbares an, deshalb approximiert meine R8-Regel (RZ am stair_exit INS Stiegenhaus, Pfeil = Abstieg) die Richtung übers Stiegen-Zentrum. Sobald Läufe+`richtung` am Plan gefüllt sind, stelle ich R8 auf die echte Gehrichtung um. **(d) Private Wohnungs-Vorräume kommen mit `communal=True`/`fluchtweg=True`:** raum_1/raum_2/raum_5 (kleine Stiegen-Vorräume, alle weiteren Türen privat) — meine R7 (`entferne_wohnungs_vorraum_rz`) und der VORRAUM-Eintrag in `_TUERLEUCHTE_KEIN_COMMUNAL` sind Engine-WORKAROUNDS, keine Lösung; fällt die Flag-Ableitung richtig, nehme ich beide zurück. **Kein Contract-Touch, nichts in deiner Lane geändert** — Repro: `scripts/demo/nachzeichnen_elektroplan.py` + Diff-Skripte `scratchpad/elektroplan_nachzeichnen/`, Details `Handoff/LEONIS.md` (STAND 2026-09-11).

- 2026-09-10 **Leonis → @polatselman: Review-Notiz zum trainierten Dialekt-Detektor (Slice 3b, deine Lane `raumerkennung`/Schicht 2).** Owner + ich haben die Idee besprochen, 3b als **trainierten Layer-Rollen-Klassifikator** zu bauen (Lernproblem *„Layer → Rolle"*, Input = **Merkmalsvektor je Layer, kein Layername**, Output `rolle + confidence`; Modell = Gradient Boosting, ~50 KB, versioniert, inspizierbar; Profil = Aggregation der Layer-Rollen). Vorgeschlagene Reihenfolge: (1) `layer_features.py` deterministischer Extractor → (2) Baseline `DecisionTree` **GroupKFold nach Plan (leave-one-plan-out)** → (3) 3b **regelbasiert auf denselben Features** → (4) **Korpus-Writer ab Tag 1** (jedes bestätigte Profil schreibt `(plan_id, layer[Metadaten], feature_vektor, rolle)`) → (5) GBM-Kopf bei ~20 Büros, gegen die Regel gemessen. Betrieb: `≥0.85` übernehmen · `0.60–0.85` Mensch · `<0.60` **HARD STOP**. **Meine Einwände als RaumModell-Konsument (bitte gegenprüfen):** (a) **Nachbarschafts-Features sind zirkulär** („berührt es die Wände?" braucht die Wand-Klassifikation) → zweistufig (Self-Features → Wände raten → Nachbarschaft relativ dazu). (b) **`stiege`/Objekte gehören NICHT in den Layer-Klassifikator** (~1 Beispiel/Plan → LOPO-Recall = Rauschen) — das ist ein **Objekt-/Muster-Matcher** (7 Linien, 0,30-Teilung, 2×45° = Shape auf Entity-Gruppe), eigener Detektor; grobe Klassen zuerst (`wand/oeffnung/raumkontur/rest`). (c) **Skalen-Detekt MUSS vor der Merkmalsextraktion** — der neue `EG/1OG_Elektroplan_DE_NEU.dxf` hat `INSUNITS=4` aber ist in **Metern**; Absolut-Features (Parallelabstand 0,20 m, Teilung 0,30 m) sind sonst Faktor-1000-Müll. Plus: Extractor braucht **eigene Golden-Tests** (1 Toleranz-Detail verschiebt lautlos jeden Vektor). (d) **Abbruchkriterium bei 5 Plänen ist selbst überkonfident** (Test-Split = 1 Plan) → Smoke-Test, kein „Weg tot". (e) **Scope-Ehrlichkeit:** 3b löst den Fremd-Dialekt-**Namens**fall; NICHT den heutigen Elektroplan-Fehler (02-TWA-Wände korrekt erkannt, aber **kein GANG typisiert + keine Gebäude-Ausgänge abgeleitet** → Fluchtweg dünn = **Topologie/Semantik downstream**, separater Slice). **Zwei Contract-Punkte (3-Owner, betreffen mich direkt):** die Naht muss **`rolle + confidence`** tragen (nicht nur `rolle`), damit die Confidence bis zur Platzierung durchkommt und **ich bei niedriger Confidence auch fail-closed** gehen kann, statt auf geratener Geometrie zu platzieren. Voller Text im Chat/Prompt an dich. **Kein Push, kein Contract-Touch — nur Board-Notiz.**
- 2026-09-12 **Selman → @EnisAMG + @mvpo3: Stempelschutz-Nachentscheid umgesetzt (Riegel-Band steigt — Owner-Entscheid, keine Regression), Muthgasse-Türband abgelöst und die letzten Fremdangaben gemessen, Slice-3b-Vorarbeit gebaut mit drei offenen Befunden.** Branch `selman/extents-ausreisser`, **PR #155**, **kein Merge**. **⚠️ CONTRACT: `raum_modell` bleibt 1.5.0** — in dieser Runde wurde dort **nur Kommentartext** geändert (die Regelnummern am Literal `BereinigungsRegel` trugen die alte Kaskade), `gen_schema --check` in sync. Der Check `contract-freeze` bleibt trotzdem pending, bis ihr beide auf dem dann aktuellen `head_sha` approvt — **jeder nachgeschobene Commit entwertet ein erteiltes Approval**, darum bitte erst nach dem letzten Commit. **(1) Der Stempelschutz war zu breit.** Er griff bei 6 von 7 Paaren, auch dort, wo das Ausstanzen die Stempeltreue **verbessert** hätte (`raum_43` +544 %, `raum_51` +1148 %, `raum_88` −48 %). Er greift jetzt nur, wenn der Verlierer den strikt besseren Quellen-Rang hat: geschützte Paare **6 → 1**, Überlapper **11 → 2**, doppelt belegt **58,260 → 2,552 m²**. Geschützt bleibt genau `raum_29` (quelle L, Stempel 16,52 m², Rang 1) gegen `raum_91` (quelle F, Rang 3) — der Fall, den der Auftrag als Testfall nannte. **(2) Das Riegel-Band für Muthgasse STEIGT** von `(0, 0.0)` auf `(2, 2.6)`. Das ist die einzige erlaubte Anhebung und **kein Nachlassen**: ohne den Schutz wäre der Ist 0 / 0,20 mm², die Kennzahl ist gegen Stempeltreue eingetauscht. Die Begründung steht im Code über `BAND`, die löschungsfeste Untergrenze `BAND_RAEUME` 47/62/101/21/14 ist unverändert und nachgemessen. **(3) @EnisAMG — dein SCHLEUSE-Eintrag:** die Ist-Zahlen in `regel_deckung.yaml` waren ein Zwischenstand aus einem reinen Provider-Parse; Ist nach dem Lauf ist `stair_exit` **6** und Türen typisiert **189 von 270**. Nur der `frage`-Text ist ergänzt, `owner: Enis` unverändert, dein Guard unberührt. Die Sachfrage (raumtyp_regeln-Eintrag oder default(rz)) bleibt offen bei dir, ebenso `E2-VF-11b`/`raum_67`. **(4) Türband abgelöst statt abgesenkt:** die 280 stammte selbst aus einem alten Ist. Jetzt 72 echte A-DOOR-Blocktüren mit Türblatt-ARC, Paarung per Kuhn, **matchingunabhängig belegt** (Hopcroft-Karp, scipy-Min-Cost und permutiertes Kuhn liefern identisch 13/13/44/59/63 von 72). Ein Provider-Parse hat die letzten Fremdangaben geschlossen — bestätigt: 399,81 mm für `tuer_48`, Median 405,96, max 780,57, die 14 von 72, die Zuordnung `tuer_18`–`21` auf 0,00 mm. **Gefallen: „103 der 121“ → 108** (die 103 ist 121 − 18 und verwechselte die 18 echten Blocktüren mit den 72 Plan-Türblöcken) und **„8–110 LINEs“ → 2–110**. **(5) @mvpo3 — Slice-3b-Vorarbeit steht, aufruferlos** (Schritte 1/3/4, Schritt 2 nur Smoke). Zwei Messbefunde sind wichtiger als der Code: **Layernamen taugen nicht als Label** (Regel gegen Namensregel: Barawitzka 57 Treffer / 72 Abweichungen, Mollgasse 33/51, EG_Grundriss 23/28), und **der Baum schlägt „immer rest“ nicht** — LOPO acc **0,792** gegen Mehrheits-Basislinie **0,815**, macro-F1 0,333, `raumkontur` F1 0,000. Das stützt deine Reihenfolge „Modell erst bei etwa 20 Büros“ mit einer Messung statt einer Vermutung. **Drei Befunde sind offen** und stehen als `lf-3`-Runde in `docs/OFFENE_FRAGEN.md` — der wichtigste: der Korpus trägt **kein Label**, gespeichert ist `rolle` (die Ausgabe der Regel), und bei 209 von 501 Zeilen widerspricht das dem behaupteten `label_quelle`; ein Training darauf wäre genau dein Zirkularitäts-Einwand (a). **Dein Einwand 5** (Gebäudeausgänge am 02-TWA/L04-Dialekt) hat weiter **Vorrang vor `lf-3`** und ist **nicht gebaut**. **Belege:** volle Suite `1296 passed, 11 skipped, 2 deselected, 12 xfailed in 1229.06s`, exit 0, keine XPASS-Zeile; Prüfstrecke über alle fünf Pläne ohne Parallellast, exit 0, 271,6 / 306,2 / 1888,0 / 57,0 / 51,4 s = 2574,2 s; alle fünf Akzeptanzkriterien des Messskripts OK. Zahlen im Detail: `docs/ENIS_UEBERGABE_0908.md` § 23, `Projekte/_ergebnis/VERLAUF.md`.

- 2026-09-12 **Selman → @EnisAMG: Korrektur an zwei eigenen Belegsätzen zu `Schl.` (deine Entscheidung bleibt tragfähig) + Zahlenkorrektur `stair_exit` Muthgasse_E2 = 6 — und „62“ ist keine `stair_exit`-Zahl.** Branch `selman/extents-ausreisser`, PR #155, **reine Doku: kein Code, kein Contract, kein Merge, in diesem Doku-Umfang keine Testdatei angefasst (Korrektur 2026-09-12: die Aussage gilt für MEINEN Umfang, nicht für den ganzen Commit — im selben Branch `selman/extents-ausreisser` ändern parallele Aufträge `tests/naht/test_soll_muthgasse.py` und `tests/raumerkennung/test_bereinigung.py`); nichts gelöscht, alle alten Aussagen stehen unverändert daneben.** **(1) Zwei meiner In-Plan-Belegsätze aus der `Schl.`-Frage vom 2026-09-10 sind gemessen widerlegt** (Wortlaut der Widerlegung: `docs/OFFENE_FRAGEN.md § „Zwei Korrekturen am Abschnitt darüber“ (:933-948)`, `docs/ENIS_UEBERGABE_0908.md` § 19.2). (a) Die „~8,2 m gemeinsame Kontaktlänge“ von `raum_65` zu `stiegenhaus_1…5` ist mit der dortigen Definition **nicht reproduzierbar**: gemessen **Σ 4,72 m** (Gegenrichtung Σ 5,21 m), Einzelkanten 662 / 1132 / 1131 / 1131 / 662 mm. (b) Die „0 mm zu fünf STIEGENHAUS-Polygonen“ sind **kein Wandkontakt zu Raumpolygonen**, sondern Überlappung mit den Bounding-Rechtecken der Treppen-Blockreferenzen (`geometrie_typ.py:47-63`, `:89`; die fünf sind geometrisch nur 2 Lagen, IoU 0,956–0,991). Gegen das STIEGENHAUS-Polygon `raum_88` aus `raeume.json` ist `raum_65` **4848 mm** entfernt, und **dieselbe 0-mm-Klasse enthält die Küchen `raum_29` / `raum_91` / `raum_86` — das Merkmal trennt Schleuse nicht von Küche.** **Deine Entscheidung `E2-VF-11a` = `SCHLEUSE` bleibt davon unberührt und tragfähig:** tragend ist das Vergleichsgeschoss, nicht der Einzelplan — `DBA-Abstr. Schleuse` steht in **E8 627 mm** von `raum_65` (9791 mm von `raum_67`), in **E9 10 mm**; an der Lage von `raum_67` trägt **E7 ein WC, E8 ein SR** (6,07 m², Parkett). Die In-Plan-Belege trennen die beiden Räume **nicht** — genau darum steckt der Beleg in der Entscheidung (`kuerzel_entscheid.py`) und nicht in einer Erkennungsregel. Unverändert aktiv und korrekt formuliert: `kuerzel_entscheid.py` schreibt den Belegtext „Stiegenkern-Lage (0 mm Kontakt/Überlappung)“ in jeden Prüfbericht — das ist die Treppen-Extent-Lesart, sie bleibt. **(2) Zahlenkorrektur: `stair_exit` Muthgasse_E2 ist 6.** Zeitreihe je Quelle: **12** (Läufe `0ce5c38` / `19a506b` / `3695ca4` / `8d2a3a7` / `ab0ad51`, 2026-09-07..09; `Projekte/_ergebnis/VERLAUF.md:40,47,54,61,68`) → **5** (nach `8b35e53`, Ausschluss der Beschriftungsfahnen; `VERLAUF.md:87,102,119`) → **7** (**nur Provider-Parse** nach `96dcef6` SCHLEUSE, **kein Prüfstreckenlauf**; `docs/ENIS_UEBERGABE_0908.md` §§ 19.4 + 19.5) → **6** (Ist, Lauf 2026-09-12 / `6ebf676`; `VERLAUF.md:142,155`, `ENIS_UEBERGABE` § 21.4, `Projekte/_ergebnis/Muthgasse_E2/bericht.md` § „Ausgänge (11)“ = 6 `stair_exit` + 5 `final_exit`). Unterschied 7 → 6: `exit_durchgang_141` ist mit den 21 weggefallenen Kontaktzonen-Durchgängen verschwunden (`durchgang_*` 169 → 148), `exit_tuer_50` blieb. **Die 7 steht an vier Stellen in `tests/naht/test_soll_muthgasse.py`** (`:38` und `:42` im Modul-Docstring, `:355` und `:440` in den xfail-`reason`-Texten) — **von mir nicht angefasst**, die Testdatei ist in diesem Lauf gesperrt. **Am Ist-Stand der Datei nachgeprüft: die Korrektur auf 6 steht dort bereits als datierter Nachtrag daneben** (Docstring `:45-52` sowie in denselben zwei xfail-`reason`-Texten `:356-362` und `:441-447`, je mit VERLAUF- und `bericht.md`-Beleg) — die 7 bleibt korrekt als Zwischenstand ihres Datums stehen, **für dich ist dort nichts nachzuziehen**. **In deiner `normwissen/data/regel_deckung.yaml:88` habe ich die stale 7 als Nachtrag korrigiert** (Ist-Stand `stair_exit` 6, Türen typisiert 189 von 270, Lauf 2026-09-12) — geändert ist **nur** der `frage`-Text, die Schlüssel `frage` und `owner` sowie `owner: Enis` sind unverändert, der Guard `tests/naht/test_regel_deckung.py` ist unberührt. **Ausdrücklich: 62 ist KEINE `stair_exit`-Zahl.** Ich habe im ganzen Repo, in allen Commit-Texten und in allen GitHub-Kommentaren **keine Fundstelle für „62 stair_exit“** gefunden (beide Suchrichtungen geprüft) — ich trage hier also keine Fundstelle nach, sondern stelle klar, dass es keine gibt. **62** ist die Überlapper-Zahl aus `ENIS_UEBERGABE_0908.md` § 14.2 (**62 von 245 Räumen**, 262,342 m² doppelt belegt), bereinigt **62 → 0** — so steht es im Titel von PR #155. Damit die Verwechslung nicht wiederkommt, die weiteren 62-Quellen: die **62 `uebersicht.json`** der Übersichtskarten, deren Architektur-Summe (45 Karten) **75** `stair_exit` ist und in der Muthgasse mit **5** steht. **Korrektur 2026-09-12 der Fundstellen-Zuordnung: `Handoff/SELMAN.md:204-206` trägt nur die Summenzeile über die 62 `uebersicht.json` (dort die **75** `stair_exit` der 45 Architektur-Karten), NICHT den Muthgasse-Einzelwert **5** — der steht in `Projekte/_uebersicht/INDEX.md:79` (Abschnitt „Muthgasse 109B“, Zeile `Muthgasse_E2`, Spalte `stair_exit`). Selbst nachgeprüft, damit hier niemand in der falschen Datei sucht.** Mollgasse „Räume gesamt 62“ (`VERLAUF.md:141`), und die Codestellen-Referenz **`ausgaenge.py:62-64` → `stair_exit`** (`ENIS_UEBERGABE` § 11.3). **(3) Welche Fundstellen ich mit datiertem Korrekturhinweis versehen habe** (Text jeweils unverändert stehen gelassen): `docs/OFFENE_FRAGEN.md:693`, `:768-770`, `:807` (Indizientabelle; Zeilennummern im Stand nach meinen Einsätzen) sowie die zwei Board-Einträge vom 2026-09-10 hier, `docs/COORDINATION.md`, Eintrag **2026-09-10 „Freeze-Absicherung nach dem #149-Vorfall“** und Eintrag **2026-09-10 „Abschluss-Runde 3 — `raum_65` ist eine Vokabular-Lücke“**. **Korrektur 2026-09-12: die hier zuerst genannten Zeilennummern `:367` und `:369` waren falsch — nach meinen Einsätzen stehen die zwei Einträge bei `:369` und `:372` (selbst nachgezählt). Darum steht hier ab jetzt Datum + Titel statt einer Zeilennummer, wo das ohne Informationsverlust geht: die Board-Einträge sind datiert, Zeilennummern verrutschen bei jedem Einsatz.** **Korrektur 2026-09-12 der Zitatspanne: der Abschnitt „Zwei Korrekturen am Abschnitt darüber“ umfasst `docs/OFFENE_FRAGEN.md:933-948` (Überschrift + beide Punkte), nicht `:935-943`; nachgeprüft und an allen Zitatstellen dieses Boards nachgezogen.** **Ebenfalls betroffen, nur genannt und NICHT geändert:** `docs/ENIS_UEBERGABE_0908.md` § 13.3 (dort steht weiterhin „direkt an fünf STIEGENHAUS-Polygonen“, ohne Nachtrag) (die Datei ist in diesem Lauf gesperrt) und der **unveränderliche Commit-Body `79fc0bb`** („0 mm zu fuenf STIEGENHAUS-Polygonen“) — ein Commit-Text lässt sich nicht nachtragen, die Korrektur dazu steht in `OFFENE_FRAGEN.md § „Zwei Korrekturen am Abschnitt darüber“ (:933-948)`. **Bereits korrigiert, nur als Verweis:** `OFFENE_FRAGEN.md § „Zwei Korrekturen am Abschnitt darüber“ (:933-948)`, `ENIS_UEBERGABE` § 19.2, `COORDINATION.md:316`, `Handoff/SELMAN.md:112`, PR-#155-Body. **(4) Zwei Doku-Punkte aus derselben Runde, beide ohne Code:** neuer Abschnitt `docs/OFFENE_FRAGEN.md` § „Slice — Gebäudeausgänge und Fluchtziel am 02-TWA/L04-Dialekt (Leonis Einwand 5)“ (**Owner-Vorgabe: Vorrang vor Slice 3b**, heute nur aufgenommen, nicht gebaut) und der Contract-Vorschlag `docs/proposals/CONTRACT_1_6_0_ROLLE_CONFIDENCE.md` (`rolle` + `confidence` durch die Naht für @mvpo3 — **Vorschlag, nichts umgesetzt**, und ausdrücklich **nicht** in 1.5.0 nachgeschoben, weil jeder nachgeschobene Commit ein erteiltes Approval entwertet, Check `contract-freeze`). **(5) `E2-VF-11b` / `raum_67` bleibt untypisiert, bis du entscheidest** — Owner-Entscheidung 2026-09-12, datiert in der Entscheidungsvorlage in `docs/OFFENE_FRAGEN.md`; der Prüfbericht weist es je Lauf aus („## Hinweise Kürzel-Auflösung“), und die Entscheidung für `E2-VF-11a` gilt ausdrücklich nur für diese eine Stempelnummer.

- 2026-09-12 **Selman → @EnisAMG + @mvpo3 + 3-Owner-Runde: Raumüberlappungen bereinigt (62 → 0), SCHLEUSE entschieden umgesetzt, Enis' CODEOWNERS-Patch übernommen und unabhängig nachgeprüft, seine zwei Auflagen dokumentiert, PR #154 geprüft.** Branch `selman/extents-ausreisser`, **neuer PR als Nachfolger von #152** (das war schon gemerged), **kein Merge**. Commits dieser Runde: `731a7d5` (WIP-Sicherung der Übersichtskarten vor dem Sync) · `804e6af` (Merge `origin/main`) · `64527f0` (BRANCH_PROTECTION § 7) · `7d41907` (Enis' Auflagen A/B) · `96dcef6` (SCHLEUSE + Kürzel-Auflösung) · `6ebf676` (**Bereinigung + Contract `raum_modell` 1.4.0 → 1.5.0**) + dieser Abschluss-Commit. **⚠️ CONTRACT-TOUCH: `raum_modell` 1.5.0 — additiv (`Raum.polygon_roh`, `Raum.bereinigung[]`), Schema regeneriert. Das braucht das Approval aller drei Owner auf dem aktuellen `head_sha`; jeder nachgeschobene Commit entwertet es (Check `contract-freeze`).** **(1) Bereinigung der Raumüberlappungen (§ 14.6/§ 14.6.1 umgesetzt, neues Modul `raumerkennung/bereinigung.py`).** **62 von 62 Überlappern gelöst, doppelt belegte Fläche 262,342 m² → 0,39 mm²** auf allen fünf Plänen (Barawitzka 9→0 · Mollgasse 16→0 · Muthgasse 37→0 · Rennweg 0/0), kein Restpaar über der Rauschschwelle von 1 mm². Regel für Regel gemessen (`scripts/analyse/ueberlappung_regeln.py`, kumulativ): Regel 1 LIFT/SCHACHT löst **1**, +Regel 2 Enthaltensein **16**, +Regel 3 Quellen-Rang/Stempelnähe **62**, Regel 4 nur noch Mikroflächen (0,38 mm²), **Regel 5 Restfläche greift in NULL Fällen** — passend zu § 14.6.1 „(d) null Fälle", also Vorsorge und nur synthetisch getestet. Buchungen: QUELLE_RANG 53 · STEMPEL_NAEHE 29 · ZERFALL 17 · ENTHALTENSEIN 7 · SCHWERPUNKT 5 · ENTFALL 5 · LIFT_SCHACHT 1 · SCHLITZ 1 · RESTFLAECHE 0. **Nicht destruktiv:** `polygon_roh` hält den Ring vor der Bereinigung, `bereinigung[]` bucht jeden Abzug mit Regel und Gegenspieler; die Bilanz geht **exakt** auf (Fläche(roh) − Fläche(bereinigt) == Σ Buchungen, gemessen 0,000000 mm² unbucht — eine erste Fassung leckte 0,512 mm² an Nebenkomponenten unter 1 mm², die Buchungsschwelle ist deshalb entfernt). **5 Räume entfallen** (Restkörper < 1 m², dasselbe Kriterium wie `kaskade.py:115`): Barawitzka `raum_44` (0,224 m²), Muthgasse `raum_82` (0,361) / `raum_86` (0,939) / `raum_92` (0,141) / `raum_93` (0,085), Σ 1,7494 m²; dazu 14,829 m² verworfene Nebenkomponenten. Roh-Polygon, Stempel und Regelkette stehen für sie in `raeume.json` unter der neuen Top-Level-Liste `entfallen` — **nichts wird gelöscht**. Abweichung > 5 % vom Stempel: Barawitzka 2→2, Mollgasse 23→23, Muthgasse 21→22, Rennweg 0→0; **neu über 5 % genau 2 Fälle** (Muthgasse `raum_85` +2,9 % → −96,7 %, `raum_29` 0,0 % → −15,6 %), Gegenrichtung `raum_87` +63,4 % → **+1,7 %**. **Riegel-Bänder nachgezogen** (je Plan (0, 0.0), Summe (0, 0.0)) **plus neue löschungsfeste Untergrenze `BAND_RAEUME` 47/62/101/21/14** über `raeume` + `entfallen` — ohne die wäre der Riegel blind gegen den einfachsten Weg zu „0 Überlapper": Räume löschen. **(2) @mvpo3 — was dich betrifft.** (a) **Der verschluckte LIFT ist weg:** `raum_88` (STIEGENHAUS, F) überdeckte `raum_79` (LIFT, L) zu 97,6 %, nach der Bereinigung kein Fall mehr (Regel 1, Buchung 3,944 m²). Leuchten können damit nicht mehr rechnerisch im Liftschacht landen. (b) **Aber `raeume.json` und `RaumModell` sind nur für die Kaskaden-Räume deckungsgleich:** `typisiere_geometrisch` und `finde_lifte` legen danach eigene Räume an (`stiegenhaus_*`, `lift_*`), die die Bereinigung nicht sieht — **Modell-Restüberlappung gemessen: Muthgasse 12 / 38,279 m², Barawitzka 4 / 7,131 m²**, Mollgasse und Rennweg 0. Der Bericht führt das jetzt dauerhaft getrennt mit. (c) **Fremde Lane, nur gemeldet:** Löcher sind als 1-mm-Schlitz kodiert (der Contract-Ring kann keine Innenringe tragen). `point_in_polygon`, Fläche, shapely `covers` und `grid_points` sehen das Loch korrekt als außen — **die Bbox-Mitte nicht** (`platzierung/geometry.py:226`, `find_center_diagonal`, und `find_center_visual` oberhalb Fläche/bbox ≥ 0,9). Derselbe Effekt besteht heute schon beim 50-mm-Schlitz der `lift_erkennung`; betroffen ist genau ein Raum (`Mollgasse raum_51`). Eigener Arbeitsschritt bei dir, von uns nicht angefasst. (d) **`lift_erkennung.py:171`**: der Skip prüft nur `raum_typ == "LIFT"`, nicht `SCHACHT` — `Barawitzka lift_2` überdeckt `rest_3` (SCHACHT, R) zu 100 %. Gemessen, nicht behoben. (e) Leuchten-Delta: nur Mollgasse bewegt sich (rz/sl 30/35 → 27/34); die **Flächenschwellen rechnen ab jetzt mit der bereinigten Fläche** (`platzierung/flaechen_strategy.py:173-174`, `hauptengine/validierung.py:433`). (f) **Branch Protection auf `main` fehlt weiterhin** — heute nachgemessen: `protection` 404, `rulesets` 403, Token `admin:false`; Anleitung und die zwei getrennten offenen Punkte in `docs/BRANCH_PROTECTION.md` § 7. **(3) @EnisAMG — was dich betrifft.** (a) **Dein CODEOWNERS-Patch ist übernommen und unabhängig nachgeprüft** (Kommentar in #152): Prüfsummen beider Paketdateien OK; `git apply --cached` des Pakets auf `6a96eb5` ergibt Tree `164e379a…` = exakt dein README-Wert; dein gepushtes `b00420a` weicht davon **nur im Docstring/Kommentar** ab (Reichweiten-Wortlaut), ausführbarer Code und Testdatei sind bitgleich. Eigene Läufe: `tests/ci` **14 passed**, `tests/ci tests/contract` **70 passed**, `ruff` clean, volle Suite **1225 passed, 0 XPASS**, und die Gegenprobe am unkorrigierten Skript von `6a96eb5` fällt bei **10 von 14** Fällen um — deckt sich mit deiner Angabe. Abweichung von deinem README § 7, dokumentiert: nicht per `git am`, weil der Branch nicht mehr auf `6a96eb5` stand und `b00420a` zu diesem Zeitpunkt schon auf dem Branch und mit #152 auf `main` war; übernommen per Merge von `origin/main` (`804e6af`). **Deine verbleibende Grenze steht getrennt in `docs/BRANCH_PROTECTION.md` § 7**: 7.1 vertrauenswürdige Ausführung des Prüfers (Workflow-Umstellung, bewusst nicht im Paket), 7.2 erzwungene Mergesperre (Repo-Einstellung bei @mvpo3, laut Plan möglicherweise nicht verfügbar) — mit deinem Satz wörtlich: bis dahin verlässliche Anzeige, keine technisch erzwungene Sperre. (b) **SCHLEUSE ist umgesetzt, aber nur für `E2-VF-11a`** (`96dcef6`): Kanon-Typ `SCHLEUSE` in `docs/VOKABULAR.md` § 1 mit deiner Begründung und Fundstelle, Nutzungsklasse `ALLGEMEIN_ERSCHLIESSUNG`, `normwissen/data/regel_deckung.yaml` mit `offen` + Owner Enis (Erschließungslage entschieden, **Notbeleuchtungsanforderung ausdrücklich offen**; dort steht auch, dass „offen" nicht „wirkungslos" heißt — die Nutzungsklasse wirkt heute schon auf Türtypisierung und Ausgänge). Das ausgeschriebene Wort steht im Wörterbuch; das **mehrdeutige Kürzel `Schl.` typisiert nur mit Zusatzbeleg UND eingetragener Owner-Entscheidung je Stempelnummer** (`raumerkennung/kuerzel_entscheid.py`). `SCHLEUSE` ist zusätzlich in deine `lb_extraktion.yaml`-Stützliste aufgenommen, weil der beidseitige Guard das im selben PR verlangt — im Kommentar begründet. **Wichtiger Messbefund, der zwei Sätze meiner früheren Frage korrigiert:** die In-Plan-Belege trennen `raum_65` **nicht** von `raum_67` — die „0 mm zu fünf STIEGENHAUS-Polygonen" stammen aus Treppen-Block-Extents und gelten genauso für die Küchen `raum_29/91/86`, die „~8,2 m Kontaktlänge" ist nicht reproduzierbar (gemessen 4,72 m bzw. 5,21 m), und DBA-Texte liegen bei beiden Räumen in der Nähe. Der **einzige** echte Unterscheider ist dein Vergleichsgeschoss: „DBA-Abstr. Schleuse" steht in E8 **627 mm** von `raum_65` und 9791 mm von `raum_67`, in E9 **10 mm** von `raum_65`; an der Lage von `raum_67` trägt E7 ein WC, E8 ein SR. Ein Einzelplan-Parse sieht das nicht — deshalb steckt der Beleg in der Entscheidung. (c) **Offen an dich: `E2-VF-11b` (`raum_67`, 3,73 m², liegt zu 99,3 % im F-Artefaktraum `raum_88`)** — Entscheidungsvorlage mit Pro/Contra in `docs/OFFENE_FRAGEN.md`; bis dahin bleibt der Raum untypisiert und der Prüfbericht sagt das ausdrücklich („## Hinweise Kürzel-Auflösung"). (d) **Deine Auflage A ist dokumentiert** (`7d41907`): `lichte_mm` hat weiter 0 Erzeuger (gemessen: 0 von 612 Türen), festgehalten sind die Auslöser-Regel, die Codestelle, die der erste Erzeuger anfassen muss (`tueren.py::_block_tueren` Z. 92-98; die 16 DL-Blöcke von Muthgasse kommen dort heute nicht an, weil `_ist_tuer_block`/`_DOOR_HINT` „TU DF" nicht kennt) und eine **fertige Test-Skizze**, die gegen den heutigen Code 2 passed / 2 strict-xfail läuft. Nebenbefund: § 6.2 der Übergabe ist widerlegt — **0 der 19 BLOCKNAME-Türen von Muthgasse trägt einen DL-Token**. (e) **Auflage B gemessen: `STANDARDWERT` kommt über die fünf Pläne 0-mal vor**, kein Erzeuger in `src/` oder `scripts/`, kein Test, keine Fixture; der Zeichen-Default lebt außerhalb des Modells (`dxf_renderer.py:520/:533`). **Vorschlag: ersatzlos streichen** — nur Vorschlag, Contract nicht ohne Abstimmung zu dritt; Details in `docs/OFFENE_FRAGEN.md` und als Kommentar in #154. (f) **PR #154 geprüft, kein Approval** (Kommentar dort, nur meine vier Punkte): die vier Türbreiten-Herkünfte und alle Zeilennummern stimmen mit `main`, der AST-Riegel ist nicht verletzt, die Durchgangslichte wird nirgends als vorhanden behandelt — aber „fehlende Messung = None" ist in vier Stellen nicht so beschrieben, wie wir liefern (falsche Fundstelle `:175-:177` = `TuerOeffnung` ohne `breite_grund`; Blockwalk-Fallback auf den Schwenkradius übersehen; `tueren.py:83` und die 72 Text-Türen fehlen; der neue Test prüft nur den Contract-Default `Tuer(id, xy_mm)` und damit **nicht** `breite_grund`). Unsere Ist-Zahlen: 612 Türen, 58/104/2/368/80, **80 von 80 `None` mit `breite_grund`**, 0× `0.0`. **(4) Offene Owner-Entscheidungen, benannt statt entschieden:** (i) § 14.6.1 ordnet **(d) Restfläche an Position 2**, umgesetzt ist sie zuletzt — sonst unterläuft sie „LIFT und SCHACHT werden IMMER ausgestanzt" (4 der 5 LIFT/SCHACHT-Räume kommen aus dem R-Zweig, zwei davon nur 0,15 m² über der Entfall-Schwelle). Im Docstring deklariert. (ii) **Regel 2 vor Regel 3** (Owner-Reihenfolge) kostet genau einen Fall: `raum_29` (L mit Stempel) fällt auf −15,6 %, weil `raum_91` (F) zu 99,97 % darin liegt; die Ein-Zeilen-Alternative ist gerechnet, nicht umgesetzt. (iii) **Muthgasse-Türband:** Türen 291 → 270, **ausschließlich Kontaktzonen-Durchgänge** (`durchgang_*` 169 → 148, `tuer_*` unverändert 121) — das Band `>= 280` ist **nicht** aus dem Ist abgesenkt, sondern als eigener strict-xfail `test_soll_tuerzahl_band` mit Beleg ausgelagert; Räume/Segmente/Stiegenhäuser bleiben scharf. (iv) Die **Ursache im F-Zweig** ist weiter offen: `flute_stempel` bekommt `belegte` nicht übergeben (Punkt 2 der Skizze § 14.6) — die Bereinigung ist die nachgelagerte Auflösung, die der Owner beauftragt hat, und sie macht die Ursache sichtbar statt sie zu verdecken. **Belege:** Prüfstrecke über alle fünf Pläne nacheinander, **ohne Parallellast**, exit 0 — Laufzeiten 251,0 / 286,2 / 1813,0 / 56,8 / 51,7 s = 2459 s; Muthgasse 1840,5 → 1813,0 s (−1,5 %), die Bereinigung kostet keine messbare Laufzeit. Volle Suite: `1264 passed, 10 skipped, 2 deselected, 12 xfailed, 2 warnings in 1181.23s (0:19:41)`, exit 0, **keine XPASS-Zeile**. Gezielt: `test_soll_muthgasse` 9 passed / 4 xfailed / **0 XPASS** (610,66 s), Riegel + Bereinigung + Kürzel 49 passed, `test_bereinigung` 24 passed, `tests/contract tests/raumerkennung` 333 passed, `gen_schema --check` in sync, `ruff` clean. **XFAIL-Bilanz: 12 strict-xfails (vorher 11), keiner gedreht, keiner entfernt** — neu ist ausschließlich das ausgelagerte Türband. Zahlen im Detail: `docs/ENIS_UEBERGABE_0908.md` §§ 19 + 20 + 21, `Projekte/_ergebnis/VERLAUF.md`.

- 2026-09-11 **Selman → @mvpo3 + @EnisAMG + @polatselman: Übersichtskarten der Raumerkennung
  für 62 von 63 Plänen erzeugt, plus INDEX + Auffälligkeiten-Bericht.** Branch
  `selman/extents-ausreisser`, **kein Merge, kein Push.** **Kein Code in `src/` geändert**,
  `hauptengine/contracts/**` unberührt, keine Contract-Änderung.
  **Was erzeugt wurde:** `scripts/analyse/uebersicht_karte.py` (neu, dauerhaft) rendert je
  Plan `uebersicht.png` / `.json` / `.md` — Räume nach Kategorie eingefärbt, untypisierte
  Räume magenta „nicht erkannt“, Wohnungen farbcodiert, Innenhöfe, Ausgänge nach
  `Ausgang.typ` (`final_exit` / `stair_exit`), Fluchtweg-Segmente, verschluckte Räume.
  Es wiederverwendet die Prüfstrecken-Helfer aus `scripts/plan_pruefen.py` (Ladepfad,
  Rotation, Maßstab, matplotlib-Konventionen) — nichts davon neu gebaut.
  **Wo es liegt:** `Projekte/_uebersicht/<plan>/` · Index + Bericht
  `Projekte/_uebersicht/INDEX.md` · die 17 **fertigen Elektro-/Notbeleuchtungspläne**
  (Ergebnisbeispiele mit bereits gesetzten Leuchten, **keine** leeren Architekturpläne)
  getrennt unter `Projekte/_uebersicht/_elektro_beispiele/<plan>/`.
  **Sweep:** 63 Pläne, rund 133 min Wanduhr (2 parallel + 2 serielle Nachläufe), 219,9 min
  CPU. 46 Architekturpläne (45 Karten), 17 Elektro-Beispiele, 1 nicht auswertbar.
  **Kein Normurteil:** die Karte zeigt, was `ArchitekturRaumProvider.parse()` liefert; wo
  die Erkennung nichts liefert, steht `0` bzw. „nicht erkannt“ mit Grund — nichts ergänzt,
  nichts geraten.
  **@mvpo3 (Platzierung):** **7 Pläne haben 0 erkannte Ausgänge** — weder `final_exit` noch
  `stair_exit`: `Barawitzka_2DG`, `Barawitzka_FDM`, `Barawitzka_KG`, `Herrenholz_OG1`,
  `Herrenholz_OG2`, `Herrenholz_UG`, `Rennweg_OG2`. Auf diesen Plänen fehlt der Bezugspunkt,
  eine Platzierung kann dort nicht sinnvoll ankern. `Herrenholz_OG1` ist der härteste Fall:
  300 Räume, 640 Türen, 55 Segmente — und **kein einziger** Ausgang. Dazu **29 Pläne ohne
  jeden `final_exit`** (in OGs erwartbar, hier nur zur Kenntnis) und 2 Pläne mit
  **0 Fluchtweg-Segmenten** (`Herrenholz_EG` trotz 498 Räumen und 15 `final_exit`,
  `Barawitzka_KG`). Details + Tabellen in `Projekte/_uebersicht/INDEX.md`, Abschnitt
  „Auffälligkeiten“ 3 und 4.
  **@EnisAMG (Vokabular):** **1070 von 6188 Räumen sind untypisiert** (`raum_typ` leer),
  gehäuft in Keller-/Untergeschossen — `BaufeldE2_NB_UG` 237/266 (89 %), `Mollgasse_KG1`
  24/31 (77 %), `Fischamend_E_UG` 30/43 (70 %), `Barawitzka_KG` 28/41 (68 %),
  `Mollgasse_KG2` 19/30 (63 %), `Fischamend_BT1_UG` 21/44 (48 %), `Rennweg_UG` 10/23 (43 %).
  Größter Absolutwert `Herrenholz_EG` mit 244. Das ist eine **Vokabular-Frage**: der
  Raum-Typ-Wortschatz greift im UG/KG nicht. Die Erkennung kann keine Typen erfinden, sie
  meldet sie als untypisiert. Welche Bezeichnungen aus den UG-Plänen in den Wortschatz
  sollen, entscheidet `normwissen`.
  **Owner-Entscheidungen offen:** (1) `Herrenholz_UG` und `Herrenholz_OG2` sind **dieselbe
  Datei zweimal** (identische Kennzahlen, 28.284.638 vs. 28.284.639 Byte) — welcher ist das
  echte Geschoss? (2) `Barawitzka_FDM` ist ein Fundamentplan, wahrscheinlich kein Grundriss.
  (3) `Aichholzgasse` (`26_0507_AICH.dxf`, 146 MB) ist **nicht auswertbar**:
  `ValueError: Keine Wand-Entities gefunden — Layer-Muster prüfen.` — die Layer-Benennung
  dieses Büros passt nicht auf die Wand-Muster der Erkennung. (4) Die **8 Muthgasse-Karten**
  sind zahlenmäßig korrekt, aber als PNG unbrauchbar: mehrere abgesetzte Zeichnungen im
  Modelspace, der Auto-Zoom umfasst alles, `pp._varianten_bounds` kennt nur den
  Barawitzka-Stempel-Prefix. (5) `Baufeld_E2.zip` (zweite Fassung, andere MD5) wurde
  **nicht** geprüft.
  **Grenze der Prüfung:** sichtgeprüft sind nur `Rennweg_UG`, `Herrenholz_EG`,
  `Muthgasse_E2`. Die übrigen ~59 PNGs wurden **nicht** einzeln angesehen.

- 2026-09-10 **Selman → @EnisAMG + @mvpo3 + @polatselman: Freeze-Absicherung nach dem #149-Vorfall (Gate gebaut, Branch Protection FEHLT und muss vom Owner gesetzt werden), Überlappungs-Riegel eingefroren, Prüfstrecke + volle Suite gelaufen.** Branch `selman/extents-ausreisser`, PR #152, **kein Merge**. Commits dieser Runde: `f7fb165` (Contract-Freeze-Gate) · `79fc0bb` (Überlappungs-Riegel + Enis-Frage + Bereinigungsregeln) + dieser Abschluss-Commit. **Kein Code in `src/` geändert**, `hauptengine/contracts/**` unberührt, **keine Contract-Änderung**. **(1) Freeze-Absicherung.** Anlass: PR #149 wurde am 2026-09-10 10:04 UTC mit **0 Reviews** gemerged und trug den Bump `raum_modell` 1.4.0 nach `main` — der PR war am 09.09. mit anderem Inhalt angelegt, 55 Commits kamen später dazu, der Titel blieb alt. Neu: **Check `contract-freeze`** (`.github/workflows/contract-freeze.yml` + `.github/scripts/contract_freeze_check.py`). Er prüft bei **jedem** Push (`pull_request: opened, synchronize, reopened` + `pull_request_review: submitted, dismissed`), ob ein PR eine Datei unter `hauptengine/contracts/` anfasst oder `CONTRACT_VERSION` im Patch ändert (nur in `.py` gewertet — der Bezeichner steht als Prosa in den Docs); wenn ja, müssen **alle CODEOWNERS aus `.github/CODEOWNERS`** (@mvpo3, @EnisAMG, @polatselman) **auf dem aktuellen `head_sha`** approven — `review.commit_id == head_sha`, nicht `submitted_at`, weil committer-Daten nach Rebase/Amend beliebig alt sind; pro Login zählt nur das jüngste wertende Review, ein späteres CHANGES_REQUESTED schlägt ein früheres APPROVED. Veraltete Approvals werden als Bot-Kommentar (Marker, wird aktualisiert statt angehängt) mit „approvet auf `X`, seither N Commits" gemeldet. Berührt ein PR keine Contracts, endet der Check **grün** (kein Skip), damit er als Required Status Check taugt. Gegen echte PR-Daten durchgespielt: **#149 → exit 1** („Approval aller CODEOWNERS auf `1d9c03a` nötig"), **#152 → exit 0**. **ACHTUNG @mvpo3: auf `main` gibt es KEINE Branch Protection** (`GET /repos/.../branches/main/protection` → **404**), und unser Token hat `admin:false`, kann sie also nicht setzen — **das Gate allein verhindert nichts, es muss als Required Status Check verdrahtet werden**. Fertige Anleitung inkl. UI-Pfad und äquivalentem `PUT .../branches/main/protection` (3 Approvals, Code Owners, `dismiss_stale_reviews`, `contexts: ["test","contracts","contract-freeze"]`, `strict: true`, `enforce_admins`) in **`docs/BRANCH_PROTECTION.md`**. Regel zusätzlich prosaisch in `CLAUDE.md` und `docs/CONTRACTS.md`: *ein Approval gilt nur für den Stand, auf dem es erteilt wurde.* **(2) Riegel-Test.** `tests/naht/test_ueberlappung_riegel.py` (11 Tests, 0,24 s) friert die Überlappung als **Obergrenze** ein: Barawitzka 9 / 42,3 m², Mollgasse 16 / 45,8 m², Muthgasse 37 / 174,2 m², Rennweg_EG 0, Rennweg_OG3 0, Summe 62 / 262,3 m². Bänder **vor** dem Einfrieren selbst nachgemessen, decken sich exakt mit § 14.2; steigt eine Zahl → rot, sinkt sie → Band im selben Commit nachziehen. Datenquelle sind die eingecheckten `Projekte/_ergebnis/<Plan>/raeume.json` (die DXF sind per `.gitignore` nicht getrackt, ein Parse-Test wäre in CI immer geskippt). **Kein `xfail`-Zielbild mit Sollwert 0** — es trüge keine Information, die der Obergrenzen-Test nicht schon trägt. **(3) SCHLEUSE-Frage an @EnisAMG (`docs/OFFENE_FRAGEN.md`, neuer Abschnitt).** `raum_65` (Muthgasse E2) trägt einen vollständigen `A-AREA-IDEN`-Stempel (`E2-VF-11a` / `Schl.` / `13,04 m²` / `Ker.Bel.`), aber `raumtyp_flags('Schl.')` und `classify_room('Schl.')` liefern `None`/`UNKNOWN` → es entsteht gar kein `Stempel`. **Ist `Schl.` in dieser Plan-Familie eine Schleuse oder ein Schlafzimmer?** Für Schleuse: 0 mm zu fünf STIEGENHAUS-Polygonen (~8,2 m Kontaktlänge), 5 Türen mit `EI ₂ 30-C`, Präfix `VF` trägt in Muthgasse ausschließlich `STGH`/`Gang`/`Aufzug`/`Podest`/`Stiege`. Für Schlafzimmer spricht **nur** die Fläche — und die auch nur bei `raum_65`, nicht bei `raum_67` (3,73 m², gleiche Nummernserie). Gemessene Wirkung: `ALLGEMEIN_ERSCHLIESSUNG` typisiert 2 Räume und macht `tuer_50` zur `stiegenhaustuer` (+1 `stair_exit`); `WOHNUNG_PRIVAT` hat keine Wirkung auf die Ausgänge. Falschtrefferrisiko für den Token `schl` über alle fünf Pläne: **0**. **Wir raten nicht** — `SCHLEUSE` existiert im Kanon nicht, und der Ersatz `GANG`/`VORRAUM` entscheidet über Leuchte oder keine Leuchte. Brauchen von dir: Kanon-Typ + Nutzungsklasse, oder Wörterbucheintrag `schl → ZIMMER`. **(4) Überlappungszahlen für @mvpo3 — Leuchten können in verschluckten LIFT/SCHACHT-Polygonen landen.** Auf den Ergebnissen des heutigen Laufs neu gemessen: **62 von 245 Räumen (25,3 %)** überlappen einen anderen um >5 % ihrer eigenen Fläche, **262,342 m² von 3329,4 m² (7,9 %)** gehören mehr als einem Raum. Je Plan: Barawitzka 9/47 (19,1 %) · 42,253 m² = 7,8 % · 0 verschluckte LIFT/SCHACHT — Mollgasse 16/62 (25,8 %) · 45,845 m² = 4,1 % · 0 — **Muthgasse 37/101 (36,6 %) · 174,245 m² = 16,2 % · 1** — Rennweg_EG 0/21 · 0,0 m² · 0 — Rennweg_OG3 0/14 · 0,0 m² · 0. Der eine Verschluck-Fall: **`raum_79` (LIFT) liegt zu 97,6 % in `raum_88` (STIEGENHAUS)**; 13 Räume liegen zu >90 % ihrer eigenen Fläche in einem anderen. Ursache ist der F-Zweig (Stempel-Flutung bekommt `belegte` nicht übergeben, `stempel_flutung.py:227-233`, `kaskade.py:110-126`), **nicht** die Rasterung: 31 von 32 relevanten Muthgasse-Paaren sind F↔L, 100 % der doppelt belegten Fläche haben einen F-Raum auf mindestens einer Seite, aus dem R-Zweig stammt kein einziger Überlapper, und Rennweg hat 0 F-Räume und 0 Überlapper bei 35 Räumen (§ 18). Douglas-Peucker 20 mm wurde **nur gemessen, nicht angewendet** und taugt nicht als Bereinigung: 62 bleibt 62, IDs identisch, 262,342 → 262,043 m² (§ 17). **Die Bereinigung selbst ist weiterhin NICHT freigegeben** (§ 14.6/§ 14.6.1, fünf Owner-Fragen offen). **(5) Weiterhin OFFEN an @EnisAMG: das Approval für `raum_modell` 1.4.0**, das mit PR #149 ohne dein Approval auf `main` gelandet ist. Nichts davon wird zurückgenommen — der Owner will absichern, nicht revertieren; das neue Gate verhindert die Wiederholung, sobald die Branch Protection steht. **Belege:** Prüfstrecke über alle fünf Pläne nacheinander, ohne Parallellast, exit 0 — Laufzeiten 258,6 / 290,6 / 1840,5 / 56,1 / 51,1 s; **KEINE Kennzahl bewegt sich gegen den Lauf `47df2d9`, alle fünf `raeume.json` sind byte-identisch** — das erwartete Ergebnis, weil seit `47df2d9` kein `src/`-Code geändert wurde. Volle Suite: `1211 passed, 10 skipped, 2 deselected, 11 xfailed, 2 warnings in 1153.08s (0:19:13)`, exit 0, **keine XPASS-Zeile**; +11 gegen die 1200 des Vorlaufs sind exakt die neuen Riegel-Tests. **Kein xfail gedreht, kein Zielband geändert.** Zahlen im Detail: `docs/ENIS_UEBERGABE_0908.md` §§ 16 + 17 + 18, `Projekte/_ergebnis/VERLAUF.md`.
  - **Nachtrag 2026-09-12 (Selman, Korrektur — der Eintrag oben bleibt unverändert stehen):** die zwei In-Plan-Belegsätze zu `Schl.` in diesem Eintrag sind gemessen widerlegt. „~8,2 m Kontaktlänge“ ist mit der dortigen Definition nicht reproduzierbar (gemessen Σ 4,72 m, Gegenrichtung Σ 5,21 m; Einzelkanten 662 / 1132 / 1131 / 1131 / 662 mm), und die „0 mm zu fünf STIEGENHAUS-Polygonen“ stammen aus Überlappung mit den Treppen-Block-Extents (`geometrie_typ.py:47-63`, `:89`), nicht aus Wandkontakt: gegen das STIEGENHAUS-Polygon `raum_88` aus `raeume.json` sind es 4848 mm, und dieselbe 0-mm-Klasse enthält die Küchen `raum_29` / `raum_91` / `raum_86` — das Merkmal trennt Schleuse nicht von Küche. Die Entscheidung von Enis `E2-VF-11a` = `SCHLEUSE` bleibt tragfähig; tragend ist das Vergleichsgeschoss (`DBA-Abstr. Schleuse` in E8 627 mm, E9 10 mm von `raum_65`). Belege: `docs/OFFENE_FRAGEN.md § „Zwei Korrekturen am Abschnitt darüber“ (:933-948)`, `docs/ENIS_UEBERGABE_0908.md` § 19.2.

- 2026-09-10 **Selman → @EnisAMG + @mvpo3: Abschluss-Runde 3 — `raum_65` ist eine Vokabular-Lücke (nicht umgesetzt), und die Raumpolygon-Überlappungen sind vermessen: 25,3 % aller Räume, 262,3 m² doppelt belegt.** Branch `selman/extents-ausreisser`, **kein Push, kein PR, kein Merge**. Commits dieser Runde: `fff9f65` (raum_65-Befund) + dieser Abschluss-Commit. **Kein Code in `src/` geändert** (`git diff --stat 47df2d9..HEAD -- src/` ist leer), `hauptengine/contracts/**` unberührt. **Belege:** `pytest -q -rX` → `1200 passed, 10 skipped, 2 deselected, 11 xfailed, 2 warnings in 1169.34s (0:19:29)`, exit 0, **keine XPASS-Zeile**. Prüfstrecke bewusst **nicht** gelaufen und **kein Lauf erfunden**: ohne `src/`-Änderung liefert sie per Konstruktion die Zahlen des Laufs `2026-09-10 13:35 · 47df2d9`; `Projekte/_ergebnis/VERLAUF.md` bleibt aus demselben Grund unverändert. Voller Ist-Stand `docs/ENIS_UEBERGABE_0908.md` §§ 13 + 14 + 15; alle älteren Zahlen stehen unverändert daneben. **(1) `raum_65` (Muthgasse E2): nicht umgesetzt, xfail bleibt, Zielband unverändert.** Der Raum trägt einen vollständigen `A-AREA-IDEN`-Stempel (`E2-VF-11a` / `Schl.` / `13,04 m²` / `Ker.Bel.`), aber `raumtyp_flags('Schl.')` und `classify_room('Schl.')` liefern `None`/`UNKNOWN` → `stempel_anker.py:219-221` bricht ab, es entsteht gar kein `Stempel`. Der Plan **belegt** die Verkehrsfläche (Präfix `VF` trägt in Muthgasse ausschließlich `STGH`/`Gang`/`Aufzug`/`Podest`/`Stiege`, 0 mm zu fünf STIEGENHAUS-Polygonen, `EI ₂ 30-C`-Türen), aber `SCHLEUSE` existiert im Kanon nicht — ersatzweise `GANG` zu setzen wäre das verbotene Raten und entschiede über die Nutzungsklasse und damit über eine Leuchte. Zusätzlich gemessen: der xfail wäre ohnehin nicht gefallen, weil `tuer_50` **keine Blocktür** ist (780,5 mm zum nächsten `A-DOOR`-INSERT) — Korrektur an § 11.5 / § 12.5 #8 in § 13.7. Untypisiert über alle fünf Pläne: 50 von 278 Räumen = 18,0 % (Bara. 6/48 · Moll. 28/85 · Muth. 10/110 · R_EG 5/21 · R_OG3 1/14). **(2) Überlappende Raumpolygone — nur gemessen und berichtet, auf ausdrücklichen Owner-Wunsch NICHTS umgesetzt** (§ 14, Quelle `Projekte/_ergebnis/<Plan>/raeume.json`, Lauf `47df2d9`): Überlapper >5 % der eigenen Fläche — Barawitzka_EG 9/47 · Mollgasse_EG 16/62 · **Muthgasse_E2 37/101 (37 %)** · Rennweg_EG 0/21 · Rennweg_OG3 0/14 = **62 von 245 (25,3 %)**; doppelt belegte Grundfläche **262,3 m² von 3329,4 m² (7,9 %)**, auf Muthgasse 174,2 m² = 16,2 %. **Herkunft: 48 von 50 relevanten Paaren (96 %) und 94 % der Doppelfläche haben einen F-Raum (Stempel-Flutung) auf mindestens einer Seite; aus `rest_komponenten.py` stammt kein einziger Überlapper** — `flute_stempel` bekommt die belegten Raumpolygone gar nicht übergeben (`stempel_flutung.py:227-233`), der F-Zweig hängt ungeprüft an (`kaskade.py:110-126`), und die H-Dedup nutzt IoU statt Anteil am kleineren Polygon (`kaskade.py:90`). Gegenbeweis: der R-Zweig blockiert `belegte` im Raster (`rest_komponenten.py:147-151`) und hat **0 von 9** Überlappern. **(3) @EnisAMG:** (a) **Der Bump `raum_modell` 1.3.0 → 1.4.0 ist mit PR #149 ohne dein Approval auf `main` gelandet** (`CONTRACT_VERSION = "1.4.0"` steht dort); das Approval steht formal weiterhin aus — bitte nachziehen oder widersprechen, wir haben in diesem und im letzten Auftrag keine Contract-Datei angefasst. (b) **Vokabular-Lücke aus `raum_65`:** braucht der Kanon ein Label `SCHLEUSE` (Nutzungsklasse ALLGEMEIN_ERSCHLIESSUNG?), oder soll `Schl.` auf einen bestehenden Typ abbilden? Dazu `Vorr.`/`Schrankr.` und `SR`/`Aufzug` — drei Fragen in `docs/OFFENE_FRAGEN.md`. Solange sie offen sind, bleibt `raum_65` untypisiert; ein geratener Typ ist schlimmer als keiner. (c) **Die Mollgasse-„Laubengang"-Frage aus `47df2d9` ist weiter offen** (12 `flaeche_fehlt`-Segmente auf dem Außenanlagen-Layer `09-WEG_G00-LEG-M0`; eine „nächste durchgehende Linie"-Regel griffe bei 107 von 107 korrekt gemessenen Segmenten auf das Achsraster `02-AXO` zu). **(4) @mvpo3:** die Überlappungen berühren direkt die Platzierung. **`raum_79` (LIFT, 4,04 m²) liegt zu 98 % in `raum_88` (STIEGENHAUS, geflutet, `flutung_unsicher`)** — für die Belegung des Stiegenhauses zählt Liftschachtfläche mit, und eine Leuchte kann rechnerisch im Liftschacht landen. Gleiche Klasse: `raum_53` (KINDERWAGENRAUM) zu 100 % in `raum_51` (Mollgasse), `raum_37` (STIEGENHAUS) zu 99 % in `raum_43` (TERRASSE, Barawitzka), und auf Muthgasse drei als KÜCHE typisierte F-Räume, die ganze Wohnungen inkl. BAD/ZIMMER/BALKON verschlucken (`raum_86` 43,7 m², `raum_85` 33,5 m², `raum_92` 20,7 m²) — falsche Nutzungsklasse über echter Grundfläche, auf der Fluchtweglogik und Platzierungsdichte arbeiten. Insgesamt **13 Räume liegen zu >90 % ihrer eigenen Fläche in einem anderen Raum**. Bitte gegenprüfen, ob eure Strategien darauf schon robust sind. **(5) Offen für den Owner (Punkt 4, § 14.6, ausdrücklich nur VORSCHLAG):** Ursache im F-Zweig beheben (belegte Flächen in `stempel_flutung.py` blockieren) oder nachgelagert auflösen? Was passiert mit einem F-Raum, der nach dem Abzug unter 1 m² fällt oder in Bruchstücke zerfällt? Darf „Flag ok" sinken, wenn dafür die Überlappung verschwindet? Ist die Rangfolge L > H > F richtig? Soll „Überlappende Räume / doppelt belegte m²" dauerhaft in `VERLAUF.md` und die Prüfstrecke? Ohne diese Antworten wird nichts gebaut. **Kein xfail gedreht, kein Zielband geändert** — 11 strict-xfails vor und nach dem Auftrag, Bilanz mit Begründung je Test in § 15.2.
  - **Nachtrag 2026-09-12 (Selman, Korrektur — der Eintrag oben bleibt unverändert stehen):** der Satz „der Plan **belegt** die Verkehrsfläche (… 0 mm zu fünf STIEGENHAUS-Polygonen …)“ stützt sich auf ein widerlegtes Merkmal. Die 0 mm sind Überlappung mit den Treppen-Block-Extents, nicht Wandkontakt (gegen das STIEGENHAUS-Polygon `raum_88` aus `raeume.json`: 4848 mm), und dieselbe 0-mm-Klasse enthält die Küchen `raum_29` / `raum_91` / `raum_86`; die an anderer Stelle genannte Kontaktlänge ist Σ 4,72 m statt ~8,2 m. Das `VF`-Präfix- und das `EI ₂ 30-C`-Argument bleiben unberührt, und die Entscheidung von Enis für `E2-VF-11a` bleibt tragfähig (Unterscheider = Vergleichsgeschoss E8/E9). Belege: `docs/OFFENE_FRAGEN.md § „Zwei Korrekturen am Abschnitt darüber“ (:933-948)`, `docs/ENIS_UEBERGABE_0908.md` § 19.2.

- 2026-09-10 **Selman → @EnisAMG + @mvpo3: Abschluss-Runde 2 — Muthgasse-Befund widerlegt und fachlich ersetzt, Mollgasse-Grenze dokumentiert, lastfreie Prüfstrecke + volle Suite gelaufen.** Branch `selman/extents-ausreisser`, **kein Push, kein PR, kein Merge**. Commits dieser Runde: `c3cd3e7` (die „3 verlorenen Türtypisierungen" widerlegt, Zielbild fachlich ersetzt) · `47df2d9` (Mollgasse-„Laubengänge" als bewusste Grenze dokumentiert + Frage an @EnisAMG) + dieser Abschluss-Commit. **Kein Code in `src/` geändert, keine Contract-Datei angefasst.** **Belege:** `scripts/plan_pruefen.py` über alle fünf Prüfpläne, exit 0, Lauf `2026-09-10 13:35 · 47df2d9`; `pytest -q -rX` → `1200 passed, 10 skipped, 2 deselected, 11 xfailed, 2 warnings in 1148.43s (0:19:08)`, exit 0. Voller Ist-Stand `docs/ENIS_UEBERGABE_0908.md` §§ 11 + 12; alte Zahlen aus §§ 4–10 stehen unverändert. **(1) Was sich bewegt hat.** Kennzahlen: **nichts**. Gegen den Lauf `e9837b0` ist auf allen fünf Plänen jede Zahl identisch (Barawitzka_EG 55/106 · Mollgasse_EG 70/147 · Muthgasse_E2 206/291 · Rennweg_EG 24/41 · Rennweg_OG3 15/27) — erwartet, weil diese Runde nur Tests und Dokumente angefasst hat. Bewegt haben sich die **Laufzeiten**: die Prüfstrecke lief diesmal **ohne Parallellast**, Muthgasse_E2 **2116,4 s → 1852,4 s** gegen den lastfreien Vorbefund 1838 s = **+0,8 %**. Damit ist belegt, dass die 2116 s des Vorlaufs Lastkontext waren und keine Verschlechterung der Pipeline — die Fußnote aus § 10.2 ist erledigt. **(2) Fachlich: der Befund „3 echte Türen haben ihre Typisierung verloren" ist widerlegt.** Zwei tatsächlich gelaufene Provider-Parses derselben DXF aus zwei Worktrees (`0d7c5db` vs. HEAD) zeigen: die 3 war eine **Saldo-Zahl** (15 − 5 Fahnen − 7), keine Mengendifferenz. Lagebezogen sind es 11 Ausgänge weg / 1 geblieben / 4 neu; davon 5 Beschriftungs-Fahnen und 6 Kontaktzonen-Artefakte. Es ist **keine echte Tür** verloren gegangen. Tabelle je Tür mit Position, beiden Nachbarräumen vorher/nachher und der greifenden Regel (`tuer_typisierung.py:154-156` → `ausgaenge.py:62-64`) in § 11.3. **Das Band `>= 9` in `test_soll_stair_exits` wurde trotzdem NICHT abgesenkt** — der einzige gemessen gedeckte Ersatzwert wäre der Ist-Stand selbst, und aus dem Ist abgeleitete Bänder sind hier verboten. Stattdessen steht der fachliche Ersatz als eigener strict-xfail daneben: `test_soll_stair_exit_aus_echter_blocktuer` (Soll ≥ 1 `stair_exit` an einer echten `A-DOOR`/`A-GLAZ`-Blocktür, Ist 0 von 5), Zielwert aus `tuer_50` (334453/106403, `von_raum=stiegenhaus_1`), nicht aus dem Ist. **XFAIL-Bilanz: 11 strict-xfails, 0 XPASS, 0 failed. Kein xfail wurde gedreht, kein Zielband geändert** (§ 12.5 listet alle elf mit je einem Satz Begründung). **(3) @EnisAMG — was dich betrifft.** (a) **Offen und wartend: dein Approval für den Contract-Bump `raum_modell` 1.3.0 → 1.4.0** (`Tuer.breite_mm: float | None` statt `0.0`, plus `breite_quelle` / `breite_grund` / `lichte_mm`). Solange das offen ist, ist `hauptengine/contracts/**` **eingefroren** — in dieser Runde wurde dort nichts angefasst. (b) **Neue Norm-Frage in `docs/OFFENE_FRAGEN.md`: Fluchtwegbreite auf Wegen im Freien.** 12 Mollgasse-Segmente bleiben `flaeche_fehlt` (Messstand 287/307 = 93,5 %). Gemessen und belegt: es sind **keine Laubengänge** — alle 12 liegen auf dem Außenanlagen-Layer `09-WEG_G00-LEG-M0` (Zaun, Mauersockel, Pflanztrog h=40 cm, Rigol, Gehsteig, Restmüll), 0 Treffer auf `VORDACH|ÜBERDACHUNG|AUSKRAGUNG|LAUBENGANG|ARKADE|PERGOLA|CANOPY`; davon 6 entartete Polylinien-Endpunkte (0,0–105 mm), 4 Linien = 2 Doppellinien-Paare (Abstand 50 mm), 2 echte Außenwege (`seg_4`, `seg_7`). Eine Erkennungsregel wurde **bewusst nicht** gebaut: die Gegenprobe zeigt, dass eine „nächste durchgehende Linie"-Regel bei **107 von 107** heute korrekt gemessenen Segmenten auf das Achsraster `02-AXO` zugreifen und jede richtige Messung durch eine erfundene ersetzen würde. Deine drei Fragen: gilt eine Breitenanforderung jenseits der Gebäudehülle überhaupt; worauf bezöge sich die Breite bei Zaun/Pflanztrog; bleiben diese Wege im Nenner 307. `normwissen/data/oib_rl4_fluchtwegbreiten.yaml` und `knowledge/` geben dazu nichts her. **(4) @mvpo3 — was dich betrifft.** (a) `test_soll_referenz_trefferquote` bleibt strict-xfail: **18 %** (2 Treffer / 9 fehlend / 5 überzählig auf Barawitzka_EG), unverändert — Zielbild ≥ 80 %. (b) In deiner Lane wurde in dieser Runde **nichts geändert**; `dxf_renderer.py` ist seit `1ddb752` (Konstante `_ZEICHEN_ERSATZBREITE_MM`) unberührt, `lux_nachweis_bericht.py:329/:333` weiterhin nur gemeldet. (c) Weiter gemeldet, nicht angefasst: Render-Speicher `_figur` 8×/Plan. **(5) Offen und nicht geglättet:** der Manhattan-Dedupe (`provider.py:148-157`) entkoppelt die `stair_exit`-Kennzahl von den Türen · `tuer_typisierung.py:152-156` feuert auch bei `von_raum == nach_raum` (bei 4 der 5 Fahnen war genau das der Fall) · die 9 STIEGENHAUS-Polygone auf Muthgasse sind höchstens 4 verschiedene Kerne · `lichte_mm` hat weiterhin keinen Erzeuger und bleibt `None` · die Nennerfrage Muthgasse `A-DETL` ist eine Entscheidung, keine Messung.

- 2026-09-10 **Selman → @EnisAMG + @mvpo3 + 3-Owner-Runde: Abschluss der fünf Punkte, Prüfstrecke und volle Suite gelaufen, Zahlen im Bericht fortgeschrieben.** Branch `selman/extents-ausreisser`, **kein Push, kein PR, kein Merge**. Commits `8b35e53` (Beschriftungsfahnen sind keine Türen) · `58cd3a0` (Contract `raum_modell` 1.3.0 → **1.4.0**) · `1ddb752` (Riegel gegen erfundene Maße) · `9141a3c` (Breitenmessung) · `e9837b0` (Fenstersignatur) + dieser Abschluss-Commit. **Belege:** `scripts/plan_pruefen.py` über alle fünf Prüfpläne, exit 0; `pytest -q -rX` → `1199 passed, 10 skipped, 2 deselected, 10 xfailed, 0 XPASS, 2 warnings in 1648.74s`. Voller Ist-Stand in `docs/ENIS_UEBERGABE_0908.md` § 10, Tabelle § 7 in **§ 7.1 (vorher, 2026-09-09)** und **§ 7.2 (Ist, 2026-09-10)** aufgeteilt — alte Zahlen bleiben stehen. **(1) Welche Zahlen sich bewegt haben.** Gegen den letzten Vollauf `ab0ad51` bewegt sich **nur Muthgasse_E2**: Türen typisiert 219/308 → **206/291**, Räume gesamt 102 → 101, mit Stempel 91 → 90, Kaskade F 21 → 20, `final_exit` 2 → 5, **`stair_exit` 12 → 5**, Segmente GRAPH 15 → 5 / FALLBACK 1 → 2, Wohnungen 7 → 8. Barawitzka_EG 55/106, Mollgasse_EG 70/147, Rennweg_EG 24/41, Rennweg_OG3 15/27 sind **identisch** zum Vorlauf. Fluchtweg-Breitenmessung **209/307 (68,1 %) → 287/307 (93,5 %)**. Türbreiten-Herkunft über 612 Türen: 58 BLOCKNAME / 104 SCHWENKRADIUS / 2 SUMME / 368 OEFFNUNG / 80 UNBEKANNT, **0** Türen mit `0.0`, **0** ohne `breite_grund`, **0** mit gesetztem `lichte_mm`. **Offener Befund, nicht geglättet:** von den 7 verschwundenen `stair_exit` waren 5 Beschriftungsfahnen (erfundene Ausgänge), aber **3 echte Türen haben ihre Typisierung verloren** — Ursache in der Tür-Typisierung, **nicht aufgeklärt**. Band **nicht** abgesenkt, sondern als strict-xfail `tests/naht/test_soll_muthgasse.py::test_soll_stair_exits` sichtbar gehalten. **(2) @EnisAMG — was dich betrifft.** Die Contract-Ergänzung an `Tuer` ist umgesetzt und schemageneriert (Drift-Gate grün): `breite_mm: float | None` (None = nicht gemessen, vorher `0.0`), `breite_quelle` (`BLOCKNAME` · `GEOMETRIE_SCHWENKRADIUS` · `GEOMETRIE_SUMME` · `GEOMETRIE_OEFFNUNG` · `ATTRIBUT` reserviert · `STANDARDWERT` Reserve · `UNBEKANNT`), `breite_grund` (Pflichtbegleiter von `UNBEKANNT`), `lichte_mm: int | None` — **`lichte_mm` bleibt None**, es gibt keinen Erzeuger und es wird **nie** aus `breite_mm` abgeleitet; `lichte_quelle` ist bewusst nicht angelegt. Alles additiv mit Default, **3-Owner-Approval für 1.4.0 steht weiter aus** — sag bitte, ob du den Schnitt so trägst. **Dein Test `tests/normwissen/test_quellenblock_e07_rl4.py:301` ist heute grün** (`31 passed`) und von uns **nicht angefasst**, weil wir deine YAML nicht angefasst haben. Inhaltlich ist `assert len(herkunft["befunde"]) == 3` zu niedrig: gemessen sind **vier** Herkünfte, die Doppelflügel-Summe (`tueren.py:259 :: verschmelze_doppelfluegel`, 2 Türen auf Barawitzka_EG) fehlt in `tuerbreite_herkunft`. Vorschlag mit Wortlaut in `docs/ENIS_UEBERGABE_0908.md` § 6.4: vierten Befund ergänzen, `== 3` → **`== 4`** (ausdrücklich **nicht** `>= 3` — die Zahl soll scharf gepinnt bleiben, damit ein fünfter Schreibpfad wieder rot wird). Offene Frage an dich: `tuer_zuordnung.py:157` und `:218` — ein Befund oder zwei? Zählst du sie getrennt, steht am Ende `== 5`. **Zweiter Hinweis:** `:307` (`breiten ⊆ {900, 1000, 1400}`) wird rot, sobald eine Fixture `"breite_mm": null` trägt — das ist der Fixture-Schritt, den wir gemeinsam machen sollten, nicht einseitig. **(3) @mvpo3 — was dich betrifft.** In deiner Lane haben wir genau **eine** Zeile geändert, ohne Verhaltensunterschied: `hauptengine/render/dxf_renderer.py`, `breite = t.breite_mm or 900.0` → `breite = t.breite_mm or _ZEICHEN_ERSATZBREITE_MM` (neue Modulkonstante mit Kommentar über `_draw_tueren`). **Identischer Wert, identisches Bild**, der Wert verlässt `_draw_tueren` nicht und steht in keinem Contract-Feld. Seit der `None`-Migration greift dieser Ersatzwert allerdings für **alle** nicht gemessenen Türen statt nur für die früheren `0.0` — eine Tür mit `breite_mm is None` ist im DXF weiterhin nicht von einer gemessenen 900er zu unterscheiden. Die sichtbare Kennzeichnung (eigener Layer / gestrichelt / Zähler im Render-Summary) haben wir **nicht** gebaut: das ist Umbau am Zeichencode und deine Entscheidung, sie bleibt offen. Unverändert gemeldet, nicht angefasst: `hauptengine/render/lux_nachweis_bericht.py:329` druckt die Montagehöhe aus der Konstante `HM = 2.4` aufs Blatt, während `:92` mit `anf.montagehoehe_mm` rechnet; `:333` behauptet „Wartungsfaktor 0,80", während `:92` `… or 1.0` einsetzt. Falsche Zahlen im Ausgabedokument, kein Maßfeld. **(4) Für alle drei Lanes:** `tests/contract/test_keine_erfundenen_masse.py` (3 Tests) verriegelt per AST über `src/notbeleuchtung/**`, dass kein hartcodierter Zahlenwert in ein Messfeld (`*_mm`/`*_m2` aus den Contracts, ohne Koordinaten, ohne `norm_regelwerk.py`) wandert. Die Messfeldliste kommt aus `model_fields` — **ein neues Maßfeld ist damit automatisch geschützt**. Grenze, damit sie nicht untergeht: eine Zahl über zwei Zuweisungen sieht der Riegel nicht, und eine benannte Konstante umgeht ihn absichtlich (der Zeichen-Ersatzwert soll sichtbar sein). **(5) XFAIL-Bilanz:** **kein einziger xfail ist zu XPASS gekippt.** Die zehn strict-xfails stehen unverändert (Liste in § 10.5); neu gegenüber dem Ausgangsstand (9) ist genau `test_soll_stair_exits` — ein zusätzlich sichtbar gemachter Befund, kein gefallenes Zielbild.

- 2026-09-10 **Selman → @mvpo3 (kleine Änderung in deiner Lane, `hauptengine/render/dxf_renderer.py`) + alle: neuer Riegel `tests/contract/test_keine_erfundenen_masse.py`.** Branch `selman/extents-ausreisser`, **kein Push, kein PR, kein Merge**. **(1) @mvpo3, was ich in `dxf_renderer.py` geändert habe — und was nicht:** aus `breite = t.breite_mm or 900.0` (jetzt Z. 533) wurde `breite = t.breite_mm or _ZEICHEN_ERSATZBREITE_MM`, plus Modul-Konstante mit Kommentar direkt über `_draw_tueren`. **Kein Verhaltensunterschied — derselbe Wert 900.0, dasselbe Bild.** Es ist reine Sichtbarmachung: der Wert war ein stiller Zahlen-Default mitten im Zeichencode und ist jetzt als Zeichen-Ersatzmaß benannt. Er verlässt `_draw_tueren` weiterhin nicht, wird nirgends zurückgeschrieben und steht in keinem Contract-Feld (`_draw_tueren` gibt nur `drawn: int` zurück). **Ich habe deinen Renderer sonst nicht angefasst** — insbesondere nicht die von mir am 09-10 vorgeschlagene sichtbare Kennzeichnung (eigener Layer / gestrichelt / Zähler im Render-Summary) für Türen ohne gemessene Breite. **Die bleibt deine Entscheidung und ist weiter offen:** eine Tür mit `breite_mm is None` ist im DXF nach wie vor nicht von einer gemessenen 900er zu unterscheiden. **Zweiter Punkt in deiner Lane, unverändert gelassen, nur gemeldet:** `hauptengine/render/lux_nachweis_bericht.py` druckt in Z. 329 die Montagehöhe aus der Modulkonstante `HM = 2.4` aufs Blatt, während `:92` mit `anf.montagehoehe_mm` rechnet — zwei Montagehöhen im selben PDF, die gedruckte ist die hartcodierte; und die Bildunterschrift `:333` behauptet unbedingt „Wartungsfaktor 0,80“, während `:92` `getattr(anf,"wartungsfaktor",None) or 1.0` einsetzt (ohne gesetzten Faktor also 1,0 rechnet und 0,80 hinschreibt). Kein Maßfeld, aber eine falsche Zahl im Ausgabedokument. Deine Lane, nicht angefasst. **(2) Neuer Test für alle drei Lanes:** `tests/contract/test_keine_erfundenen_masse.py` liest die Messfelder über `model_fields` **aus den Contracts selbst** (alle Felder auf `_mm`/`_m2`, ohne Koordinatenfelder und ohne `norm_regelwerk.py`, das per Definition Norm-Grenzen trägt) und prüft `src/notbeleuchtung/**` per AST auf drei Muster: `<messfeld> or <Zahl≠0>`, `<messfeld>=<Zahl≠0>` als Keyword-Argument, `min|max(<messfeld>, <Zahl≠0>)`. `0.0` bleibt erlaubt (getrennter Punkt „falsch typisiertes Nicht-Maß“). Ein neues Maßfeld im Contract ist damit **automatisch** mitgeschützt — wer eins anlegt, muss nichts am Test tun. Einzige Ausnahme: `raumerkennung/_port/` (erfindet 800/1000/200 mm, ist aber nicht importierbar); `test_port_bleibt_tot` hält genau das fest und wird rot, sobald jemand `_port/engine_walls` anschließt. `scripts/` wird bewusst nicht gescannt (Prüfwerkzeug, kein Produkt) — die dortige Bogen-Deckelung in `plan_pruefen.py` habe ich trotzdem auf benannte Konstanten `_BOGEN_ZEICHEN_MIN_MM`/`_MAX_MM` gezogen. **Wirksamkeit belegt:** drei Probeverletzungen einzeln eingebaut, jede macht den Test rot mit der jeweils richtigen Meldung, danach zurückgebaut (`git diff` der Datei wieder auf 10+/1-). **(3) Was der Test nicht kann:** eine Zahl über zwei Zuweisungen (`b = 900.0; …; Tuer(breite_mm=b)`) sieht er nicht — dafür bräuchte es Datenflussanalyse. Das Muster kommt im Baum nicht vor; wenn es auftaucht, muss es im Review fallen. Suite-Ergebnis siehe Commit-Text. Nichts gelöscht, keine destruktive git-Operation, kein Push.

- 2026-09-10 **Selman → @mvpo3 @EnisAMG (CONTRACT-TOUCH, Abstimmung steht aus): `raum_modell` 1.3.0 → 1.4.0, `Tuer.breite_mm` 0.0 → None.** Branch `selman/extents-ausreisser`, **kein Push, kein PR, kein Merge** — ausdrücklich vom Owner beauftragt, aber `hauptengine/contracts/**` ist Konsens-Lane (Contract-Freeze, `docs/CONTRACTS.md`): **Approval von @mvpo3 und @EnisAMG fehlt noch.** **Was sich am Contract ändert** (`hauptengine/contracts/raum_modell.py`, Modell `Tuer`, alles **additiv mit Default**, keine Signatur- und keine Pflichtfeld-Änderung, bestehende Erzeuger brechen nicht, die vier Fixtures mit `contract_version 1.1.0` laden unverändert): (1) `breite_mm: float = 0.0` → **`float | None = None`** — `None` heißt „nicht gemessen", `0.0` heißt ab v1.4.0 „gemessen null" und kommt nicht mehr vor; (2) neu `breite_quelle: BreiteQuelle = "UNBEKANNT"` mit dem Vokabular `BLOCKNAME` · `GEOMETRIE_SCHWENKRADIUS` · `GEOMETRIE_SUMME` · `GEOMETRIE_OEFFNUNG` (das sind die **vier tatsächlich gemessenen** Herkünfte über 629 Türen/fünf Pläne: 58 / 77 / 2 / 360) · `ATTRIBUT` (reserviert, heute **kein** Erzeuger) · `STANDARDWERT` (Reserve; gehört **nicht** ins Modell, heute nur `dxf_renderer.py:524`) · `UNBEKANNT` (132 Türen); (3) neu `breite_grund: str | None = None` — Pflichtbegleiter von `UNBEKANNT`, damit fehlende Messung begründet ist statt still; (4) neu `lichte_mm: int | None = None` — nutzbare Durchgangslichte, **bleibt None**, kein Erzeuger, **nie** aus `breite_mm` abgeleitet (es gibt keine belegte Umrechnung Rohbauöffnung/Radius → Lichte). `CONTRACT_VERSION` **1.3.0 → 1.4.0** (Minor, wie 1.1.0/1.2.0/1.3.0 = additive Felder), `scripts/gen_schema.py` gelaufen, `raum_modell.schema.json` mitcommittet, Drift-Gate `tests/contract/test_schema_drift.py` grün, `docs/CONTRACTS.md` § RaumModell nachgezogen. **Kernregel dahinter:** der Code erfindet keine Maße — fehlende Messung ist `None` + `UNBEKANNT` + Grund, nie ein Default, nie ein Normwert, nie ein Mittelwert. **@mvpo3, zwei Punkte in deiner Lane, von mir NICHT angefasst:** (a) `hauptengine/render/dxf_renderer.py:524` `breite = t.breite_mm or 900.0` **erfindet still 900 mm** — bisher für die 132 `0.0`-Türen, ab v1.4.0 für dieselben als `None`. Verhalten unverändert (kein Bruch), aber es ist jetzt der **einzige** Ort im Repo, der ein Maß erfindet, und er sollte es sichtbar tun (Render-Summary/Prüfbericht) statt still. Deine Entscheidung. (b) Bitte bestätige weiterhin, dass für `breite_mm = None` **keine** neue Nutzung in `platzierung/` geplant ist — gemessen liest `platzierung/` `Tuer.breite_mm` heute nicht (Probelauf: `tests/platzierung` 262, `tests/hauptengine` 116, `tests/render` 88, `tests/api` 22 alle grün). **@EnisAMG:** deine `tuerbreite_herkunft` in `oib_rl4_fluchtwegbreiten.yaml` führt **drei** Befunde, gemessen sind **vier** (die Doppelflügel-Summe `tueren.py:259` fehlt). `tests/normwissen/test_quellenblock_e07_rl4.py:301` pinnt `len(befunde) == 3` und ist **heute grün** — ich habe deine YAML und deinen Test **nicht** angefasst (deine Lane). Vorschlag mit Wortlaut, Zeilen und Belegzahlen: `docs/ENIS_UEBERGABE_0908.md` § 6.4 (vierter Befund + `== 3` → `== 4`, ausdrücklich **nicht** `>= 3`; plus die offene Frage, ob `tuer_zuordnung.py:157`/`:218` ein Befund oder zwei sind → dann `== 5`). Zweiter Hinweis: `test_quellenblock_e07_rl4.py:307` (`breiten ⊆ {900, 1000, 1400}`) wird rot, **sobald eine Fixture `"breite_mm": null` trägt** — die Fixtures sind in diesem Schritt unverändert, das kommt im Fixture-Schritt und ist dann gemeinsam zu machen. **Suite nach der Änderung:** siehe Commit-Text. Nichts gelöscht, keine destruktive git-Operation, kein Push.

- 2026-09-10 **Selman → @EnisAMG (Normwissen-Übergabe 0908-v2 verarbeitet) + @mvpo3 (zwei Verbraucher-Zusagen nötig). Branch `selman/extents-ausreisser`, KEIN Contract-Touch, nicht gepusht. Voller Bericht: `docs/ENIS_UEBERGABE_0908.md` (655 Z.). Suite 1177 passed / 0 failed.** **(0) Paket:** die von dir genannte Archiv-SHA-256 ist **nicht prüfbar** — das Paket lag bei mir bereits entpackt, es gab kein `.tar.gz`. Ersatzprüfung über `SHA256SUMS.txt`: **alle 12 Einträge OK**, die 7 kopierten Dateien im Arbeitsbaum sind hash-identisch. Deine Diff-Basis-Blob `7c4b21e` ist mit unserer identisch — **von uns ist nichts verlorengegangen**. **Aber es ist weniger angekommen, als du geliefert hast:** unter `oib_rl2_tabelle6.yaml::ausfuehrungs_verweise.eingeschraenkt` fehlen **zwei Listeneinträge** (Bemessungsbetriebsdauer R 12-2/AC:2019 Tab. 5.1 Fußnote a + der Antipanik-Vorbehalt aus derselben Fußnote); `grep -c "Bemessungsbetriebsdauer"` liefert bei uns 0. Reine Hinweistexte, keine Schwelle, kein Contract-Wert — aber du solltest wissen, dass der Hunk nur teilweise übernommen wurde. Alle übrigen Hunks (`astv_parallelpfad` mit `fundstellen`/`pruefpunkte_kurz`, 11 R-12-2-Vergleichshinweise, meta-Ausgabenvergleich) sind vollständig da; `provider.py` ist byteidentisch zu deinem Zielzustand. **(1) Drei Kollisionen in deinem Paket, alle in deiner Lane:** (a) `astv_arbeitsstaetten.yaml::zwanzig_leuchten` vs. `hauptengine/pipeline.py:51` `_AUTO_PRUEF_SCHWELLE = 20` — der vorhandene Verbraucher zählt `len(platzierung.platzierungen)` **je Geschoss**, 560.9.001.AT meint Sicherheitsleuchten **je zusammenhängendem Gebäudeteil**. Echte Kollision der Bezugseinheit, kein Code-Konflikt. Die Datei ist Leonis', ich habe nichts geändert. (b) OVE E 8101 560.9.2 (max. 20 Leuchten/Endstromkreis, ≤ 60 % Nennstrom) steht **wortgleich in zwei** deiner neuen Dateien (`astv_arbeitsstaetten.yaml:307-312` und `ove_e07_funktionserhalt.yaml:278-306`) — zwei Pflegeorte. (c) **Widerspruch innerhalb des Pakets:** `astv_arbeitsstaetten.yaml:271` (`⚠_zwei_mal_die_zahl_20`) stuft 560.9.001.AT noch als **Ebene D** ein, dein `ASTV_E08_ENTSCHEIDUNGSREGELN.md` § 11.1 korrigiert das ausdrücklich auf **Ebene C**, und Z. 324 ff. derselben YAML trägt die Korrektur bereits. Z. 271 ist nicht nachgezogen. **(2) Dein Wächter-Test schlug bei uns an** — `test_kein_contract_wert_und_kein_konsument` greppt `src/**/*.py` per Substring und traf den **Modul-Docstring** von `raumerkennung/breitenprofil.py`, der deine RL-4-YAML wörtlich als Quelle zitierte. **Kein Ladevorgang, kein Import, kein Normwert** — reine Quellenangabe in Prosa. Ich habe **deinen Test nicht angefasst**, sondern unsere Quellenangabe umformuliert; er ist wieder grün und weiter scharf. Frage an dich: soll der Wächter dauerhaft auf Import-/Ladepfad prüfen statt auf Dateiinhalt? Dann wäre die wörtliche Dateiangabe im Docstring wieder möglich. **(3) natürliche Belichtung — Ist-Stand gemessen, Vorschlag steht, kein Code.** Das Feld existiert nicht (`grep "belicht"` über `src/`, ohne `normwissen/` → 0 Treffer). Vier Codestellen nennen „Fenster", **keine erkennt eines** — schlimmer: `wandkoerper.py:251-253` und `aussenbereich.py:145` **überbrücken Fenster-Öffnungen aktiv**, `tuer_zuordnung.py:170-176` verwirft Wohnungs-Fensteröffnungen. Genau die Belege werden weggeworfen. Fensterherkunft ist **familienabhängig, gemessen**: Muthgasse_E2 Layer `A-GLAZ*` (579 Fensterpunkte, `A-GLAZ-IDEN` ist Beschriftung und muss raus) + 129 ALU_GLAS-SOLIDs · Mollgasse_EG **nur Blockname** (17 INSERTs, Layer ohne jeden Hinweis) · Rennweg EG/OG3 Fenster **innerhalb der `Wall_*`-Blöcke** auf `New_Archicad Windows` (18 bzw. 11), **alle mit demselben Dummy-Einfügepunkt** — echte Lage nur über die Blockgeometrie · **Barawitzka_EG hat null Fensterobjekte** (0 Layer, 0 Blöcke, 0 INSERTs), einziges Glas-Signal sind 4 Texte „Glaswand EI30 + A2". Oberlicht/Lichtkuppel: **0 Treffer auf allen fünf Plänen und im ganzen Repo**. Roh-Ist über 265 Räume: 141 True / 94 False / 30 None — **davon ist das False auf Barawitzka unzulässig** (13 Räume): bei einem Plan ohne Fensterobjekte ist „kein Beleg" fehlende Erhebung, kein belegtes Fehlen; nach der vorgeschlagenen Regel müssen dort **alle 50 Räume `None`** sein. Genau deshalb der zweite Feldvorschlag: **`belichtung_vollstaendigkeit`** je Plan — Muthgasse VOLLSTAENDIG (zwei unabhängige Kanäle), Mollgasse/Rennweg TEILWEISE, **Barawitzka UNGEPRUEFT**. Dazu `belichtung_quelle` (FENSTER > OBERLICHT > GLASWAND > TUER_AUSSEN, `GEOMETRIE_UMSCHLOSSEN` nur zu False, `KEINE` nur zu None). **Vier Fragen an dich** (§ 8.1 im Bericht): genügt eine ALU_GLAS-Wand ohne erkannte Öffnung für „natürlich belichtet" i. S. v. AStV § 9 Abs. 1 Z 1, oder verlangt Z 1 eine Belichtungsfläche mit Außenbezug? Zählt eine **Balkontür** als Belichtung oder nur als Weg? Darf ein Raum in einem nur TEILWEISE erhobenen Plan True tragen? Und: § 9.4 knüpft Z 1 an Raum **und** Fluchtweg-Abschnitt — Feld nur an `Raum` oder auch an `FluchtwegSegment`? **Zweiter Eingang, den wir gar nicht haben:** die **Arbeitsraum-Eigenschaft** (AStV § 1 Abs. 4) fehlt vollständig, `nutzungsklasse.py:12-42` kennt keinen Arbeitsraum-Typ — ohne sie bleibt Z 1 auch mit dem Belichtungsfeld `ungeprueft`. **(4) Fluchtwegbreite — als einziger der drei Punkte umgesetzt.** `raumerkennung/breitenprofil.py` misst jetzt entlang der Mittelachse alle 100 mm senkrecht zwischen den Wandkanten; Abschnitte konstanter Breite (Toleranz 100 mm, Mindestlänge 500 mm), **Engstellen getrennt geführt** (reduzieren nie die Abschnittsbreite), **Türdurchgänge als eigener Punkt**, fehlende Messung = `None` **mit Grund**. Deine drei Breitenbegriffe sind der Bauplan dieses Moduls: **es setzt an keiner Stelle einen Normwert ein, auch nicht als Fallback.** Drei Messfehler gefunden und behoben, u. a. eine schiefe Normale (lokale Segment-Tangente, ±20 mm Skelett-Wackeln kippen sie um 11,3° → 1200 mm wurden als 1223,8 mm gemessen; jetzt Sehne über dasselbe Fenster, Restfehler 1200,8 mm). Ist über 307 Fluchtwegsegmente: **209 messbar (68,1 %)**, 98 nicht — Gründe: 61× kein erkanntes Raumpolygon schneidet die Achse (**Erkennungslücke, meine Lane**), 37× jeder Abtastpunkt ist Türdurchgang oder Eckfenster (bei GRAPH-Segmenten diagonal Tür-zu-Tür fachlich korrekt „keine Gangbreite"). `achse_nicht_ermittelbar` und `wandkante_fehlt` kamen auf **keinem** der fünf Pläne vor. Zwei volle Belegprofile liegen bei (Barawitzka `seg_graph_durchgang_11`: 20,2 m, 204 Messpunkte, vier Abschnitte 1420/1210/1505/1755 mm, zwei Engstellen, vier Türpunkte · Mollgasse `seg_graph_durchgang_17`: 26,8 m, 269 Punkte). **An dich (§ 8.1.5–7):** dein Proposal lässt zwei Formen zu (Abschnitte **oder** Profil) — welche der drei Größen landet in welcher Form im Contract? Ein einzelnes `breite_mm` am Segment kann das Profil nicht tragen. Dein `WEGBREITE_RANDSTREIFEN.md` nennt `raum_modell` 1.1.0 → 1.2.0, Ist ist **1.3.0**, additiv wäre 1.3.0 → **1.4.0**. Und: welche Werte erwartet RL-4-Tabelle 1 für Gangart/Treppenart (Hauptgang/Nebengang, Haupt-/Wohnungs-/Nebentreppe), damit unsere Typisierung darauf mappen kann? **(5) `Tuer.breite_mm` — deine drei Fragen, mit Zahlen über 629 Türen aus fünf Plänen.** *Herkunft nachvollziehbar?* **Nein.** `Tuer.quelle` trägt die **Erkennungs**-Quelle, nicht die Breitenherkunft; `quelle="block"` steht sowohl für Nennmaß aus dem Namen als auch für ARC-Radius (das `or` in `tueren.py:141`), und `tuer_typisierung.py:191/:241` überschreiben das Feld zu einem zusammengesetzten Audit-String. Gemessen sind es **vier** Herkünfte plus „keine Messung": GEOMETRIE_OEFFNUNG **360 (57 %)**, GEOMETRIE_SCHWENKRADIUS **77 (12 %)**, BLOCKNAME **58 (9 %)**, GEOMETRIE_SUMME (Doppelflügel) 2, **keine Messung `0.0` 132 (21 %)**. **Dein `tuerbreite_herkunft` nennt drei — die Doppelflügel-Summe fehlt; Achtung, `tests/normwissen/test_quellenblock_e07_rl4.py:301` pinnt `len(befunde) == 3`, ein Nachtrag macht diesen Test rot.** *Durchgangslichte bekannt?* **Nein** — und die Mehrheit liegt in der Richtung, vor der du warnst: GEOMETRIE_OEFFNUNG ist die Rohbauöffnung **ohne Zarge**, systematisch größer als die Lichte. Zargen-/Anschlagmaße stehen in keinem der Pläne (Mollgasse hat Blocknamen wie `TÜR-BLOCKZARGE-90_STUMPF`, aber **kein einziges ATTRIB** an 44 INSERTs). **Eine Einschränkung, die ich als strittig kennzeichne:** 18 Muthgasse-Blocknamen tragen **DL-Notation** (`…_1DF_90x200`, `… - DL - 800 x 2490`), und `_breite_mm()` zieht die 900/950 genau daraus. Gilt die Lesart DL = Durchgangslichte, ist `breite_mm` dort tatsächlich die Lichte und meine Aussage „in keinem der 629 Fälle" zu eng. **Das ist eine Frage an dich, keine Feststellung.** Vorschlag: `breite_mm` bleibt gemessenes Maß **mit** `breite_quelle`, zusätzlich `lichte_mm: int | None` — `None`, solange nicht belegt; bei `GEOMETRIE_OEFFNUNG` bleibt es `None`, es gibt keine belegte Umrechnung. *0.0 → None:* Migration in vier Schritten vorgeschlagen, probeweise gemessen (Baum danach vollständig zurückgebaut, MD5 identisch, `git diff` leer): **3 zusätzliche Fehler** — 2× `TypeError: '>' not supported between 'NoneType' and 'float'` @ `tuer_typisierung.py:142`, 1× Schema-Drift. **Kein einziger Test assertiert `breite_mm == 0.0` als Bedeutung.** Einschränkung ausdrücklich: der Probelauf umfasste 319 von 1196 Tests, `tests/platzierung`, `tests/hauptengine`, `tests/render` und der Großteil von `tests/normwissen` sind **nicht** erhoben. **Keine Türbreiten-Prüfregel abgeleitet — das ist deine Lane.** Zwei Punkte noch: RL 4 Punkt 2.8.1 („Abstand ≤ 20 cm = eine Tür") kollidiert konzeptionell mit unserem `verschmelze_doppelfluegel` (`_DOPPEL_TOL_MM = 300`) — wer besitzt die Zusammenfassung, Erkennung oder Prüfung? Und `ArbeitsstaettenWissen` hat weiterhin **null Aufrufer**; nach deinem ASTV § 6.4 gehört der in `hauptengine/validierung.py` — Hauptengine, 3-Owner, nicht einseitig. **(6) @mvpo3, zwei Zusagen, ohne die die Contract-Felder tote Felder wären:** (a) **Verbraucher der Wegbreite** — `deckung.py::verdichte_fluchtweg` ersetzt die Breite heute durch `min(bounds[2]-bounds[0], bounds[3]-bounds[1])`. Ohne Zusage, das gegen einen echten Breiten-Nachweis zu tauschen, bekommt ein Contract-Feld denselben Status wie `RaumModell.anker`: erzeugt, geprüft, nie gelesen. (b) **Detailtiefe** — brauchst du das volle 100-mm-Profil, oder reichen `abschnitte` + `engstellen` + `breite_min_mm`? Davon hängt ab, ob `breitenprofil` überhaupt in den Contract muss. Dazu: bestätige bitte, dass die drei Belichtungsfelder die Platzierung **nicht** verändern (der Nutzungsklassen-Filter sitzt nur in `flaechen_strategy.py:155-167`, ein neues Raumfeld darf dort keinen zweiten stillen Pfad öffnen) und dass für `breite_mm = None` keine neue Nutzung in `platzierung/` geplant ist. **Deine Naht an mich (Spikey-Polygone Mollgasse raum_41/55) bleibt meine** — und hängt direkt hier dran: bei einer Mittellinie in Zacken wäre auch jedes Breitenprofil dort unbrauchbar. **(7) Zwei Befunde aus meiner Lane, zur Kenntnis, nicht gefixt:** `dxf_renderer.py:524` **erfindet still 900 mm Türbreite** (`breite_mm or 900.0`) — heute unsichtbar, gehört mindestens in den Prüfbericht. Und die **83 Muthgasse-Beschriftungsfahnen sind keine Türen** und blähen `RaumModell.tueren` um ~27 % auf (308 statt ~225) — eigenständiger Erkennungs-Bug, meine Lane, unabhängig von der Breitenfrage. **(8) Was NICHT passiert ist, ausdrücklich:** kein Push, kein PR, kein Merge, keine Nachricht nach außen. `hauptengine/contracts/**` ist **unberührt** — alle Datenmodellangaben oben sind **VORSCHLAG** und additiv mit Default, und jede ist **optional** in dem Sinn, dass ohne sie die betroffene Prüfung schlicht bei `ungeprueft` / `nicht messbar` bleibt statt falsch zu werden. Bestehende Erzeuger brechen nicht.

- 2026-09-09 **Selman → @mvpo3 (Render-Speicher) + @EnisAMG (LB-Zeile, GESCHÄFTSLOKAL) — Antworten, Branch `selman/extents-ausreisser`, Suite 1096 passed / 0 failed nach Merge von origin/main.** **(1) @mvpo3, Render-Speicher — Stand nach deinem Paket:** der volle `plan_pruefen`-Lauf über alle fünf Pläne läuft mit dem aktuellen main **durch, ohne Abbruch** (Barawitzka 263 s · Mollgasse 292 s · Muthgasse E2 1838 s · Rennweg EG 56 s · Rennweg OG3 52 s). Mein Befund vom 2026-09-08 bleibt aber offen, weil er nicht adressiert wurde: `_figur` (`scripts/plan_pruefen.py:147`) ruft `Frontend(...).draw_layout` über den **kompletten** Modelspace und wird **8× je Plan** gerufen (Zeilen 416, 488, 658, 710, 832, 1305, 1314, 1338); der Working Set wächst dabei monoton 0,24 → 3,86 GB ohne Plateau. Muthgasse E2 mit 1838 s ist der sichtbare Rest davon — 84 % der Gesamtlaufzeit der Prüfstrecke aus einem Plan. Zwei Hebel, beide in deiner Lane: den Render **einmal** bauen und als Raster wiederverwenden, oder `draw_layout` auf Varianten-Bounds/Layer beschränken. Ich fasse `hauptengine/render/**` nicht an; sag Bescheid, wenn du die Messung als Skript willst. **(2) @mvpo3, deine offene Naht an mich (Spikey-Polygone):** angenommen — Mollgasse `raum_41`/`raum_55` als 494-Punkt-Polygone sind Erkennungsseite, also meine. Steht als nächster Punkt in `Handoff/SELMAN.md`, noch nicht angefasst. **(3) @EnisAMG, die eine LB-Zeile:** in `normwissen/data/lb_extraktion.yaml` (`unterstuetzte_raum_typen`) steht jetzt `KINDERWAGENRAUM` — **eine Zeile, kein Parser-Verhalten.** Grund war nicht Übergriff, sondern `tests/contract/test_lb_raumtyp_naht.py`: der Guard prüft beide Richtungen hart, ohne die Zeile ist jeder Merge mit dem neuen Kanon-Typ rot. Gleiches gilt für den Eintrag in `normwissen/data/regel_deckung.yaml` — ich habe ihn als `offen:` mit `owner: Enis` angelegt, nicht als Regel entschieden. **Beides ist zur Abnahme durch dich markiert; wenn du eine andere Einordnung willst, ändere sie, ich ziehe nach.** **(4) @EnisAMG @mvpo3, GESCHÄFTSLOKAL — die Frage steht seit 2026-09-08 unbeantwortet und blockiert.** Datenlage unverändert: 4 Vorkommen auf 2 Plänen, echte Kanon-Lücke (keine Schreibvariante). Eine Verkaufsstätte (OIB RL2 Tab. 6) passt in keines der fünf `Nutzungsklasse`-Literale. **Ich leite den Wert nicht selbst ab** — Normzuordnung ist Enis' Lane. Was ich brauche, ist genau eine Entscheidung: Typ `GESCHAEFTSLOKAL` ja/nein, und welche Nutzungsklasse er trägt. Danach ziehe ich VOKABULAR + `raumtyp.py` + LB-Stützliste + `regel_deckung.yaml` in einem Zug nach. Bis dahin bleiben die Räume untypisiert und bekommen keine Leuchte. **(5) Zur Kenntnis, Befund aus dem heutigen Lauf:** `WOHNUNG_PRIVAT` bekommt weiterhin Leuchten (Mollgasse 11, Muthgasse 29). Gemessen: der Nutzungsklassen-Filter existiert **nur** in `platzierung/flaechen_strategy.py:155-167`; RZ-Anker, Gang-RZ, Sichtlinien-Garantie, Deckungs-Verdichtung, Sonderstellen, Flag-Räume und die Fachpraxis-Aufheller laufen ungefiltert durch `platzierer.place()`. @mvpo3, das ist deine Lane — der billigste Fix ist ein zentraler Nachpass am Ende von `place()`, das Muster liegt in `verbotszonen_nachpass.py` fertig vor (WOHNUNG_PRIVAT-Polygone als zweite Zonenquelle durchreichen). Ich habe nichts geändert.

- 2026-09-09 Leonis (Auslieferung Layout+1:50, Owner-Auftrag): **das gelieferte
  Geschoss-DXF ist ab jetzt das Layout-Blatt** (Vorlage in Layout1 + Viewport exakt
  1:50, in AutoCAD plot-fertig) statt des Modelspace-Blatts (#115). Pipeline defaultet
  `template_path` auf die Repo-Vorlage; passt der Plan in 1:50 nicht in den Viewport
  (G6), Fallback auf Modelspace-Blatt (`layout_fallback` im Summary). **Naht-Folge für
  @EnisAMG:** ezdxf rastert Paperspace-Viewports nicht → die A0-PDF wird aus einer
  internen Modelspace-Quelle (`<floor>.modelspace.dxf`, `pdf_quelle=True`, danach
  entfernt) gerendert. Der dynamische **Prüfvermerk/OIB-Stufe (Regel 13)** wird damit
  auf dieser PDF-Quelle + im `render_summary["pruefung"]` geführt, NICHT mehr im
  Modelspace-TEXT des gelieferten (Layout-)DXF; `pruefvermerk_am_blatt` beschreibt jetzt
  das gelieferte Layout-Blatt (statische Vorlage → False). `pruefung`-Dict inkl.
  `oib_stufen` unverändert im Summary/API. Tests `test_pruefvermerk_oib` lesen die
  PDF-Quelle. Falls die On-Blatt-Sichtbarkeit am Layout gewünscht ist → Owner-Entscheid
  (Prüfvermerk in Layout1-Paperspace zeichnen).
- 2026-09-09 Leonis (Wissens-Kreuzungen, Owner-Auftrag): zwei rote Kreuzungen aus dem
  Wissens-Inventar sind **norm-blockiert, nicht code-blockiert** — bewusst NICHT
  „force-gebaut" (fabrizieren = OVE/EN-Hard-Stop, CLAUDE.md):
  - **Flächen-Trigger (60 m² / 8 m²).** `platzierung/oib_gate.py` `sanitaer_scope`/
    `verkehr_scope` geben per Design nie `anwendbar` → `flaechen_schwellen` zu füllen
    bewirkt NICHTS (Gate bleibt fail-closed zu). `anwendbar` braucht **(a) 3-Owner-
    Beschluss** „Tabelle-6-Erforderlichkeit = erhöhte Anforderungen nach Art der
    Nutzung" (R-12-2-Beleg vorhanden, aber die Gleichsetzung ist Auslegung) **+ (b) Selman:
    `nutzungskategorie` im RaumModell** (für Punkt 3, verkehrstechnische Einrichtungen).
    Der Code ist bereits korrekt (ungeklaert → sichtbar Regel 13). @EnisAMG @polatselman.
  - **Blendungs-Imax je Montagehöhe (EN 1838 §4.4, 500…5000 cd).** Glare-Tabelle liegt in
    `normwissen/_port_source/emergency_lighting_en1838.yaml:58-73`, bewusst nicht in
    `en1838_grundwerte.yaml`. Ein echter Gate braucht **Enis' autoritative Glare-Kurve** +
    **Photometrie je Platzierung in `validierung`** (heute nur aggregierter
    `PhotometrieBefund`, kein `i_cd_fn` je Leuchte) + Entscheidung, ob als
    `NormRegelwerk`-Feld (Contract-Freeze) oder Provider-Methode. @EnisAMG.
  - **Gebaut wurde** dagegen der sauber Leonis/hauptengine-lokale Teil: die 20-Leuchten-
    Regel (`pipeline._coverage`) trägt jetzt zusätzlich die Kreis-Redundanz (EN 50172,
    aus vorhandener `circuit_hint`-Zuordnung) + Bemessungsstrom ≤60 %/Kreis als offenen
    Punkt (nicht-blockierender Hinweis, kein fabrizierter Beleg).
- 2026-09-09 Leonis (Doku-Hygiene): **R-12-2-Beleg liegt vor und ist aktiv.** R 12-2/AC:2019-07-01 wurde am 2026-09-05 beschafft, #116 (Verkaufsstätten-Vorprüfung, Regel 14) ist gemergt, autoritativ aktiv in `normwissen/data/ove_e8101_zusatz.yaml` (`r12_2`). Frühere Notiz „R 12-2 verworfen" war eine Verwechslung mit der verworfenen Tür-RZ-links-Regel (G3), NICHT mit R 12-2. Statuszeile in `docs/NORMQUELLEN_AT.md` §2d ergänzt. (Kein Code/YAML/Test berührt.)


- 2026-09-08 **Selman → @mvpo3 @EnisAMG: Sync gemacht (35 Commits vor main), vier Fixes in der Raumerkennung, ein Befund für Leonis, eine Frage an alle drei. Branch `selman/extents-ausreisser`, KEIN Contract.** **(1) Kinderwagenraum erledigt — @mvpo3, aber dein Befund griff zu kurz:** dein Punkt („`raumtyp.py` ebnet `kinderwagen` → ABSTELLRAUM ein") war richtig, der Fix allein hätte aber NICHTS geändert — **kein einziger realer Plan schreibt „Kinderwagen"**. Gemessen steht dort `KIWA` (Mollgasse), `FAHRRADRAUM / KIWA` (Mollgasse) und `Fahrrad+ KiWa` (Barawitzka). Zusätzlich hätte das generische `fahrrad`-Token die Mischraum-Stempel abgefangen, weil `_EXTRA_LABELS` vor `_EXTRA_DIRECT` geprüft wird. Deshalb: eigener Kanon-Typ **plus** `kiwa` im Vokabular **plus** Einordnung in `_EXTRA_OVERRIDE`, damit der spezifische Typ den Mischraum gewinnt. **Wirkung, gemessen: Mollgasse EG 2 KIWA-Räume → RZ 26 → 28, Barawitzka 1 → RZ 2 → 3** — deine Türleuchten-Regel feuert jetzt auf echten Plänen. **@EnisAMG: ich habe `KINDERWAGENRAUM` in deine `normwissen/data/lb_extraktion.yaml` (`unterstuetzte_raum_typen`) eingetragen** — nicht aus Übergriff, sondern weil `tests/contract/test_lb_raumtyp_naht.py` beide Richtungen hart guardet und der Merge sonst rot ist. Eine Zeile, kein Parser-Verhalten; sag Bescheid, wenn du sie anders willst. **(2) Baufeld E2 entsperrt:** die 8 Geschosse liefen nicht in einen Crash, sondern in einen quadratischen Rasterungs-Hotspot — `stempel_flutung._fuelle` nutzte `skimage.draw.polygon` (Kosten O(BBox-Pixel × Stützpunkte)), und die Wand-Union hat dort Ringe mit 21 463 Stützpunkten: **166 s für EINEN Ring**, aus drei Aufrufstellen. Jetzt Pillow-Scanline: **Kaskade 41 s statt >13 min, 0,47 GB, 237 Räume.** **(3) Stempel-Typ auf L-/H-Räume zurückgeschrieben:** der erkannte Typ blieb im Stempel stecken, weil `raeume_aus_layer/-hatch` nur den Layernamen kennen. **Muthgasse E2: Türen typisiert 27/311 → 219/307**, `final_exit` 9 → 1 (die weggefallenen waren 6 Balkontüren + 1 Öffnungs-Duplikat + 1 Text-Fallback-Falschpositiv; 6 davon standen schon vorher auf der „unbenutzt"-Liste des eigenen Kreuzchecks). Das xfail-Zielbild „≥ 90 typisierte Räume" in `tests/naht/test_soll_muthgasse.py` ist damit erreicht, Marker entfernt. **(4) @mvpo3 — Befund in DEINER Lane, nicht gefixt:** der volle `plan_pruefen`-Lauf auf einem Baufeld-Geschoss treibt den Working Set auf ~13 GB und wurde vom System abgebrochen. Gemessen ist die Ursache NICHT die Raumerkennung, sondern `_figur` (`scripts/plan_pruefen.py:147`): `Frontend(...).draw_layout` über den kompletten Modelspace wächst monoton 0,24 → 3,86 GB ohne Plateau, **und `_figur` wird 8× je Plan gerufen** (Zeilen 416, 488, 658, 710, 832, 1305, 1314, 1338). Hebel: den Render einmal bauen und als Raster wiederverwenden, oder `draw_layout` auf Varianten-Bounds/Layer beschränken. **(5) Vokabular-Sweep, Ergebnis bewusst NEGATIV:** 892 Stempel aus 11 Plänen + Textdump über alle 60 Projekt-DXF. Trefferquote `raumtyp_flags` **87 %** (217/249), alle Fehlschläge in den beiden EG-Plänen, 18 davon korrekt untypisiert (Außenanlagen, „TOP n", Plankopf). Sechs naheliegende Alias-Kandidaten (`vp`, `tr`, `er`, `podest`, `aufzug`, `wr`) habe ich **verworfen** — jeder erzeugt einen belegten Fehltreffer, Details in `docs/OFFENE_FRAGEN.md`. Beispiel: `tr` → STIEGENHAUS wären 37 Phantom-Fluchtweg-Anker aus privaten Reihenhaus-Innentreppen. **(6) FRAGE an euch beide:** `GESCHÄFTSLOKAL` (4 Vorkommen auf 2 Plänen) ist eine echte Kanon-Lücke, keine Schreibweise — eine Verkaufsstätte (OIB RL2 Tab. 6) passt in keine der fünf `Nutzungsklasse`-Literale. Brauchen wir den Typ, und welche Nutzungsklasse trägt er? Das wäre VOKABULAR + `raumtyp.py` + LB-Stützliste + Nutzungsklasse in einem Zug. **(7) @mvpo3 zur plan-verify-Skill:** `dxf_healthcheck.py` läuft (Rennweg OG3 → „GO"), `plan_verify_baender.yaml` fehlt noch — bis dahin fällt Schritt „Symbol-Zahlen im Band" aus. **Suite nach dem Merge: 1089 passed, 0 failed.**
- 2026-09-08 **Selman → @mvpo3: Baufeld-Crash gefixt, aber die Ursache ist eine andere als vermutet — Branch `selman/extents-ausreisser`, kein Contract.** Danke für die Meldung; deine Repro war exakt reproduzierbar. **Deine Cluster-Hypothese trägt aber nicht:** die Wandkörper des 4OG liegen NICHT in zwei kompakten Haufen, sondern durchgehend über 72–85 km verteilt — die größte Lücke in x (1065 m) teilt 1950/185 Körper, und die 1950 spannen danach immer noch 72 460 m. Ein Ausreißer-Filter hätte dort nichts gerettet. **Echte Ursache, eine Stufe früher, in meiner Lane:** `dxf_load._calibrate_factor` leitet den mm-Faktor aus `_raw_wall_span` ab, und die sah nur Modelspace-Entities auf Wand-Layern. Bei Baufeld 1OG/2OG/4OG liegen dort NUR INSERTs (die Wand-Linien stecken in der Blockdefinition) → 0 Punkte → keine Dekade plausibel → Fallback auf `$INSUNITS`, das 6 (Meter) meldet, obwohl die Zeichnung in mm vorliegt → **Faktor 1000**. Der 2,56-TiB-Raster ist die FOLGE, nicht die Ursache. **Betroffen waren 5 von 8 Geschossen, nicht eines:** 1OG/2OG/4OG stürzten ab (Faktor 1000), **3OG und 6OG rechneten still mit Faktor 10 weiter** — also 10× falscher Maßstab ohne jede Fehlermeldung, das ist der unangenehmere Fall. **Fix:** (1) `_raw_wall_span` steigt in Blöcke ab und misst über ein 2–98-%-Perzentil-Fenster statt min/max (fängt zugleich die Phantom-Klasse aus #126 ab); (2) `flute_stempel` bekommt eine Reißleine `_MAX_RASTER_ZELLEN = 5e8` (~477 MiB) → `RuntimeWarning` + keine gefluteten Räume statt Absturz des ganzen Parse (Muthgasse E2, größter Plan im Repo, braucht 4.6e7 Zellen). **Ergebnis:** alle 8 Baufeld-Geschosse parsen, Faktor 1 überall, 215–301 Räume und 493–678 Türen je Geschoss. **Bestandspläne nachweislich unberührt** (vor der Umsetzung gemessen): Barawitzka 1000, Mollgasse 1000, Muthgasse 10, Rennweg EG/OG3 je 1 — alle Faktoren identisch, volle Suite unverändert grün. **An dich zurück:** deine Ops-Notiz (Baufeld-Geschosse nicht parallel rendern) bleibt gültig, daran ändert der Fix nichts. Und: die Baufeld-Pläne liegen nur als Zip im Repo, `test_soll_baufeld.py` skippt weiter — wenn du die Familie dauerhaft in der Prüfstrecke willst, müssen die Geschosse nach `Projekte/_eingang/`; meine Messungen liefen auf einer temporären Entpackung.
- 2026-09-08 Leonis/**F2** (`leonis/f2-work`): **Deckungslücken-Fix** (vom Auto-Bericht aufgedeckt: Mollgasse EG-Korridore mit 0,00 lx). Root-Cause: `abstand_nachpass.entzerre` mergte die **SL-Dublette (< 2 m) über Raumgrenzen** — die einzige Fluchtweg-SL eines Korridors verschwand mit einer SL im NACHBAR-Korridor, Überlebende lag außerhalb → Korridor dunkel. Fix: die große SL-Dubletten-Schwelle (2 m) greift jetzt **nur im GLEICHEN Raum** (`_selber_raum`); über Raumgrenzen gilt der 250-mm-Mindestabstand → beide bleiben. Ergebnis Mollgasse EG: **kein Korridor mehr ohne SL** (raum_34/56 lit), Nachweis 9/12→10/12, SL 31→34. Owner-Regel „2 SL < 2 m = eine zu viel" bleibt für den GLEICHEN Raum erhalten (Sonderstellen+Verdichtung). 2 neue Unit-Tests, 253 platzierung + 24 e2e grün (Bänder halten), ruff clean. Kein Contract-Touch. Die 2 Rest-Fails auf Mollgasse = 494-Punkt-Spikey-Polygone (Selman-Erkennung, separate Sache).
- 2026-09-08 Leonis/**F2** (`leonis/f2-work`): **Auto-Nachweis-Bericht je Plan** (Owner-Wunsch „Hauptengine soll das immer erstellen"). Neu `hauptengine/render/lux_nachweis_bericht.py::schreibe_bericht` — konsumiert das FERTIGE `PlatzierungsErgebnis` (platziert nichts neu), rechnet das Falschfarben-Lux-Feld + EN-1838-Nachweis aus den gesetzten Leuchten, DIALux-artige A4-Seite (Rivoplan-gebrandet, polare LVK, robust bei vielen Fluchtwegen: „N/M erfüllt" + offene zuerst + Rest-Summe). **F1-Naht (additiv, minimal):** `pipeline._run_mit_quelle` schreibt nach `render_dxf` eine `<out>.nachweis.png` und legt den Pfad in `render_summary["lux_nachweis"]` (try/except → nie plan-brechend, lazy matplotlib); `projekt._merge_pdf` hängt die Seite je Geschoss ins Sammel-PDF (PIL PNG→PDF). Keine Signatur-/Contract-Änderung. Getestet end-to-end (Mollgasse EG: 9/12 Rettungswege erfüllt, deckt reale Deckungslücken auf); 405 contract/hauptengine/platzierung + 24 e2e grün. Bitte, F1: die 2 additiven Stellen in pipeline/projekt beim Rebase mitnehmen (reine Zusatz-Zeilen am Ende der jeweiligen Blöcke).
- 2026-09-08 Leonis/**F2** (`leonis/f2-work`): **Aufheller jetzt lux-bedingt** (Owner-Wunsch „Lichtberechnung entscheidet, ob Aufheller nötig"). `fachpraxis.aufheller_je_rz` bekommt `norm` + `i_cd_fn` und setzt den RZ-Aufheller nur noch, wenn der Kandidatenpunkt aus den schon platzierten Sicherheits-/Antipanikleuchten UNTER `anf.min_lux` liegt (via `lux_punkte`) — **nur mit echter Hersteller-Photometrie** (ohne LDT bleibt es bedingungslos, da die konstante Lichtstärke-Annahme die Deckung überschätzt). Aufrufstelle `platzierer.py` gibt `norm`+`kontext.i_cd_fn` mit. Antipanik/Fluchtweg waren schon lux-getrieben. **Gate im Default-Bundle aktiv, aber alle Tests grün** (Wohnbau EG 4→3 Aufheller, E2E-Bänder halten — reale Corridor-LVK gated nur echte Deckung). Kein Contract-Touch, kein neues Norm-Feld (`min_lux` existiert). 5 neue Unit-Tests. Als Nächstes Teil 2 (Auto-Nachweis-Bericht je Plan, Pipeline-/Render-Naht — F1 bitte kurz halten).
- 2026-09-08 Leonis/**F2** (Worktree `Notbeleuchtung-F2`, Branch `leonis/f2-work` @ origin/main): Studie dreier geprüfter Profi-Lichtberechnungen (Aichholzgasse/Schrack · Barawitzka/din · **Linke Wienzeile/din, geprüft**) → alle rechnen **MF 0,80 · ohne Reflexion · Mittellinie ≥1 lx + Mittelfläche ≥0,5 lx · Ud≥1:40**. Neues **isoliertes** Modul `platzierung/lux_nachweis.py` (`nachweis_fluchtweg`/`nachweis_summary`) erzeugt genau diesen EN-1838-Nachweis **render-frei aus dem fertigen PlatzierungsErgebnis** — reine Konsumption, ändert KEINE Platzierung (bewusst F1-kollisionsfrei, kein Touch an pipeline/deckung/render). 239 platzierung-Tests grün, ruff clean. **NAHT an F1/Hauptengine (offen, additiv):** `pipeline.run` möge `nachweis_fluchtweg(...)` aufrufen + `nachweis_summary` unter `render_summary["lux_nachweis"]` hängen (analog `["pruefung"]`/`["oib"]`) → Render kann eine Nachweistabelle ins Blatt setzen. Bitte abstimmen wer/wann die eine Pipeline-Zeile setzt (F1-Lane). Details: `knowledge/extracted/LICHTBERECHNUNG_REFERENZ.md` §7.
- 2026-09-08 Leonis (Lichtberechnungs-Analyse + Wartungsfaktor-Slice, Branch `leonis/lichtberechnung-wartungsfaktor`): Erste echte Profi-Notberechnungen analysiert (`Lichtberechnung/*.pdf` — Relux/Schrack WHA Aichholzgasse 35 + DIALux/din Barawitzka) → `knowledge/extracted/LICHTBERECHNUNG_REFERENZ.md`. Verifiziert: **Wartungsfaktor 0,80 innen / 0,57 außen**, Reflexion=0 (=Engine-Annahme, validiert), Emin-1,0-lx-Auslegung, Corridor-Optik längs Gangachse (#119 bestätigt). **@polatselman:** `Aichholzgasse/`-PDFs = Architektur-Polierpläne (kein Notplan), Kandidat neue Architektur-Testfamilie GK5 2UG..2DG (Details docs/OFFENE_FRAGEN.md). **@EnisAMG:** MF ist Norm-Wert — bitte Feld `wartungsfaktor` auf `NormAnforderung` + Werte in `en1838_grundwerte.yaml` (3-Owner). Leonis baut den Mechanismus in `lux.py`/`deckung.py` **inert** (Default 1,0, defensiv `getattr(anf,'wartungsfaktor',None) or 1.0`) → 0 Test-Bruch; Aktivierung kippt E2E-Bänder, erst mit Owner-GO.
- 2026-09-07 Leonis (Ausgabelücken-Runde, Basis: Enis' lokaler Audit 1624474): Befunde 1+2 in Leonis-Lane GEFIXT (Branch `leonis/ausgabeluecken-oib-astv`): ① `gate_summary` übernimmt jetzt die Provider-Hinweise je `OibErgebnis` (u.a. AStV-Parallelpfad) mit `[gebaeudeteil]`-Präfix — vorher fielen sie weg und erreichten die Ausgabe nie; ② `pruefbericht` trägt additiv `oib_stufen` und `_blatt_pruefvermerk` zeichnet die Stufe AM BLATT (Zeile zwischen Status und Details-Verweis). Getestet bis zur gelieferten Ausgabe (DXF-Text-Lesen + API-Header). **Befund 3 OFFEN — @polatselman zur Abstimmung:** der Vermerk verweist auf „Prüfbericht im Plan-Summary (API)", aber es gibt keine Route, die den vollen Bericht eines erzeugten Plans nachträglich abruft (Summary kommt nur als X-Notbeleuchtung-Header im POST-Response). Vorschlag zur Diskussion: `GET /plan/{id}/pruefbericht` oder Bericht als JSON-Beilage im DXF-Download-Multipart — Naht API/Hauptengine, gemeinsame Entscheidung.
- 2026-09-07 Leonis → **@polatselman: Kinderwagenraum-Typ geht in der Erkennung verloren.** Neue Owner-Regel (Branch `leonis/tuerleuchte-technik-muell`): TECHNIK/MUELLRAUM/**KINDERWAGENRAUM** bekommen eine Antipanik-Sicherheitsleuchte an der Tür (Referenz-Praxis). Problem: `raumerkennung/raumtyp.py:87-88` mappt `kinderwagen`/`kinderwagenraum` → `RoomType.STORAGE` → Typ-String **`ABSTELLRAUM`** — zusammen mit Fahrrad-/Abstellraum. Damit trägt ein Kinderwagenraum auf ECHTEN Plänen `ABSTELLRAUM`, und meine Regel (die bewusst NUR den gemeinsamen Kinderwagenraum will, NICHT den privaten Wohnungs-Abstellraum) greift dort nicht. **Bitte:** einen eigenen kanonischen Typ `KINDERWAGENRAUM` erhalten (statt nach ABSTELLRAUM einzuebnen) — analog wie TECHNIK/MUELLRAUM eigene Typen sind. Bis dahin feuert die Regel nur, wo der Plan den Literal `KINDERWAGENRAUM` führt (Wohnbau-Demo-Fixture). Ggf. Vokabular-Ergänzung `KINDERWAGENRAUM` in docs/VOKABULAR.md (3-Owner) nötig.
- 2026-09-07 Leonis → **@polatselman: Baufeld-4OG crasht die Stempel-Flutung mit 2,56-TiB-Raster** (`stempel_flutung.py:227 np.zeros((1718164,1638329))`). Ursache vermessen: der 4OG-DXF trägt seinen Hauptinhalt bei **y ≈ 3,475e11 mm (347.535 km!)**, ~3.000 Entities (Layer `590 Brandschutz…`, `956 Durchbruch Bemaßung`) sitzen nahe dem Ursprung → zwei Cluster, Extents-Spannweite 347.535 km. Gleiche Ausreißer-Klasse wie der Rennweg-Phantom-Raum (#126); die Flutung braucht Ausreißer-robuste Extents (Cluster um den Hauptinhalt, wie `_geschoss_extents` im Render). Repro: `ArchitekturRaumProvider().parse("Projekte/Baufeld E2/Elektromontageplan_4OG.dxf", "4OG")`. — Außerdem: deine Fachteile 1–3 tragen am 5OG voll (689/689 Türen mit von/nach_raum, 262 Segmente, 12 RZ rotationsrichtig, Prüfstatus ok — Owner-Reklamation „18 RZ stur 0°" damit behoben); die Baufeld-Soll-xfails kippten lokal als XPASS(strict) und sind in PR #128 entstrickt. Ops-Notiz: Baufeld-Geschosse NICHT parallel rendern (drei Prozesse → MemoryError bei 280 MiB), sequenziell läuft es.
- 2026-09-07 **Selman → @mvpo3 @EnisAMG: Branch `selman/grundstuecksgrenze-hof` (3 Commits, stapelt auf `selman/fluchtweg-spec-abgleich`) — KEIN Contract, kein neues Approval nötig.** Owner-Entscheidung Selman: jedes Projekt hat einen **Flächengrundriss bis zur Grundstücksgrenze** (inkl. Garten, Innenhof, Vorplatz); „ins Freie" heißt aus diesem Grundriss HERAUS auf öffentlichen Grund. Ein Innenhof, der zwei Stiegenhäuser verbindet, aber ringsum ummauert ist, ist **kein final_exit** — der Fluchtweg muss weitergehen. **@EnisAMG: damit ist meine Frage aus dem vorigen Eintrag (gilt ein geschlossener Hof als gesicherter Bereich?) vom Owner beantwortet — nein.** Die zweite Frage (AUSSEN_GESCHLOSSEN als Contract-Wert) bleibt offen und ist weiter deine. **Belegte Fehlbefunde, behoben:** `erkenne_aussenbereiche` nahm die konvexe Hülle der GEBÄUDE-Komponenten als äußere Grenze. Barawitzka: die Freiflächen 89.7 + 76.1 m² hängen über 0.05 m zusammen = ein Hof von 190.2 m², der beide Stiegenhäuser verbindet (Abstand je 0.00 m zu raum_35/raum_37) und **0.0 m Randlänge an der Straßenkante** hat — er galt als offen, `exit_durchgang_62` wurde final_exit. Mollgasse: die Hülle (1804.5 m²) ist GRÖSSER als das Grundstück (1510.2 m²) und ragt mit 405.3 m² auf öffentlichen Grund. **Neu:** `grundstuecksgrenze()` (Layer-Regex GRUNDST.CKSGRENZE|GRUNDSTUECKSGRENZE|KATASTER|PROP-?LINE **mit Entity-Typ-Filter** — Rennweg_OG3 trägt auf dem gleichnamigen Layer nur DIMENSION, ohne Filter ein Falschpositiv; Barawitzka braucht Brückung < 400 mm über 7 Grenzpunkt-Marker) → gemessen Barawitzka 616.8 m², Mollgasse 1510.2 m², die drei übrigen None; `_strassenkante()` über GEHSTEIG|GEHWEG|RANDSTEIN|BORDSTEIN (**`STRA.ENVERKEHR` darf NICHT hinein** — der Layer läuft bei Barawitzka auch an der Südgrenze und öffnet den Hof wieder). Ohne Grenz-Layer bleibt der Hüllen-Fallback, auf Rennweg_EG/OG3 und Muthgasse_E2 als bit-gleich nachgewiesen. **Zweiter, unabhängiger Pfad — Naht-Detail für alle:** `tuer_typisierung` Regel 1 (`AUSSEN × ALLGEMEIN_ERSCHLIESSUNG → hauseingang`) bezieht ihr AUSSEN aus `nutzungsklasse.py` (`TERRASSE → AUSSEN`), NICHT aus `aussenbereich`; die Tür STIEGENHAUS raum_37 → TERRASSE raum_43 im ummauerten Hof wurde dadurch hauseingang und über `ausgaenge.py` final_exit, an der Außen-Analyse vorbei. Deshalb der optionale Guard `kein_weg_ins_freie` (Default None = Bestandsverhalten). **Ergebnis:** Barawitzka 2 → **1** final_exit (`exit_tuer_27`, an der Straße), Mollgasse 10 → **9** (beide Hofausgangs-Cluster erhalten, GT ≥ 4 hält). Der Soll-Nahttest Barawitzka ist auf den gemessenen Wert nachgezogen. **969 passed, 10 skipped, 10 xfailed, 0 failed.** **@mvpo3 — Überdachung vor dem Haupteingang** (Owner-Anforderung: dort werden Aufheller platziert): namensbasierte Erkennung ist TOT (kein Plan trägt VORDACH|ÜBERDACHUNG|AUSKRAGUNG|LAUBENGANG|ARKADE|PERGOLA|CANOPY, geprüft über alle Layer/Blöcke/Texte von 5 Plänen). Einziges Signal ist ein nicht flächendeckender Decken-Layer ∩ offene Außenfläche; real gemessen nur auf Barawitzka: **1 Überdachung, 0.707 m², 2.22 m vor `exit_tuer_27`**. Liegt bewusst als reiner Prüfstrecken-Output im Bericht (Abschnitt „Außenbereich") — ein `Ausgang.ueberdacht: bool` wäre die Naht zu deiner Aufheller-Platzierung, aber auf Basis EINES Plans und eines 0.7-m²-Stücks zu früh für einen 3-Owner-Antrag. **Sag Bescheid, wenn du den Aufheller wirklich davon abhängig setzen willst — dann stelle ich den Antrag.**
- 2026-09-07 **Selman → @mvpo3 @EnisAMG: Branch `selman/fluchtweg-spec-abgleich` (17 Commits) — ⚠️ braucht 3-Owner-Approval (Contract v1.2.0 → v1.3.0).** Umsetzung von Leonis' 05_fluchtweg-Rückmeldung (Punkte 1–6) plus der geforderten Vorarbeit, danach ein systematischer Abgleich jedes Spec-Punkts gegen den echten Code (jeder Befund gegengeprüft). **Contract-Änderung (rein additiv, kein Erzeuger bricht):** `Tuer.quelle` und `Tuer.untypisiert_grund`, beide `str | None = None`, Schema regeneriert, `CONTRACT_VERSION` 1.3.0. `quelle` ist der Audit-Trail der Türquelle (`block`/`arc`/`arc_aussen`/`durchgang`/`oeffnung_aussenwand`/`text:<Wort>`/`doppelfluegel`/`windfang`), `untypisiert_grund` trägt den Grund für die Gründe-Tabelle im Prüfbericht. **Fachlich:** Fluchtweg-Ziel nach Geschoss (EG/UG → final_exit, OG → stair_exit + „Restweg im EG"), Außenbereich je Gebäude-Komponente inkl. Hof, fünf zusätzliche Türquellen, Kreuzcheck Fluchtweglinien ↔ Endausgänge mit `notausgang_kandidat`, Referenzvergleich gegen den Fachplaner-Plan (`07_referenzvergleich.png`), Muthgasse als 4. erschlossene CAD-Familie. **Wirkung:** Barawitzka 2 → 3 final_exit, Muthgasse 2 → 9; erstmals alle fünf Pläne in einem Prüfstrecken-Lauf (Muthgasse_E2 und Rennweg_EG liefen vorher nie vollständig durch). **965 passed, 10 skipped, 10 xfailed, 0 failed.** **@EnisAMG — zwei Fragen aus deiner Lane, bewusst NICHT entschieden:** (1) Gilt ein Innenhof ohne eigenen Weg ins Freie (AUSSEN_GESCHLOSSEN) als gesicherter Bereich, d.h. darf eine Tür dorthin ein Endausgang sein? Heute: nein, kein final_exit. (2) Soll `AUSSEN_GESCHLOSSEN` ein eigener `Nutzungsklasse`-Wert im Contract werden (heute nur interne Polygonliste) — das wäre ein weiterer 3-Owner-Slice. **@mvpo3 — eine Frage aus deiner Lane:** soll ein `final_exit`, an dem kein Weg endet, ein Contract-Feld `unbenutzt` bekommen (heute nur Prüf-Output im Bericht)? **⚠️ Offener Befund für die Raumerkennung (dokumentiert, NICHT gefixt), `docs/OFFENE_FRAGEN.md`:** bei Mollgasse gelten 960 m² Gebäudeinneres als AUSSEN — 20 von 64 Räumen liegen darin, darunter Gänge und Zimmer. Ursache gemessen: `finde_wandkoerper` baut Wandkörper nur aus HATCH-Flächen; die 989 Wandumriss-Segmente auf `02-TWA`/`02-WDA`/`02-ZWA` bleiben ungenutzt, weil der Doppellinien-Fallback hinter `if len(out) < 5` steht und bei 171 Hatch-Körpern nie läuft. Folge: 5 Mini-Komponenten statt einem Trakt. Das erklärt, warum der Kreuzcheck bei Mollgasse 40 `notausgang_kandidat` meldet, obwohl real 1–2 Ausgänge fehlen (Leonis' Einschätzung ist richtig, der Kreuzcheck überzeichnet). Drei Fix-Varianten gemessen — jede kostet Barawitzka seinen AUSSEN_GESCHLOSSEN-Hof, deshalb hier bewusst kein Halbfix. Der neue Berichtsabschnitt „Außenbereich" macht den Defekt bei jedem Lauf sichtbar (Mollgasse: 600 m² Komponenten gegen 1307 m² „offener Außenbereich").
- 2026-09-05 **Owner → @polatselman: neue Vorlage `Vorlagen-Legende/Baulegende.dxf|pdf` auf main** (Push durch Leonis, `bf59bdf`) — die Bau-/Wandtypen-Legende im Muthgasse-Polierplan-Stil (Alu-Glas · Ziegel · GK-Wände **EI90/EI30/EI0** · Beton · Stahlbeton · Wärmedämmung · Brandabschnitt). Für deine Wand-/Raumerkennung gedacht (EI-Klassen-Vokabular + Schraffur-Referenz); `.gitignore`-Ausnahme gesetzt. Dazu passend: die Voll-Analyse des din-Profi-DWG (`STROMKREISNUMMer_DWG.md`-Nachtrag 2026-09-05) listet jetzt auch `din_brandabschnitte(_virtuell)`-Layer + komplettes SIBEL-Farb-Schema.
- 2026-09-05 **Leonis → @EnisAMG @polatselman: ⚠️ LOST-MERGE bei den vier Konsum-PRs — Code hat main NICHT erreicht.** Danke für Approvals + Merges der drei Contract-PRs (#93/#87/#96 — sauber auf main, 572 grün). ABER: #88, #92, #95, #98 wurden um 11:02 als **Stacked-PRs in ihre BASIS-Branches** gemergt (#88→`leonis/oib-gate-contract`, #92→`leonis/oib-gate-konsum`, #95→`leonis/oib-api-gate-v2`, #98→`leonis/sonderstellen-konsum`) — alle vier Merge-Commits sind **nicht auf main** (verifiziert via `git merge-base --is-ancestor`), der Konsum-Code strandet in toten Branches. GitHub zeigt sie „merged", aber `platzierung/` auf main hat weder `oib_gate.py` noch `sonderstellen_strategy.py`. Gleiche Fehlerklasse wie der #16-Mishap. **Rettung läuft (Leonis):** saubere Neuschnitte je Konsum-Commit frisch auf main — **#105** ersetzt #98 (Symbol-Datenmodell-Konsum, cherry-picked, 580 grün, mit #102-Labels komponiert); #95-Ersatz folgt MIT Enis' Review-Fixes (Fallback-Kennzeichnung + `manuell_pruefen`-Regel); #88/#92-Ersatz danach. Bitte künftig Stacked-PRs erst mergen, wenn die Basis auf main ist (bzw. PR-Base vorher auf main umstellen).
- 2026-09-05 **Leonis → @polatselman: Muthgasse-Crash-Ursache identifiziert + konkrete Kipp-Anleitung** (`knowledge/extracted/MUTHGASSE_POLIERPLAN_BRANDSCHUTZ.md`): die Familie nutzt den **AIA-Layer-Standard** — Wände `A-WALL`/`I-WALL`, Räume `A-AREA-BNDY`+`A-AREA-IDEN` (Stempel-Code `{Etage}-{Top}-{Raum}`, **`VF` = Verkehrsfläche = GANG-Marker**), Türen `A-DOOR`+`-IDEN` (mit EI₂30-Klasse im Text!), Stiegen `S-STRS`. Bonus: Architekt schreibt **Fluchtweglängen als MTEXT** (`FLW-L 37,2 m`, Layer `A-FLOR-HRAL-IDEN`, alle 9 Etagen ausgewertet, max 37,2 m vs. AStV-40-m-Grenze) → externes Soll für die vertagte N2-Weglänge-Deckung; und die Legende führt **F+H (Feuerlöscher+Hydrant)**/BRE/BST → reale Datenquelle für den Sonderstellen-Contract #93. PDFs = Rohbau-Polierpläne OHNE Notbeleuchtung (kein Golden-Soll fürs Placement).
- 2026-09-05 **Leonis: PR #102 GEMERGT (main `ee562e6`, 572 grün) — inkl. Library-Wechsel: kanonische Symbol-Library ist jetzt `CAD_Symbole/Notbeleuchtungssymbole.dxf` (Owner-Entscheidung, NUR noch diese Symbole).** Drei Owner-Feedback-Nachfixes in-Session: (1) NODEID/Stromkreisnummer-Label je Symbolart neben dem Symbol positioniert (Halbbreite RZ 290/SL 435 + 280 Clearance), Höhenkote 150→90 = Label-Größe. (2) **SL-Aufheller grün statt blau** — Library-Blöcke tragen explizite Blau-ACIs (HATCH 150), die den Layer-Grün-Override übergehen; `library.py` stellt sie beim Import auf BYLAYER (+Regressionstest). (3) Library-Umstellung: kuratierter 5-Block-Extrakt + Legenden-Block, Geometrie identisch zur E-Symbole-Teilmenge (Visual-Goldens unverändert grün); ⚠️ Naht-Detail für alle: `blocks.block_names()` normalisiert **lowercase**, und der „unten"-Pfeil heißt neu `notbeleuchtung- richtungspfeil nach unten` (mit Bindestrich); `sync_layers` legt `din_SIBEL_10_emergency_lighting` jetzt immer selbst an. E-Symbole.dxf bleibt nur als Herkunfts-Referenz im Repo.
- 2026-09-05 **Leonis: Stromkreisnummer Anlage/Kreis/Adresse als NODEID-Zweitzeile (Branch `leonis/stromkreisnummer-labels`, kein Contract).** PR #101 (DWG-Input) ist gemergt (User-GO, main `a809764`, 568 grün). Folge-Slice = Digest-Empfehlung #1 aus `STROMKREISNUMMER_DWG.md`: **Der Zuweisungs-Pass existierte schon** (`platzierung/circuit_zuordnung.py`: Cap 20/Kreis, DL/BL getrennt) — gefehlt hat das Profi-Format **`Anlage/Kreis/Adresse`** (LABELING1) am Symbol. Neu `render/dxf_renderer.py::_stromkreisnummern`: render-seitig deterministisch aus `circuit_hint` (Anlage = Gebäude-Letter A=1/B=2, Kreis = distinct Hints je Anlage in Erst-Auftretens-Reihenfolge — deckt alte `AGV-A-F13`- UND neue `AGV-A-F13-DL-1`-Hints ohne Suffix-Parsing, Adresse = fortlaufend im Kreis; ohne Hint kein Label). `_draw_nodeid_labels` rendert zweizeilig `RZ-001\P1/1/1` (User-Entscheidung: NODEID bleibt als Wartungs-ID, wie Profi-Plan luminaire_ID + LABELING1 getrennt), Summary-Feld `stromkreisnummern_drawn`. Belegungsliste unangetastet. Mollgasse-EG verifiziert: 43/43 Labels zweizeilig, Kreis-Summen 1:1 deckungsgleich mit der Belegungsliste, Cap sichtbar (AGV-B-BL-1 = exakt 20 → Rollover in Kreis 2/3). +3 Unit-Tests + Golden-Test erweitert, **571 passed**, ruff clean, Visual-Goldens unverändert grün, kein Contract. 3-Owner-Stacks #87/#88/#92 · #93/#95 · #96/#98 weiter ohne Approvals; wenn #96 (v1.2.0 `luminaire_id`) irgendwann gemergt ist, kann die Ableitung vom Render in den Platzierungs-Pass wandern.
- 2026-09-04 **Leonis: DWG-Input via ODA File Converter (Branch `leonis/dwg-input-odafc`, kein Contract) + Muthgasse 109B als 5. CAD-Familie.** Tool-Recherche-Kandidat #1 umgesetzt: neues `hauptengine/dwg_input.py` (Discovery der versionierten ODA-Installations-Ordner + `stelle_dxf_bereit`: DXF passthrough/bit-identisch, DWG konvertiert, ohne Konverter klarer `OdaKonverterFehlt`); `pipeline.run` nimmt `.dwg` (Konvertat im TemporaryDirectory), API `/plan` + `/projekt` nehmen DWG-Uploads (fehlender Konverter → 503). Erstes **skip-if-Tool**-Testpattern. **@polatselman — neues Real-Material:** `Projekte/Pläne 19., Muthgasse 109B - 2026-05-07_13-12/` (9 Etagen E2–E9+DD, aus DWG konvertiert, + Plan-PDFs als Soll-Referenz). Ist-Stand sondiert: **kein Wand-Layer-Muster greift → `bounds_mm` bricht ab** (Crash-Klasse, im E2E-Netz als raises-Assert mit Kipp-Anleitung gepinnt). **Wissens-Gap zu:** `Stromkreisnummer.dwg` extrahiert (`knowledge/extracted/STROMKREISNUMMER_DWG.md`) — Schema **Anlage/Stromkreis/Adresse**, 2 Gruppenbatterien SU 6P NET E30 à 6 Kreise, Cap ≈20 Leuchten/Kreis, `IsBLString`=DL/BL (bestätigt Symbol-Datenmodell #96), Typ-Letter A–P, DIN-`#v1`-Obfuskierung dekodiert (XOR 0xFF). **568 passed**, ruff clean, kein Contract; die 3-Owner-Stacks #87/#88/#92 · #93/#95 · #96/#98 bleiben unberührt offen.
- 2026-09-02 **F2: Prüfregel 8c (Türen ≫ Räume = Erkennungsbasis widersprüchlich) + Barawitzka EG im E2E-Netz (Branch `leonis/barawitzka-basis`, kein Contract).** PR #94 (8b + Fischamender/Herrenholz-E2E) ist gemergt (main `917375d`). Vierte Familie sondiert: **Barawitzka EG = 116 Türen erkannt, aber nur 2 Räume** (Raum-Layer-Nacharbeit, bekannt) **+ 0 Symbole → Prüfstatus war „ok"**, weil Regel 8/8b auf n_raeume ≥ 15 gaten. Neue **Regel 8c**: ≥ 30 Türen (Gebäude-Beweis) bei < 15 Räumen → Warnung „Prüfbasis Räume (Erkennung widersprüchlich)" — die Tür-Erkennung ist das unabhängige Signal, dass die Raum-Erkennung das Gebäude nicht erschlossen hat. E2E: `test_familien_durchstich.py` + Barawitzka (Tür-Band ≥ 60 hält, Raum-Lücke gepinnt mit Kipp-Anleitung, leeres Ergebnis ≠ ok). Mollgasse/Fischamender/Herrenholz unberührt (Räume ≥ 15 → 8c schweigt). +5 Tests, **551 passed/5 skip**, ruff clean, kein Contract, kein Overlap mit #87/#88/#92/#93.
- 2026-09-02 **F2: Prüfregel 8b (Prüfbasis fehlt ≠ ok) + E2E-Regressionsnetz Fischamender/Herrenholz (Branch `leonis/e2e-familien`, kein Contract).** PR #90 + #91 sind gemergt (User-GO, main `c5ad5ed`). Sondierung auf den Nicht-Mollgasse-Familien deckte auf: **Fischamender BT1 EG/1.OG läuft mit Prüfstatus „ok" durch, obwohl 0 Ausgänge + 0 Segmente erkannt werden** (Bug B2) — die Kern-Regeln 3/4/4b/5 laufen dann schlicht nie, „ungeprüft" wurde als „erfüllt" verkauft. Neue **Regel 8b**: ≥15 Räume + Symbole platziert, aber 0 erkannte Ausgänge bzw. 0 Fluchtweg-Segmente → je eine **Prüfbasis-Warnung** (nur wenn Symbole da sind — sonst erzählt Regel 8 die Geschichte schon). Dazu **`tests/e2e/test_familien_durchstich.py`** (skip-if-Asset, Muster von #84): Fischamender EG (Räume/Türen-Bänder halten, B2 als Warnung ausgewiesen, Status ≠ ok; kippt sichtbar wenn @polatselman B2 fixt) + Herrenholz EG (473 Räume, 0 Symbole → Plausibilität MUSS „fehler" sagen, fail-closed-Schranke). Mollgasse EG unberührt (hat Ausgänge+Segmente → 8b schweigt). +9 Tests, **546 passed/5 skip**, ruff clean, kein Contract; berührt NICHT die #87/#88/#92-Dateien.
- 2026-09-02 **Leonis (@mvpo3) → @EnisAMG: alle vier Quellen-Korrekturen bestätigt + umgesetzt; dein OIB-Gate-Vorschlag ist gebaut (PR #87 + #88). Und: Sonderstellen-Option A hat mein GO.** Im Einzelnen: **(1) Ud 40/40 statt 40/10** — übernommen; die Fehlangabe ist korrigiert in `platzierung/lux.py::ud_min_aus_norm` (Docstring, PR `leonis/quellen-korrekturen`), im `gleichmaessigkeit_max`-Kommentar von `contracts/norm_regelwerk.py` (PR #87) und in `Handoff/LEONIS.md` (alte „Antipanik 1:10"-Versprechen als korrigiert markiert). **(2) Uo≠Ud** — im Docstring/Kommentar explizit festgehalten (kleinste:mittlere vs. kleinste:größte, EN 12665), damit der Kategorienfehler nicht wiederkehrt. **(3) 60/8 m² nicht aus EN 1838** — der `FlaechenSchwellen`-Docstring attribuiert jetzt OVE E 8101:2019 718.560.9.001.AT / ÖVE/ÖNORM E 8002-1 samt Scope-Bindung (PR #87, Schema-Description regeneriert). **(4) Umschaltzeit zweistufig** — Kommentar stellt klar: das Contract-Feld trägt den 60-s-Vollwert, der 5-s-Halbwert liegt in deiner YAML. **Dein Gate-Vorschlag ist umgesetzt:** PR **#87** (Contract, v1.2.0: `FlaechenSchwellen.quelle` additiv, `ProviderBundle.oib`, `Platzierer.place(…, *, oib: OibBefund|None)`) + PR **#88** (stacked: neues `platzierung/oib_gate.py`, v1 projekt-global + **fail-closed** — nur `eingeschraenkt`/`uneingeschraenkt` öffnet, `review_required`/kein Kontext = Gate zu = heutiges Verhalten; `pipeline.run(…, projekt_kontext=…)` ruft `bundle.oib.bewerte_oib`, `registry` verdrahtet deinen `OibRl2Provider`, `render_summary["oib"]` trägt Stufen+Gate+Hinweise). Getriggerte Leuchten übernehmen `flaechen_schwellen.quelle` als `norm_quelle` (Fallback: Antipanik-Regel-Quelle, Naht hält). 550 grün, Mollgasse-E2E ohne Kontext bit-identisch. **#87 wartet ausdrücklich auf eure Approvals** (dein Prozess-Punkt gilt — kein Merge mit < 3 Stimmen). **Follow-up an dich (deine Lane, nach #87/#88-Merge):** `flaechen_schwellen`-Werte + `quelle` in `normwissen/data` füllen UND `provider._snapshot` so erweitern, dass die Schwellen-Quelle in `NormRegelwerk.quellen` landet — sonst bricht beim ersten getriggerten Raum die Naht-Invariante. **Sonderstellen-Contract:** ich gebe als Owner das **GO für Option A** (`RaumModell.sonderstellen[]` + `ist_barrierefrei` + `besondere_gefaehrdung`, rein additiv) — damit 2 von 3 Stimmen (du + ich); der Contract-PR braucht noch @polatselman. Dein §7-Handoff (`docs/SPEC_SONDERSTELLEN_CONTRACT.md`) kann vorbereitet werden.
- 2026-09-02 **F2: disconnected-graph-Anker — Luftlinien-Richtung statt fabriziertem „unten" (Branch `leonis/anker-disconnected`, kein Contract).** Zweiter Handoff-Kandidat: `anker_strategy.plan_rettungszeichen_anker` fiel für Kreuzungs-Anker in einer Graph-Komponente **ohne erreichbaren Ausgang** (Provider-Lücke, kein Dijkstra-Gefälle) auf `richtung="unten"` zurück — der Pfeil behauptete „Ausgang erreicht", obwohl keiner erreichbar ist. Jetzt: Luftlinie zum **geometrisch nächsten Ausgang** (beste verfügbare Richtungs-Information); existiert gar kein Ausgang im Modell, bleibt der dokumentierte Letzt-Fallback „unten". Mollgasse EG unverändert (15 RZ + 21 SL, ok — dort keine ausgangslose Komponente, No-op). +2 Tests, **537 passed/5 skip**, ruff clean, kein Contract. Unabhängig von `leonis/lb-vokabular-warnung` (PR #90, nicht gestackt).
- 2026-09-02 **F2: Prüfregel 10b — tote LB-Bereichsregeln sichtbar (Branch `leonis/lb-vokabular-warnung`, kein Contract).** PR #84 ist gemergt (Union-Auflösung wie von F1 erbeten, main `b96ea50`). Nächster Handoff-Kandidat umgesetzt: `hauptengine/validierung._lb_konformitaet` meldet jetzt Bereichsregeln, deren `raum_typ` **keinen Raum mit gültigem Polygon** matcht — bisher war so eine Regel ein **dreifach stiller No-op** (`lb_override` wirkt nicht, Regel 9/10 laufen gar nicht erst, Prüfbericht „ok"): der Plan sah konform aus, obwohl eine explizite Auftraggeber-Vorgabe nie angewendet wurde (die reale Bug-Klasse ABSTELLRAUM/LAGER/TECHNIK aus PR #58, dort nur statisch per Drift-Guard gedeckt — 10b ist das **Laufzeit**-Pendant je Plan). Status **warnung** (Raumtyp kann legitim fehlen), Detail nennt die toten Typen + Hinweis „nur Räume ohne gültiges Polygon" wenn der Typ existiert, aber geometrisch nicht wirkbar ist. **Real-Data-Verifikation:** Mollgasse EG + eigene Elektro-LB → bit-identisch (15 RZ + 21 SL, ok; die LB hat keine Bereichsregeln). Cross-Check Mollgasse + Fischa-GK4-LB → Exklusion STIEGENHAUS/GANG wirkt real (21 SL entfernt), Inklusion GARAGE matcht keinen Raum → 10b-Warnung `['GARAGE']`. +5 Tests, **535 passed/5 skip**, ruff clean, Schema kein Drift. Nur `hauptengine/validierung.py` + Tests — berührt NICHT `flaechen_strategy`/Contracts (kein Overlap mit F1s offenen #87–#89).
- 2026-09-01 **Leonis → @F2: PR #84 (`leonis/abstand-nachpass`) selbst auflösen + mergen — Handoff-Konflikt mit #85.** Auf User-Wunsch sollten #83 (Enis, gemergt+verifiziert) und #84 gemergt werden. #83 ist auf `main` (`cfcbbde`), Track B damit AKTIV — Regress-Check ok: Mollgasse EG unverändert 15 RZ + 21 SL, Prüfstatus ok, 524 passed. **#84 konnte ich NICHT mergen:** (a) der Branch ist live in eurem Worktree `Notbeleuchtung-F2` (ich fasse ihn nicht an, kein force-push), (b) er kollidiert mit meinem gemergten #85 auf `Handoff/LEONIS.md`. **Bitte bei euch lösen:** `git fetch origin && git merge origin/main` auf `leonis/abstand-nachpass`, den Handoff-Konflikt **per Union** auflösen (mein „STAND (2026-09-01, Nacht)"-Block bleibt oben stehen, euren abstand-nachpass-STAND darunter einreihen), dann PR #84 mergen. Nur der Handoff-Doc kollidiert — der Code (`platzierung/abstand_nachpass.py` + Test) ist konfliktfrei.
- 2026-09-01 **F2: Abstands-Nachpass gegen Kollisionen an der Strategie-Naht (Branch `leonis/abstand-nachpass`, kein Contract).** Neuer letzter Geometrie-Pass `platzierung/abstand_nachpass.py` (`entzerre`, läuft in `place` nach `lb_override`, vor `deckungs_zuordnung`): gleich-artige Koinzidenzen mergen (echte Dubletten aus Anker+Gang / zwei Flächen-Pässen), verschieden-artige deterministisch nudgen (Prio rz>sl>antipanik, im Raumpolygon geclamped, **nie eine benötigte Leuchte löschen**). Jede Strategie deduplizierte bisher nur intern (`anker._MIN_RZ_MERGE_MM`, nur RZ↔RZ) — die Naht zwischen Strategien war ungeprüft. **Ehrlich reframed:** DOD-Befund #5 (1 Paar < 250 mm, 2026-08-31) **reproduziert auf `main` f92010f nicht mehr** (mit LB verifiziert: 36 Symbole, 0 Kollisionen, mit UND ohne den Pass) — der Nachpass ist **kein Bugfix für einen lebenden Defekt, sondern eine strukturelle Garantie** (Defense-in-depth: Kollisionsfreiheit invariant statt zufällig); auf Mollgasse EG aktuell ein No-op. Wert-Schwerpunkt daher der **fehlende Real-Data-Regressionstest** `tests/e2e/test_mollgasse_eg_durchstich.py` (skip-if-Asset): Kollisions-Regel=ok + alle Abstände ≥ 250 mm + Symbolzahl-Band — schließt die Fixture-Lücke, an der C2/Belegung/covers_segment durchrutschten (nur das dünne 4OG-Fake wurde getestet). +11 Tests (8 Unit synthetisch, 3 E2E), Mechanismus gegen Koinzidenzen unit-getestet. Suite grün, ruff clean, Schema kein Drift (kein Contract). PR wartet auf User-GO.
- 2026-09-01 **Enis: KORREKTUR einer eigenen bindenden Entscheidung — §4.1.2 nennt sehr wohl ein Lux-Niveau (5 lx VERTIKAL).** Der bis heute wichtigste offene fachliche Punkt ist geprüft, und die Annahme vom 31.08. („§4.1.2 belegt die Hervorhebungspflicht, nicht den Wert; die 5 lx stammen aus der Projekt-LB") ist am Volltext widerlegt. §4.1.2 **h)**: „nahe jeder Erste-Hilfe-Stelle, **so dass 5 lx vertikale Beleuchtungsstärke am Erste-Hilfe-Kasten erreicht werden**"; §4.1.2 **i)**: „nahe jeder Brandbekämpfungs- und Meldeeinrichtung, **so dass 5 lx vertikale Beleuchtungsstärke an den Melde-, den Brandbekämpfungseinrichtungen und der Anzeigen der Brandmeldeanlage erreicht werden**" (`knowledge/_extracted_text/normen/EN 1838 - Notbeleuchtung 2019.txt`, Norm-S.9). Damit ist der Wert für **feuerloescher, hydrant, erste_hilfe, brandmelder** normativ belegt; die reale Elektro-LB §5.1.23 **wiederholt** ihn nur. Die Hierarchie bleibt: weicht eine LB ab, übersteuert sie. **Entscheidend und unverändert ist die Bezugsfläche** — die 5 lx sind **vertikal am Gerät**, der Lux-Nachweis der Engine (`platzierung/lux.py::lux_raster`) rechnet **horizontal am Boden**. Ein Einsetzen als `min_lux` wäre derselbe Kategorienfehler wie Ud gegen Uo. Deshalb trägt die Query-API die Achse jetzt im Namen: `norm_lux_vertikal()` → 5.0, `norm_lux_horizontal()` → **immer** `None`, `norm_lux_bezugsflaeche()` → `"vertikal"`; die alte achslose `norm_lux()` wurde **entfernt**, nicht umgewidmet. In `platzierung_regeln.yaml` heißt der Schlüssel `min_lux_vertikal_norm` — er kann nicht versehentlich in den Bodenraster laufen. **Nebenbefund, damit ebenfalls erledigt:** §4.1.2 führt **b) Treppen** und **c) „jede andere Niveauänderung"** als getrennte Punkte — die frühere Notiz „unsere Extraktion nennt Treppen, die reale LB nennt Niveauänderungen" ist gegenstandslos. `RZ-06-NIVEAUAENDERUNG` und `SL-04-NIVEAUAENDERUNG` sind von `beleg: LB` / `decision_source: lb_explizit` auf `BELEGT` / `norm_default` gezogen; ihr **Lux-Wert bleibt offen** (§4.1.2 c) nennt keinen), also weiter `MANUELL_PRUEFEN`. **Warum es zuerst übersehen wurde:** die Prüfung am 31.08. stützte sich auf `_port_source/emergency_lighting_en1838.yaml` statt auf den Volltext; die Extraktion führt §4.1.2 verkürzt und ohne die Buchstaben h)/i). Lehre, die ab jetzt gilt: **die Extraktion ist ein Index, kein Beleg.** Geändert: `normwissen/data/sonderstellen.yaml` (5 Typen), `normwissen/sonderstellen.py` (Lux-API), `normwissen/data/platzierung_regeln.yaml` (SL-04…SL-08, RZ-06), 3 Tests umgedreht + 3 neue, `docs/NORMQUELLEN_AT.md` Abschnitt **2c**, `docs/SPEC_SONDERSTELLEN_CONTRACT.md` §3, `Handoff/ENIS.md`. **519 passed / 5 skipped**, ruff sauber, Schema in sync, **kein Contract berührt**, Mollgasse-EG-Durchstich unverändert (15 RZ + 21 SL, ok). **@mvpo3 / @polatselman:** das ändert nichts am Sonderstellen-Vorschlag selbst — es macht ihn stärker: vier der fünf Typen haben jetzt einen belegten Norm-Wert statt eines Review-Flags.
- 2026-09-01 **Enis: Track-B-Norm-Werte gefüllt (Branch `enis/norm-trackB-werte`, KEIN Contract) + vier Quellen-Korrekturen an PR #72.** Zwei der vier v1.1.0-Felder sind jetzt belegt gefüllt, zwei bleiben bewusst leer. Geprüft am Volltext der im Repo liegenden Ausgabe (`knowledge/_extracted_text/normen/EN 1838 - Notbeleuchtung 2019.txt`), Details `docs/NORMQUELLEN_AT.md` Abschnitt **2b**. **@mvpo3 — vier Korrekturen:** (1) **Antipanik-Ud ist 40, nicht 10.** §4.2.2 (Rettungsweg) und §4.3.2 (Antipanik) sagen wortgleich „darf 1 : 40 nicht unterschreiten". (2) Die „10" stammt aus **§4.4.2** (Arbeitsplätze mit besonderer Gefährdung, **Uo ≥ 0,1**) — Uo (kleinste:mittlere, EN 12665) ist **nicht** Ud (kleinste:größte), also ein anderes Maß und das falsche Feld. Die Fehlangabe steht im Docstring von `hauptengine/contracts/norm_regelwerk.py` (Feld `gleichmaessigkeit_max`), im Docstring von `ud_min_aus_norm` in `platzierung/lux.py` und im PR-#72/#81-Text — beide Dateien liegen nicht in Enis' Lane, deshalb nur gemeldet, nicht angefasst. (3) **60 m² / 8 m² stehen NICHT in EN 1838**; der Contract-Docstring schreibt sie „EN 1838 §4.3 / OIB" zu. In der vorliegenden Ausgabe kommt überhaupt keine flächenbezogene Auslöse-Schwelle vor; §4.3.8 nennt Toiletten für Menschen mit Behinderung **ohne** Flächenmaß. (4) `umschaltzeit_max_s` ist als **Skalar** nicht ausreichend: §4.2.6/§4.3.6/§5.4.6 fordern **zweistufig** 50 % in 5 s und 100 % in 60 s. **Gefüllt:** `gleichmaessigkeit_max` = 40 für Rettungsweg (§4.2.2) und Antipanik (§4.3.2) über ein neues `gleichmaessigkeit_ref` in `raumtyp_regeln.yaml`; `umschaltzeit_max_s` = 60 s (Vollwert), die 5-s-Halbwertstufe bleibt als `umschaltzeit.halbwert_s` in der YAML sichtbar. Aufheller/Betonungsleuchten (§4.1) bekommen **keinen** Ud-Wert — die Norm nennt für sie keinen. **Beides ist inert:** 40 ergibt über `ud_min_aus_norm` exakt den bisherigen Default 1/40, `umschaltzeit_max_s` greift in `validierung.pruefe` nur gegen einen LB-Wert. Mollgasse-EG-Durchstich vorher/nachher **identisch: 15 RZ + 21 SL, Prüfstatus ok, 7 Befunde**. **NICHT gefüllt — `flaechen_schwellen`, mit Vorschlag:** die Werte sind belegt, aber in **OVE E 8101:2019 `718.560.9.001.AT`** bzw. **ÖVE/ÖNORM E 8002-1**, und dort **scope-gebunden** — die 8 m² gelten nur „für Räume, Anlagen oder Gebäude, an die erhöhte Anforderungen nach der Art der Nutzung (OVE R 12-2 bzw. OIB-RL 2) gestellt werden"; die 60 m² dort nur für **Flughäfen und Bahnhöfe**; der allgemeine 60-m²-Satz steht in E 8002-1 §3.2.2.1.2 nur als **ANMERKUNG** in einer Begriffsbestimmung. `flaechen_strategy._ist_flaechen_antipanik` wendet die Schwelle dagegen **global auf jeden Raum** an — ein Füllen würde die Norm über ihren Geltungsbereich hinaus anwenden und den Auslöser im Audit-Trail unter `norm_quelle = "ÖNORM EN 1838:2013 §4.3.1"` führen, obwohl er aus OVE stammt. **Vorschlag:** den Flächen-Trigger an den bereits vorhandenen **`OibRl2Provider`** gaten — der bewertet genau die „erhöhten Anforderungen nach der Art der Nutzung", auf die OVE E 8101 verweist. Bis zur Entscheidung bleiben beide Felder `None` (inert, kein Fehlalarm). **NICHT gefüllt — `arbeitsplatz_lux`:** §4.4.1 (10 % der Nennbeleuchtungsstärke, mind. 15 lx) ist belegt, aber ohne Raumtyp „Arbeitsplatz mit besonderer Gefährdung" im RaumModell wäre der Wert toter Code → **Track C, @polatselman**; dieselbe Lücke führt `sonderstellen.yaml` als `besondere_gefaehrdung`. **Nebenbefund:** Anhang B (A-Abweichungen) führt Frankreich, Italien, Deutschland und die Niederlande — **für Österreich keine Abweichung**; die deutschen 15 s für §4.2.6/§4.3.6 gelten hier ausdrücklich nicht. `tests/fixtures/norm_regelwerk_snapshot.json` blieb unangetastet (3-Owner-Lane) → `FakeNormProvider` liefert weiter `None`, die Fake-basierten Tests üben also den Fallback-Pfad; das ist gewollt. +9 Tests, **517 passed / 5 skipped** (vorher 508), Schema in sync, ruff sauber, kein Contract berührt.
- 2026-09-01 **Enis → @mvpo3 / @polatselman: Sonderstellen-Contract — Entscheidung weiterhin offen (2. Bitte).** PR #69 ist seit 31.08. gemerged, hat aber **0 Reviews und 0 Kommentare**, und im Log steht keine Reaktion. Der fachliche Vorschlag ist fertig und ausführbar (`docs/SPEC_SONDERSTELLEN_CONTRACT.md` §6 Optionen A/B/C, §7 Handoff nach dem GO; `normwissen/data/sonderstellen.yaml` + `SonderstellenKatalog` + 17 Tests). **Empfehlung unverändert Option A** — generisches `RaumModell.sonderstellen[]` (feuerloescher · hydrant · erste_hilfe · brandmelder · niveauaenderung) + `ist_barrierefrei` (§4.3.8) + `besondere_gefaehrdung` (§4.4.1). Rein additiv, alle Felder mit Default, schaltet **exakt** die 8 heute blockierten Placement-Regeln frei (ein Test hält die Gleichheit fest); vier davon sind belegte Pflichtstellen aus §4.1.2. Solange das GO fehlt, bleibt jeder erzeugte Plan in diesem Punkt unvollständig, **ohne dass man es der Ausgabe ansieht**. `besondere_gefaehrdung` ist zusätzlich die Voraussetzung dafür, dass das schon existierende Contract-Feld `arbeitsplatz_lux` überhaupt einen Auslöser bekommt (s. Eintrag oben) — die beiden Themen hängen zusammen. Track B fasst `hauptengine/contracts/**` bis zum GO nicht an.
- 2026-09-01 **Enis: Prozess-Punkt — Approvals in der `normwissen`-Lane.** Sachlich, keine technische Frage. `src/notbeleuchtung/normwissen/` ist per CODEOWNERS Enis' Lane; **#14, #22, #23 und #40** gingen ohne Enis-Approval durch. Neu dazu: **PR #72** hat `hauptengine/contracts/norm_regelwerk.py` auf v1.1.0 gehoben, war im eigenen PR-Text ausdrücklich als **„braucht 3-Owner-Approval"** deklariert und trägt trotzdem nur **ein** Approval (@polatselman) — gemerged von @mvpo3. Genau dieser PR hat die vier Quellen-Fehler transportiert, die der Eintrag oben korrigiert; ein Review aus der Normwissen-Lane hätte sie vor dem Merge gefunden. Bitte: Contract-PRs und `normwissen/**`-PRs vor dem Merge auf das dritte Approval warten lassen. Der Contract selbst ist inhaltlich in Ordnung und wird nicht zurückgedreht — es geht nur um den Weg dorthin.
- 2026-09-01 **Leonis: Norm-Integration Platzierung Track B — Konsumption der v1.1.0-Felder (Branch `leonis/norm-trackB-konsum`, kein Contract).** PR #72 (`NormRegelwerk` v1.1.0) ist gemergt → Leonis konsumiert die neuen abfragbaren Felder, **defensiv**: solange Enis' Werte `None` sind, ist der Plan bit-identisch. (A) `lux.lux_raster` bekommt `ud_min`; `deckung` + `flaechen_strategy` leiten es über `ud_min_aus_norm(anf.gleichmaessigkeit_max)` ab (Hardcode `1/40` weg) — sobald die Norm füllt, bekommt Antipanik sein korrektes **1:10**, Fluchtweg bleibt 1:40. (B) `flaechen_strategy` liest `regelwerk_snapshot().flaechen_schwellen`: offene Fläche ≥ `antipanik_min_m2` bzw. WC/Sanitär ≥ `wc_sanitaer_min_m2` wird antipanik-pflichtig (EN 1838 §4.3) — reiner **Zusatz**-Trigger, überschreibt nie eine Klassifikation; Antipanik-Parameter (0,5 lx/Symbol/Quelle) aus Enis' eigener Antipanik-Regel, nicht fabriziert. (D) `validierung.pruefe(…, norm=…)`: neue Regel **Umschaltzeit ≤ Norm-Höchstwert** (LB-`umschaltzeit_max_s` vs. strengster Norm-Wert) — LB > Norm → Warnung; fehlt ein Wert, übersprungen. Pipeline reicht `bundle.norm` durch. **Mollgasse EG unverändert: 15 RZ + 21 SL, Prüfstatus ok.** +8 Tests, 513 passed/2 visual-deselect, ruff clean, Schema kein Drift (kein Contract). **@EnisAMG:** die Felder werden jetzt konsumiert — sobald du `gleichmaessigkeit_max` (40/10), `flaechen_schwellen` (≈60/8 m²) und `umschaltzeit_max_s` in `normwissen/data` füllst, aktiviert sich die Logik automatisch (bis dahin inert, kein Fehlalarm). **Track-B-Rest VERTAGT:** Arbeitsplatz-Lux 15/5 lx (`arbeitsplatz_lux`) ist verdrahtungsbereit, aber es fehlt der Raumtyp „Arbeitsplatz mit besonderer Gefährdung" (EN 1838 §4.4) im RaumModell/Vokabular → **Track C** (@polatselman: neuer Raumtyp/POI, + Enis-Vokabular). Ohne den bliebe die Konsumption toter Code, daher bewusst nicht gebaut. PR wartet auf User-GO.
- 2026-09-01 **Leonis → @F2: circuit_hint DL/BL-getrennt + gedeckelt (erledigt #78-Befund).** F2s Verifikation deckte auf: alle Leuchten hingen grob auf 2 Kreisen (AGV-A/B-F13), Dauer- (RZ) + Bereitschaftslicht (SL/AP) gemischt. Neuer zentraler Nach-Pass `platzierung/circuit_zuordnung.py` (läuft zuletzt in `platzierer.place`, nach lb_override): je (Gebäude, Schaltungsart) fortlaufende Kreise `AGV-{G}-F13-{DL|BL}-{n}`, Deckel `_MAX_LEUCHTEN_JE_KREIS=20`. Mollgasse EG danach: `AGV-A-F13-DL-1` (10 RZ), `AGV-B-F13-DL-1` (5 RZ), `AGV-A-F13-BL-1` (2 SL), `AGV-B-F13-BL-1` (19 SL) — DL/BL sauber getrennt, `F13`-SV-Kennung bleibt (Naht zu validierung), Prüfung ok. **(a) DL/BL-Trennung + (b) Deckel erledigt.** Offen: der echte Deckel ist strombasiert (§3b, mA) → braucht Produkt-Stromaufnahme (Symbol-Datenmodell #6 / LB); bis dahin Stückzahl-Platzhalter. +3 Tests, kein Contract. 507 passed/5 skip (inkl. visual), ruff clean.
- 2026-09-01 **Leonis: Render-Test-Fixture-Lücke geschlossen — zweiter Visual-Golden (Misch-Szene).** Sowohl C2 (Doppelpfeil) als auch der Belegungs-Overflow versteckten sich hinter derselben dünnen Fixture: der 4og-Golden trägt nur 5 RZ (alle gerichtet, kein „gerade", wenige IDs) → beide Bugs waren grün. Neuer `test_visual_golden_mix` mit einer realistischen Szene, die genau die blinden Pfade ausübt: **RZ `richtung="gerade"` (Doppelpfeil), Sicherheitsleuchte + Antipanik als eigene Symbole, Belegung mit DL+BL über zwei Kreise**. Selbst-validierend (Entity-Asserts: 7 Platzierungen → 8 INSERTs weil die gerade-RZ = 2 Blocks; DL+BL in der Belegung) → kein blindes Golden. Vergleichslogik in `_assert_matches_golden` refaktoriert, 4og-Referenz unangetastet. Neuer Golden `mix_notbeleuchtung.png`. NUR `tests/`, kein Contract, kein Produktivcode. 502 passed/5 skip (+2 visual grün), ruff clean.
- 2026-09-01 **Leonis: Stromkreis-Belegungsliste kompakt — Overflow-Fix (#74-Follow-up).** Die Belegungs-Info-Box listete JEDE Leuchten-ID pro Kreis → auf realen Plänen 200+-Zeichen-Zeilen (Mollgasse EG: 217, 24 IDs), die in der festhöhigen Box umbrachen und nach unten in die Legende-Box überliefen. `pytest` grün, weil der 4og-Golden nur 5 IDs hat (dieselbe Fixture-Lücken-Klasse wie C2). Jetzt eine **kompakte Zeile je Kreis** (Anzahl je Art + Schaltungsart + Σ, z.B. `AGV-B-F13: 5× RZ (DL) · 19× SL (BL) — Σ24`, 41 Zeichen); die genaue ID↔Kreis-Zuordnung trägt der Plan selbst (NODEID-Annotation + Kreis-Label je Symbol). +Regressionstest (35 Leuchten → kurze Zeile), Visual-Golden neu. NUR `render/dxf_renderer.py`, kein Contract. 502 passed/5 skip, ruff clean.
- 2026-09-01 **Leonis: C2-Render-Fix — Doppelpfeil nur für Rettungszeichen (`kind=="rz"`).** Regression aus #73: `inserter.insert_platzierung` schaltete den beidseitigen Doppelpfeil allein an `richtung=="gerade"` — aber **Sicherheitsleuchten + Antipanik tragen ebenfalls `richtung="gerade"`** (= keine Richtung). Folge auf main: jede SL/AP rendert als **2 RZ-Richtungspfeile** statt ihres Symbols (Mollgasse EG: 21 SL → 42 falsche Pfeile). `pytest` war grün (4og-Golden hat keine „gerade"-Platzierung) → RIVOPLAN hätte es live geschaltet. Fix: Gate zusätzlich auf `p.kind == "rz"`; SL/AP behalten ihr Katalog-Symbol. +Regressionstest (SL/AP·gerade → 1 Symbol). Durchstich Mollgasse EG danach: 15 RZ + 21 SL = 36 Inserts (1:1). NUR `symbols/inserter.py`, kein Contract. 501 passed/5 skip, ruff clean.
- 2026-09-01 **F2 (Render, Branch `leonis/render-belegung`, kein Contract): Stromkreis-Belegungsliste (§3b).** PR #73 (DIN_SIBEL-Layer + Doppelpfeil + NODEID) ist **gemergt** (main `030815b`). Folge-Slice: neue Info-Box „STROMKREIS-BELEGUNG" oben in der Schriftfeld-Leiste (Layer `din_SIBEL_11_system`) — je Endstromkreis (`circuit_hint`) die zugeordneten Leuchten-IDs (dieselbe NODEID wie die Symbol-Annotation, via geteiltem `_nodeids()`-Helper), Schaltungsart **DL/BL** (`_SCHALTUNGSART`: RZ=Dauerlicht, SL/AP=Bereitschaftslicht) und Anzahl — kompatibel zur Profi-Vorlage `1.xlsx`. Render-only, konsumiert nur vorhandenes `circuit_hint` + `kind`, **kein Contract**. `_draw_nodeid_labels` auf den geteilten Helper umgestellt (IDs deckungsgleich). Summary-Feld `stromkreis_belegung_drawn`. Visual-Golden bewusst neu. +2 Tests. NUR `render/dxf_renderer.py`. 494 passed/5 skip, ruff clean, Schema in sync.
- 2026-09-01 **F2 (Render-Ausbau): Commit 3 — NODEID-Text-Annotation je Leuchte.** Fortlaufende Leuchten-ID (RZ-001/SL-002/AP-003, je Art gezählt) als kleiner MTEXT neben jedes Symbol, Layer `din_SIBEL_63_luminaire_ID` — Wartung/Adressierung (Profi-Plan §1.4). Rein Render-seitig aus der Platzierungs-Reihenfolge synthetisiert, **kein Contract-Feld**. Sitzt tangential zur Symbol-Achse (kollidiert weder mit Stromkreis-Label +Normale noch Höhenkote −Normale). Neue Funktion `_draw_nodeid_labels` + Layer + Summary-Feld `nodeids_drawn`. NUR `render/dxf_renderer.py`. Visual-Golden **bewusst neu** (neue Texte = sichtbare Änderung, `NOTBEL_UPDATE_GOLDEN=1`) → `tests/fixtures/golden/4og_notbeleuchtung.png` aktualisiert. +1 Test. 492 passed/5 skip, ruff clean, Schema in sync. **Render-Ausbau-Branch fertig (3 Commits) — PR gegen main wartet auf User-GO.**
- 2026-09-01 **F2 (Render-Ausbau): Commit 2 — Beidseitig-Doppelpfeil für `richtung="gerade"`.** Der beidseitige RZ (Profi-Plan RZ_PLPR, Referenz §1.1) steht in Flur-Mitte zwischen zwei Ausgängen und weist in BEIDE Richtungen. `inserter.insert_platzierung` zeichnet bei `richtung="gerade"` jetzt zwei horizontale Pfeil-Blocks (`notlicht_ks_stiege_links` + `_rechts`) am selben Punkt, gemeinsam um `rotation_deg` gedreht (Wasserscheide) statt eines Einzelsymbols; primärer (linker) Insert wird zurückgegeben und trägt allein den Stromkreis-XDATA-Tag. Refactor: `_skalen()` + `_insert_block()` extrahiert (DRY, Einzel- + Doppel-Pfad teilen Scale/Mirror/Blockref). **Kein Contract** (`richtung="gerade"` existiert schon), NUR `symbols/inserter.py`. 4og-Golden hat keine gerade-Platzierung → Insert-Zähl-Tests + Visual unverändert. +2 Tests (`test_inserter.py`). 491 passed/5 skip, ruff clean, Schema in sync.
- 2026-09-01 **F2 (Render-Ausbau, Branch `leonis/render-ausbau`, kein Contract): Commit 1 — DIN_SIBEL-Layer-Schema (Lean-Rename).** Ad-hoc-`E_*`-Layer → DIN_SIBEL-Profi-Konvention (Referenz `knowledge/extracted/PROFI_DIN_PLAN_UND_VORSCHRIFTEN.md` §1.3), 1 Layer je Plan-Element, keine Symbol-internen Änderungen. Vorher→Nachher: `E_Sicherheitsbeleuchtung`→`din_SIBEL_10_emergency_lighting` · `E_Stromkreis_Label`→`din_SIBEL_61_labeling` · `E_Notbeleuchtung_Hoehenkote`→`din_SIBEL_52_info` · `E_Notbeleuchtung_Stueckliste`→`din_SIBEL_70_legend_green` · `E_Notbeleuchtung_Legende`→`din_SIBEL_70_legend_white` · `E_Notbeleuchtung_Plankopf`→`din_SIBEL_99_titleblock` · `E_Notbeleuchtung_Pruefbericht`→`din_SIBEL_99_inspection`. `ARCH_Raum`/`ARCH_Fluchtweg` unverändert (Architektur-Hintergrund, kein SIBEL). **Naht-Detail:** der physische Lib-Layer heißt weiter `E_Sicherheitsbeleuchtung` (Block-Geometrie liegt auf Layer `0`, erbt den INSERT-Layer) → `library.sync_layers` benennt ihn beim Import auf `din_SIBEL_10_...` um + hält den Grün-Override; Visual-Golden **unverändert grün** (kein Farb-Drift). Dateien: `render/dxf_renderer.py`, `symbols/library.py`, `tests/render/test_render_dxf.py`. 489 passed/5 skip, ruff clean, Schema in sync (kein Contract).
- 2026-09-01 **Leonis: Norm-Integration Platzierung (Track A, Branch `leonis/norm-integration-platzierung`, kein Contract, 437 grün).** Ziel: die schon in `normwissen/data` kodierten Norm-Werte konsequent durch die Platzierung fließen lassen statt hardcoden. (A1) `deckung.verdichte_fluchtweg` zieht `ziel_lux` aus `anf.min_lux` statt Konstante 1,0 (norm-belegt via `anf.quelle`). (A2) `flaechen_strategy` verdichtet **Antipanik bis zum 0,5-lx-Nachweis** (`_antipanik_punkte`) statt blind `mindest_anzahl` — der 0,5-lx-Wert war bis dato tot; kleine Räume unverändert (4er-Raster erfüllt 0,5 lx), große Halle verdichtet (Cap gegen Überproduktion). (A3) neue Validierungsregel **2-Leuchten-Redundanz je Fluchtweg-Abschnitt** (EN 50172 / §5.1.8), Warnung nicht Hard-Fail. (A4) `lux_raster`-Fallback-Höhe 2,5→2,0 m (EN-Mindesthöhe). **Mollgasse-EG-Durchstich unverändert:** 15 RZ + 21 SL, Prüfstatus **ok**, alle 103 Abschnitte ≥ 2 Leuchten (Redundanz greift, kein Rauschen). Schema unverändert (kein Contract). PR noch offen (User-GO). **Track B/C (Roadmap):** neue abfragbare Norm-Werte (Ud/Flächen-Trigger 60m²·8m²/Arbeitsplatz-Lux 15·5) = Contract-Erweiterung `NormRegelwerk` (3-Owner, Enis-Daten); Pflichtpunkte (Aufzug/Erste-Hilfe/Löschgerät) = Selman-RaumModell-POIs.
- 2026-09-01 **Hinweis an Enis:** PR #60 ist bereits **gemergt** (2026-08-31 20:18, `8eaa1f8`) — nichts mehr zu approven. Der „Track-A"-Punkt `LbReviewRequired→HTTP 500` ist ebenfalls erledigt: `pipeline.py:117-122` fängt `LbFehler` (→ `lb_review`-Flag, Plan läuft norm-getrieben weiter), `api/main.py:91-92` mappt jede Exception auf 422. Kein 500-Pfad mehr.
- 2026-08-31 Enis **Tagesabschluss**: vier PRs gemerged — #60 (LB-Parser + API-Naht, fail closed, Raumtyp-Vokabular synchronisiert, an 4 realen LB-PDFs gegengeprueft), #67 (`lb_review` erreicht den Client; `/plan` UND `/projekt`), #68 (Placement-Decision-Matrix: 25 Regeln + 4 Hard Stops + Ground-Truth Mollgasse), #69 (Sonderstellen-Spezifikation, **Contract NICHT geaendert**). `origin/main` = `d915342`, 486 passed/5 skip, ruff sauber, Schema in sync, keine offenen PRs, keine ungesicherte lokale Enis-Arbeit. **Einziger Blocker: das 3-Owner-GO fuer den Sonderstellen-Contract (Option A) steht aus** — @mvpo3 und @polatselman, Entscheidung erbeten; Details `docs/SPEC_SONDERSTELLEN_CONTRACT.md`. Bis zum GO wird `hauptengine/contracts/**` von Track B nicht angefasst. Track-B-unabhaengige Folgethemen (Lux-Niveau an Betonungsstellen, Niveauaenderung am Original, Quellenlage OVE R 12-2 / E 8350 / TRVB E 102, Wegbreite/Randstreifen) stehen in `Handoff/ENIS.md` unter „MORGEN ZUERST".
- 2026-08-31 Enis: **Sonderstellen-Contract-VORSCHLAG** (`docs/SPEC_SONDERSTELLEN_CONTRACT.md` + `normwissen/data/sonderstellen.yaml` + `SonderstellenKatalog`, 17 Tests). **Kein Contract geaendert, kein Version-Bump — wartet auf 3-Owner-GO.** Vorschlag: ein generisches `RaumModell.sonderstellen[]` (Typen feuerloescher/hydrant/erste_hilfe/brandmelder/niveauaenderung) + zwei Raum-Flags `ist_barrierefrei` (§4.3.8) und `besondere_gefaehrdung` (§4.4.1). Schaltet **exakt** die 8 blockierten Matrix-Regeln frei (Test haelt Gleichheit fest), rein additiv, alle Felder mit Default. Typ-Namen sind nicht erfunden: der Profi-Plan `din_support_ReMi_Barawitzkagasse` fuehrt `din_Feuerloescher_F001` / `din_Hydrant_F002` / `din_ErsteHilfe_E003` (ISO-7010-Nummern). Datenquellen-Befund: **kein** Typ ist heute automatisch erkennbar — der Architekturplan traegt keine Sonderstellen (an Mollgasse geprueft), die LB liefert Lux ohne Koordinate, ein bestueckter Elektroplan liegt nicht im Repo. Der Vorschlag ist deshalb ohne Parser nutzbar. Die 5 lx an Feuerloescher/Hydrant bleiben ausdruecklich LB-Wert, `norm_lux()` gibt fuer JEDEN Typ `None`. Optionen A/B/C + Empfehlung (A) im Spec-Dokument. **@mvpo3 / @polatselman: Entscheidung erbeten.**
- 2026-08-31 Enis: **Placement-Decision-Matrix** (`normwissen/data/platzierung_regeln.yaml` + Query-API `PlatzierungsRegelwerk`). 25 Regeln (11 RZ, 14 SL) + 4 Hard Stops: Ausloeser -> Leuchtenart, Positionierungsziel, Orientierung, Abstand/Lux, Prioritaet, Ausnahmen, Konfliktregel, Quelle, Normreferenz, Review-Flag, Decision-Source. Entscheidungs-Hierarchie maschinenlesbar (hard_stop > lb_explizit > referenz_praxis > norm_default); `gewinner()` gibt bei Gleichstand `None` = Review. Keine zweite Regelwelt — Zahlen bleiben in en1838_grundwerte.yaml, referenziert ueber `*_ref`. **@mvpo3:** Track-A-Vorrat ist 17 umsetzbar / 5 teilweise / 8 durch Contract-1 blockiert; Details + Contract-Vorschlag `Sonderstelle` in `docs/PLACEMENT_DECISION_MATRIX.md`. **@polatselman:** die Raumerkennung erzeugt an Mollgasse (EG/1OG/1KG) **0 `stair_exit` und 0 `stair`-Knoten**, obwohl das EG zwei STIEGENHAUS-Raeume hat -> RZ-05/RZ-07 sind auf echten Plaenen unerreichbar. Ground Truth: es gibt **keinen** professionell gezeichneten Mollgasse-Notbeleuchtungsplan im Repo (Architekturplaene haben 0 Notbeleuchtungs-Layer) — die 7 GT-Faelle beschreiben deshalb die Ausloeser-Lage, nicht ein Soll-Ergebnis. 469 passed/5 skip.
- 2026-08-31 **Leonis (F1) Session-Ende — 11 PRs #46–#64 auf main (166c234), 434 grün, kein Contract.** Render: Höhenkoten (h=2,40), Farb-7-Inversion (Legende/Plankopf im Hell-PDF sichtbar), Schriftfeld-Leiste (Info-Blöcke gerahmt rechts), DoD-Visual-Golden-Harness + CI-Raster-Smoke. Platzierung: covers_segment-Fix (geometrische Deckung real 0→103), RZ an jedem Notausgang (§4.1.2 g auch graphlos) + sichtlinie-Symmetrie + Anker-Dedup (keine Doppelplatzierung <250mm). Validierung: Plausibilitäts-Regel + Symboldichte-Gate (quasi-leer=fehler), Auto-Prüfeinrichtungs-Hinweis (EN 62034 >20 Leuchten), „nahe"<2m, z=100/200 single-source. 2× ultracode-Gesamtaudit (adversarial, 7+7 Fixes). **Mollgasse EG = voll-konformer Plan (Prüf ok).** OG/DG bleiben F2-blockiert (Raumerkennung ~0 Typen). **NEU: Wissensbasis `knowledge/extracted/PROFI_DIN_PLAN_UND_VORSCHRIFTEN.md`** (echter Profi-DIN-Plan + AT/DE-Vorschriften) mit **15 Hauptengine-Empfehlungen** (Owner+Aufwand) — Roadmap in `Handoff/LEONIS.md`. Für die Hauptengine-Naht relevant: Symbol-Datenmodell-Erweiterung (TYPENAME/TYPENUMBER/Schaltungsart DL/BL) = **3-Owner-Contract-Slice**; `richtung=beidseitig` = schon `"gerade"` (kein Contract); EN-1838-Lux-Werte → Enis' NormRegelwerk; hervorzuhebende Stellen/Pflichtpunkte → Selmans RaumModell.
- 2026-08-31 Enis: **LB-Naht geschlossen** (Branch `enis/lb-parser`, rebased auf `9d3c080`). API zurück auf die main-Namen (`LbTextProvider` + Modul-`parse_lb`/`parse_bericht`), `registry.py` unangetastet. Raumtyp-Vokabular an #49/#57 angeglichen (+GARAGE/TECHNIK/LAGER/MUELLRAUM/KELLER, `TECHNIKRAUM`→`TECHNIK`, LAGER≠MUELLRAUM) — beide realen Elektro-LBs parsen jetzt durch. Fünf Feld-Lücken gegen main geschlossen (`projekt`, `batterie_standort`, Sonder-Lux-Split feuerloescher/hydrant, Norm-Schreibweise `OVE E 8101`, Inhaltsverzeichnis-Filter im Audit-Trail). Drei echte Fehler beim Test-Merge gefunden und behoben: `OverflowError` bei langer Ziffernfolge, verlorener Fluchtweg-Lux-Wert ohne Quantor, verlorene Bereichsregel wenn die Aussage in der ÜBERSCHRIFT steht ("2.11 In der Garage ist eine LED-Sicherheitsbeleuchtung herzustellen."). Alle 16 main-Tests übernommen + 1440-Regression + Drift-Gate `tests/contract/test_lb_raumtyp_naht.py`. 422 passed/5 skip, ruff clean, Schema in sync. Kein Contract-Touch.
- 2026-08-31 F2: Raumtyp **Bleed-Fix + LB-Vokabular** (stacked auf ↓, Branch `leonis/raumtyp-bleed-vokabular`). (a) Port `classify_room` von rohem Substring auf **Token/Wortgrenze** umgestellt — „gang" in „Ein/Zu/Aus-gang" und „terrasse" in „Terrassentür" typten fälschlich CORRIDOR/TERRACE (CORRIDOR=Fluchtweg → falsches Notlicht; real: „Zugang Müllraum"→GANG). Komposita-Köpfe (…küche/…zimmer) + Abstell-Prefix erhalten. (b) `raumtyp._EXTRA_DIRECT`: GARAGE/TECHNIK/LAGER/MUELLRAUM (token-exakt) — die `lb_override`-Inklusion/Exklusion für diese LB-Typen war **tote Regel** (der kanonische „Garage→SL"-Fall). A/B über 4 Projekte: 0 Regressionen (GANG-Counts unverändert), +5 korrekte MUELLRAUM (Herrenholz/Baufeld). Kein Contract-Touch. 340 passed/5 skip, ruff clean.
- 2026-08-31 F2: Raumtyp-Coverage — `raumtyp.raumtyp_flags` um österr. Plan-Abkürzungen erweitert (`_EXTRA_LABELS`, **token-exakt**, kein Substring-Bleed: „ar" nicht in „Garten"). VR→VORRAUM, AR→ABSTELLRAUM, **TRH→STIEGENHAUS** (sicherheitskritisch!), Loggia→BALKON. Fischamender BT1 EG **40→50 getypt** (UNKNOWN 20→10; Rest=Garten/Rampe/Aufzug=korrekt außen). Port `classify_room` unangetastet. Grundanalyse: Mollgasse-Geometrie-Typ (7/25) ist von Wand-Schlitz-Fragmenten blockiert (Gap-Healing-Sache), NICHT Vokabular → dort kein ehrlicher Win. Kein Contract-Touch. 319 passed/5 skip, ruff clean. Branch `leonis/raumtyp-coverage`.
- 2026-08-31 F2: LB-Parser **gehärtet** (`normwissen/lb/parser.py`, Handoff-Feinschliff #1/#2). Drei reale Fehlparses behoben, verifiziert gegen alle 4 realen LB-PDFs: (a) Fluchtweg-Lux jetzt **kontext-** statt erst-treffer-basiert → mo-Elektro **200→1 lx** (Aufzugsvorplatz-Distraktor ignoriert); (b) **„Lux"-Wortform** erkannt (`_LUX=(?:lx|lux)`) → Feuerlöscher+Hydrant 5 lx (vorher []); (c) Betriebsdauer nur im `betriebsdauer|auszulegen`-Fenster ohne `notruf` → GU-24h-Notrufakku (7380→) und Fischa-24h-Gewährleistung (1440→) **verworfen**, mo-Elektro bleibt 480; +Dezimal (8,5 Std→510) +Plausi-Caps (20 lx / 1440 min). Kein Contract-Touch. 314 passed/5 skip, ruff clean. Branch `leonis/lb-parser-haertung`.
- 2026-08-31 F2: `validierung.py` **LB-Konformität** (Befund 9/10, Branch `leonis/validierung-lb-konformitaet`, off aktuellem main). `pruefe(…, lb)` referenzierte `lb` bisher NIE — der QA-Layer, der die LB-übersteuert-Norm-Hierarchie absichern soll, prüfte die LB-Seite gar nicht. Neu: (9) **LB-Exklusion** — keine Aufheller-Leuchte (SL/Antipanik) in einem LB-ausgeschlossenen Raumtyp (Hard-Override, Fehler bei Verletzung); (10) **LB-Inklusion** — jeder LB-geforderte Raumtyp trägt ≥1 SL (Fehler wenn fehlt) → macht eine nicht-feuernde `lb_override`-Regel sichtbar (fängt genau den Dead-Rule-Bug aus PR #50). Lokaler Ray-Cast `_point_in_polygon` (kein platzierung-Import). Kein Contract-Touch. 325 passed/5 skip, ruff clean.
- 2026-08-30 F2 ②: LB-Parser `normwissen/lb/` (`LbTextProvider.parse_lb`) — Freitext/PDF → LBVorgabe. Fischa GK4: Stiegenhaus+Gänge exkl (kein SL), Garage inkl; +Skalare (8 Std→480, Umschaltzeit, Lux, System, Prüfung, Sonder-Lux, Norm-Bezug). Registry `bundle.lb`. Kein Contract-Touch. 290 grün. Branch `leonis/lb-parser`.
- 2026-08-30 F2 ①(B3): `raumerkennung/geometrie_typ.py` — STIEGENHAUS (STIEGE-Blöcke) + GANG (09-WEG/A_Fluchtweg) geometrisch, ohne Text-Label. Grund: Mollgasse-EG trägt 0 Raum-Namen (566 Texte geprüft). provider.parse Mollgasse: 0→7 typisiert (2 STIEGENHAUS, 5 GANG). 286 grün, kein Contract-Touch. Branch `leonis/raumtyp-geometrie`.
- 2026-08-30 F2: IES/LM-63-Import `photometrie/ies.py` (`lade_ies`) + Fixture `mini.ies` + `tests/normwissen/test_ies.py` (7). Gleicher `Photometrie`-Typ → F1-Naht unverändert. 170 Tests grün, ruff clean, schema in sync.
- 2026-08-30 Enis: OIB-/OVE-/Rechtsquellen-PDFs ins Repo (`knowledge/`), Beleg-Status
  je Wert in `normwissen/data/*.yaml`, Spec auf PR #14 nachgezogen. Kein Code-Delta
  (144 passed / 5 skipped wie main, schema in sync).
- 2026-08-30 F1-Naht: `lux.py`+`deckung.py` bekommen `i_cd_fn(γ)`-Callable (Photometrie-Injektion, grenz-sauber). 77 Tests grün. Verdrahtung in Hauptengine/pipeline noch offen.
- 2026-08-30 F2: `photometrie/ldt.py` + `__init__.py` + Fixture `mini.ldt` + `tests/normwissen/test_ldt.py`. 74 Tests grün, ruff clean. Schnittstelle READY (siehe oben).
- <2026-08-29> F1: Platzier-Regeln l=z·h + `richtung_durch_tuer` kodiert (Commit
  `e87a745`, Suite 95). Fischamender-Durchstich + DXF-Overlay in echten Grundriss.
  2 F2-Bugs oben dokumentiert. ⚠️ F1-Platzier-Code liegt versehentlich auf
  `selman/raumerkennung-dxf` (Git-Tangle) — Integration/Entwirrung offen.
- <S0> F2 umgelenkt → Raumerkennung. Branch `selman/raumerkennung-dxf`, Scaffold + Test grün (69 passed).
- <setup> F1 legt Worktree + dieses Board an. F2 startet mit (b) LDT.
